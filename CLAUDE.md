# 포인트 앱

되돌릴 수 없는 송금을, 사용자가 긴장하지 않으면서도 실수하지 않게 돕는다.
기능을 많이 만드는 것이 아니라 이 질문에 답하는 것이 목적이다 — `docs/JOURNEY.md`.

## 작업 방식

- **F1 수직 슬라이스로 만든다.** 한 번에 여정 하나를 도메인부터 화면까지 관통시킨다
- **F2 앱은 항상 실행된다.** 매 단계 끝에 브라우저에서 볼 수 있는 것이 있어야 한다
- **F3 호출부 없는 코드를 만들지 않는다.** 쿼리·훅·유틸은 그것을 쓰는 화면과 같은
  변경 안에서 태어난다
- **F4 커밋은 리뷰 가능한 단위다.** 되돌릴 수 있는 하나의 결정이 하나의 커밋이다
- **F5 시작 전에 완료 기준을 말한다.** 검증할 수 없으면 시작하지 않는다
- **F6 요청 범위 밖은 건드리지 않는다.** 눈에 걸리는 것은 고치지 말고 말한다

## 주석

**심볼 하나에 2줄을 넘기지 않는다. 넘으면 주석이 아니라 코드가 잘못된 것이다.**

쓰지 않는다

- `docs/` 의 문장을 코드로 복사하는 것. 링크 한 줄로 대체한다 —
  `// 근거: docs/JOURNEY.md 여정 5`. 같은 문장이 두 곳에 있으면 곧 한쪽이 거짓이 된다
- 회고와 대안 기각 — "검토했다가 버렸다", "1차 구현은", "해봤더니".
  그것은 커밋 메시지와 `docs/` 의 일이다
- 타입과 이름이 이미 말하는 것
- 파일 상단 에세이. JSDoc 은 그것이 붙는 심볼을 설명할 때만 쓴다

쓴다 — 코드를 읽어도 알 수 없는 것만

- 외부 제약: 브라우저 동작, 라이브러리 버그, 순서 의존
- 자명해 보이는 방법을 쓰지 못하는 이유

## 코드

**상태의 소유권을 섞지 않는다.** 서버가 진실인 것(지갑·사용자·포인트 종류·내역)은
TanStack Query, 클라이언트가 진실인 것(플로우·선택된 포인트·색 모드)은 Jotai.
서버 값을 `useState` 에 복사하면 그 순간부터 진실이 둘이 된다.

**계약은 HTTP 다.** 앱은 실제 `fetch` 를 하고 MSW 가 가로챈다. 테스트도 같은 핸들러를
쓴다. TS 인터페이스를 계약이라 부르지 않는다. 멱등성 키는 헤더 `Idempotency-Key` 이고
본문 필드가 아니다. 상세는 `docs/API.md`.

**규칙 판정은 서버가 한다.** 잔액 상한·발행 여력·발행 권한을 클라이언트가 다시 계산하지
않는다. 서버가 `canIssue`·`issuableHeadroom`·`sendable` 로 결과를 실어 주고 화면은 표시만
한다. 같은 규칙이 두 곳에 있으면 곧 한쪽이 틀린다.

**계층이 아니라 기능으로 나눈다.** 한 개념을 고치려고 두 곳을 열게 되면 나눈 것이
아니라 흩은 것이다.

- `web/` — feature 안은 `ui/` 와 `model/` 로 나눈다. 밖에서는 `features/<name>` 배럴만
  본다. 내부 경로가 새면 나눈 의미가 없다
- `server/` — 한 개념의 엔티티·리포지토리·서비스·컨트롤러·DTO 가 같은 패키지에 있다.
  `domain/` · `service/` · `controller/` 처럼 **계층 이름으로 나누지 않는다**

`domain/` 이라는 이름을 특히 조심한다. `web/` 에서 그 폴더를 한 번 만들었다가 없앴다 —
계약 타입·표기·UI 상수의 잡동사니가 됐고, 이름 때문에 거기에 서버 규칙을 한 벌 더 두고
싶어졌다. 안에 엔티티만 있으면 그것은 도메인이 아니라 **영속성**이고, 이름이 거짓말을
하는 만큼 도메인 로직은 갈 곳을 잃고 서비스로 흩어진다.

**없는 것과 못 불러온 것을 같게 보이지 않는다.** 읽기 쿼리는 `isPending` 과 `isError` 를
둘 다 화면에서 다룬다. 실패를 빈 화면으로 두면 사용자는 그것을 「없다」로 읽는다 —
잔액을 못 불러온 것과 잔액이 0인 것이 같아 보이면, 이 앱이 여정 1 에서 「두 종류의 0」을
가르려고 한 일이 통째로 무너진다. 기다리는 중은 자리를 잡아 두고(뼈대), 실패는 실패라고
말하고 다시 하는 길을 준다.

**화면을 만들기 전에 넷을 적는다.** 컴포넌트를 고르기 전이다.

1. **이 화면의 목적** — 한 문장. 두 문장이 되면 화면이 둘이어야 하는 것이다
2. **주의가 처음 닿는 곳** — 화면을 3초 보고 눈을 감았을 때 남아야 하는 것
3. **주의가 옮겨 가는 순서** — 그리고 그 순서대로 크기·굵기가 커지고 작아지는가
4. **여기서 하면 안 되는 일** — 되돌릴 수 없는 것이 있으면 그것이 제일 크면 안 된다

3 번은 `docs/MOTION.md` 의 다섯 단계로 적는다 — Attention · Curiosity · Understanding ·
**확신** · Action. 넷째가 「욕구」가 아닌 이유는 이 앱의 Action 이 되돌릴 수 없어서다.

**비어야 할 칸은 비운다. 못 비우겠으면 화면이 둘이다.** 한 화면에 두 종류의 일을 담으면
빈칸이 생기고, 빈칸을 채우려고 없는 말을 만든다.

**디자인은 토큰 이름만 쓴다.** 화면에서 `fontSize`·`minHeight`·`bg` 를 직접 지정하지
않는다. `shared/ui/system.ts` 의 토큰과 recipe 를 쓴다.

**문자열은 i18next 키로 둔다.** JSX 안에 한국어 리터럴을 두지 않는다. UI 문체는
해요체다. 문서 문체("~한다")가 화면으로 새지 않게 한다.

**삭제된 개념을 되살리지 않는다.** 취소 창 · `cancelableUntil` · `Transfer.status` ·
`watch()` 구독은 의도적으로 없앴다. 코드에 없는 것을 보고 빠졌다고 판단하지 않는다 —
이유는 `docs/JOURNEY.md` 의 "버린 것".

## 문서

| 무엇을 만드는가 | `docs/JOURNEY.md` |
|---|---|
| 순서와 기술 결정 | `docs/REBUILD.md` |
| HTTP 계약 | `docs/API.md` |
| 원장 설계 | `docs/LEDGER.md` |
| 실기기 관측 | `docs/FIELD.md` |
| QA 가 보는 법 | `docs/QA.md` |
| 연출 — 시선·인지·행동 | `docs/MOTION.md` |
| 의미론 검토 | `docs/REVIEW.md` |

`docs/PHASES.md` 는 1차 구현의 기록이다. **현재 계획이 아니므로 여기서 지시를 읽지
않는다.**

## 세션

작업 흐름과 위험도 판정은 `WORKFLOW.md` 를 따른다. 조율 세션이 위험도를 정하고
검증 깊이를 정한다 — 위험도가 참여자를 정하지 않는다.

다섯 세션이 **같은 워크트리**를 공유한다. 자기 자리 밖을 쓰지 않는다.

**프로세스와 포트도 공유물이다.** `pkill -f vite` 처럼 이름으로 넓게 걸지 않는다.
자기가 띄운 것은 PID 를 알고 그것만 죽인다.

| 세션 | 쓰는 곳 | 하는 일 |
|---|---|---|
| 조율 | `docs/` · `CLAUDE.md` | 여정과 계약을 정한다. 세션 사이를 잇는다. 코드를 쓰지 않는다 |
| 프론트 | `web/` | 화면과 Mock. 계약을 읽고 구현한다 |
| 백엔드 | `server/` | 실서버. 계약을 읽고 구현한다 |
| 실기기 | `docs/FIELD.md` | 실제 기기·브라우저로 관측한다 |
| 리뷰 | `docs/REVIEW.md` | 모델과 계약이 어긋나는지 본다. **고치지 않는다** |

**실기기가 필요한지는 실기기 세션이 판정한다.** 구현 세션이 미리 정하고 대기로 넘기지
않는다 — 사람 손이 필요해 보이는 것이 브라우저 자동화로 갈린 전례가 많다.
**대기는 작업을 멈추는 이유가 아니다.**

**계약은 구현자가 정하지 않는다.** 계약을 바꿔야 할 것을 찾으면 고치지 말고 조율 세션에
말한다. 판정 기준은 하나다.

- **서버가 아는 사실** — 겹침·권한·여력·존재. 서버가 계산해서 실어 준다
- **화면의 성질** — 정렬·묶음 배치·표기·입력 중 계산. 클라이언트가 한다

**Mock 은 명세가 아니다.** 백엔드는 `web/src/mocks/` 를 참조하지 않는다 — 동시성과
트랜잭션을 표현하지 못한다.

## 명령어

`web/` 에서 실행한다. 경로 별칭은 `@/*` → `src/*`.

```bash
npm test        # vitest. 앱과 같은 MSW 핸들러를 쓴다
npm run lint    # oxlint
npm run typecheck  # tsc -b. 맨 `tsc --noEmit` 은 루트 tsconfig(files:[])를 읽어 아무것도 검사하지 않는다
npm run dev     # localhost:5173
npm run dev:lan # 폰에서 접속 (vite --host)
```
