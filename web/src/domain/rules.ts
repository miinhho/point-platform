// 도메인 규칙 상수. 근거는 docs/JOURNEY.md 의 해당 순간에 있다.

/**
 * 확정에 필요한 홀드 시간 (여정 5 — 보낼지 결정한다).
 *
 * 금액과 무관하게 일정하다. 금액이 커질수록 무거워지는 설계를 검토했다가 버렸다 —
 * 홀드가 존재하는 이유는 위험도 전달이 아니라 오터치 방지다. 탭은 스크롤 중 스칠 수
 * 있지만 홀드는 지속적 의사표시라서 실수로 하기 어렵고, 누르고 있는 동안 화면을
 * 읽을 시간이 생긴다.
 *
 * **취소 창을 버린 뒤로 이것이 마지막 방어선이다.** 뒤에 아무것도 없다.
 */
export const HOLD_MS = 600

/** 키패드 입력 자릿수 상한. 표기가 깨지는 구간까지 받을 이유가 없다. */
export const MAX_AMOUNT_DIGITS = 13
