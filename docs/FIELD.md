# 현장 검증 — 두 세션 운영법

`JOURNEY.md` 의 "확인 방법" 중 절반은 **브라우저에서 확인할 수 없다.** 손가락 크기,
한 손 도달 범위, 스프링의 체감, 실제 GPU 위의 프레임, WebView 티가 나는 순간은
에뮬레이션으로 답이 안 나온다. 그래서 세션을 둘로 나눈다.

## 두 세션

| | 작업 세션 | 실기기 세션 |
|---|---|---|
| 하는 일 | 코드를 쓴다 | 만져보고 기록한다 |
| 저장소 | `web/`, `app/`, `docs/` 전부 | **`docs/FIELD.md` 만** |
| 답할 수 있는 질문 | 상태 기계가 맞나, 계약이 맞나, 토큰이 일관된가 | 눌러지나, 닿나, 부드러운가, 웹처럼 보이나 |
| 답할 수 없는 질문 | 실제로 어떤 느낌인가 | 왜 그렇게 만들었나 |

**핵심 규칙: 실기기 세션은 `web/src` 를 고치지 않는다.** 같은 워크트리를 공유하기
때문이다. 고치고 싶은 것이 생기면 아래 "현장 기록"에 쓰고 작업 세션이 고친다.
관찰과 수정을 한 세션이 겸하면, 관찰이 자기 수정을 정당화하는 방향으로 휜다.

### 공유되는 것은 파일만이 아니다

R2 에서 두 세션이 **같은 브라우저 탭**을 조작하고 있었다는 것이 드러났다. 한쪽이
클릭하지 않았는데 화면이 혼자 바뀌었고, 22:30~22:37 사이의 관측이 서로 오염됐다.
파일만 나눈 규칙으로는 부족하다. 실제로 공유되는 자원이 셋 더 있다.

| 자원 | 증상 | 대처 |
|---|---|---|
| `agent-browser` 의 `default` 세션·탭 | 상대의 클릭이 내 화면에 들어온다 | `AGENT_BROWSER_SESSION` 을 각자 다르게 (`dev` / `field`) |
| Mock 의 인메모리 원장 | 잔액·최근 목록이 서로 오염된다 | 상태에 의존하는 관측은 앞뒤로 원장을 리셋하고 잡는다 |
| 워크트리 파일 | HMR 로 상대의 수정이 내 화면에 즉시 반영된다 | 관측 시작 전에 코드 상태(스프링 값 등)를 기록에 적는다 |

이 호스트는 `--args "--no-sandbox"` 없이는 브라우저가 뜨지 않는다
(`kernel.apparmor_restrict_unprivileged_userns=1`).

## 지금 실기기에서 확인할 수 있는 것 / 없는 것

Phase 7(RN 셸)이 아직 없다. 그래도 폰의 Chrome 으로 개발 서버를 열면 여정 1~6 의
**손의 문제**는 오늘 바로 확인된다.

| 확인 가능 (지금, 폰 Chrome) | Phase 7 이후에만 (WebView 안) |
|---|---|
| 터치 타깃 크기, 한 손 도달 | 오버스크롤 바운스 제거 확인 |
| 화면 전환 스프링의 체감·프레임 | 탭 하이라이트·롱프레스 메뉴 제거 확인 |
| 한글 병기가 실제 글자 크기에서 읽히는가 | safe area / edge-to-edge |
| 다크 모드의 숫자 가독성 | 시스템 back 버튼 |
| 홀드 확정이 스크롤과 충돌하는가 (Phase 4 이후) | 햅틱 · 생체인증 |

## 연결

작업 세션이 개발 서버를 LAN 에 연다.

```bash
cd web && npm run dev:lan          # vite --host
```

**지금 떠 있는 주소: `http://172.30.1.19:5173/`** (호스트 무선 인터페이스 `wlo1`).
폰을 같은 Wi-Fi 에 두고 이 주소를 연다. 주소가 안 열리면 폰과 호스트가 다른 AP 에
붙어 있거나 AP 가 클라이언트 격리를 켠 것이다.

`adb` 로도 붙을 수 있다. 그러면 `adb reverse tcp:5173 tcp:5173` 후
`http://localhost:5173` 이고, 무엇보다 `chrome://inspect` 로 **프레임 실측**이 된다.

**adb 는 무선으로 붙인다.** R1 에서 USB 경로는 끝까지 가지 못했다 (충전 전용 케이블).
무선 순서는 이렇다 — 페어링 포트와 연결 포트가 **다르다**는 점이 함정이다.

```bash
# 폰: 개발자 옵션 → 무선 디버깅 → 기기 페어링 (코드와 ip:port 가 뜬다)
adb pair 172.30.1.31:42905 <6자리 코드>
adb mdns services            # 페어링 후에야 _adb-tls-connect 가 노출된다
adb connect 172.30.1.31:42241
```

페어링만 하고 `adb devices` 를 보면 `offline` 로만 뜬다. 페어링 뒤 mdns 재조회가 필요하다.

**눈으로 본 인상을 프레임 수치로 적지 않는다.** 실측이 안 되면 "부드럽다/걸린다"로만
쓰고, 수치는 adb 가 붙은 뒤에 채운다.

## 채널

기록은 이 파일이 정본이다. 오간 메시지는 사라지지만 파일은 남고, 남아야
`JOURNEY.md` 의 "확인 방법"에 대한 답이 된다. 메시지는 **상대를 깨우는 용도로만** 쓴다.

Orca orchestration Run 하나를 공용 우편함으로 쓴다.

```
run_a09932d74361   포인트 앱 현장 검증 — 작업 세션 ↔ 실기기 세션
```

실기기 세션이 처음 시작할 때 한 번:

```bash
orca orchestration run-use --id run_a09932d74361
```

이후 양쪽 모두:

```bash
orca orchestration send --run run_a09932d74361 --to run:run_a09932d74361 \
  --subject "R1 현장 기록 올림" --body "전환 방향 2건, 눌림 피드백 1건"
orca orchestration check --run run_a09932d74361      # 읽고 읽음 처리
orca orchestration check --run run_a09932d74361 --peek  # 읽음 처리 없이 보기
```

`--from` 에는 **터미널 핸들(`term_...`)만** 넣는다. 사람이 읽는 이름을 넣으면
`stable_pane_required` 로 실패한다. 보통은 생략하면 되고, 그러면 `ORCA_PANE_KEY` 로
자기 터미널이 자동으로 잡힌다.

**중요 — `check` 는 pull 이다.** 상대는 `check` 를 실행하는 순간에만 메시지를 본다.
작업 중인 상대를 실제로 깨우려면 프롬프트에 직접 타이핑한다:

```bash
orca terminal list                                    # 상대 핸들 확인
orca terminal send --terminal <term_...> --enter \
  --text "FIELD.md R1 결정 섹션 업데이트했다. check 해봐."
```

`orca orchestration check --wait --timeout-ms <n>` 로 블로킹 수신도 되지만, 그 세션은
기다리는 동안 아무것도 못 한다. 폰을 만지고 있어야 하는 실기기 세션에는 맞지 않는다.

## 라운드 형식

한 라운드 = 요청 → 기록 → 결정. **결정이 없으면 그 라운드는 끝난 게 아니다.**

기록에는 다음이 들어간다. 없으면 되돌아볼 수 없다.

- 기기 / OS / 브라우저 버전
- 여정의 어느 순간인가
- 무엇을 했나 (재현할 수 있게)
- 무엇이 보였나 (해석 말고 관찰)
- 기대와 다른 점

---

# R1 — Phase 2 (디자인 시스템 · 화면 전환)

## 확인 요청 · 작업 세션

- [ ] **전환 방향** — 최근 보낸 사람을 탭했을 때 새 화면이 **오른쪽에서** 들어오고,
      뒤로 갔을 때 **왼쪽에서** 들어오는가. 방향을 헷갈리는 순간이 있나
- [ ] **전환 속도** — 스프링(stiffness 420 / damping 42)이 폰에서 굼뜬가 급한가.
      브라우저에서는 적당했는데 이건 데스크톱 GPU 기준이다
- [ ] **전환 중 글자** — 전환 도중 화면 캡처에서 글자가 사라지는 순간이 있었다.
      헤드리스 렌더 아티팩트로 보이지만, 실기기에서 글자가 깜빡이거나 흐려지면
      `will-change: transform` 의 레이어 승격 문제다. **이건 실기기에서만 답이 난다**
- [ ] **눌림 피드백** — 탭 하이라이트를 지웠다. 눌렀는데 눌린 것 같지 않은 요소가 있나
      (최근 목록 행 / 하단 버튼 두 개 / 색 모드 3택)
- [ ] **한 손 도달** — "다른 사람에게 보내기"와 "포인트 발행"에 엄지가 닿나.
      색 모드 3택은 헤더 오른쪽 끝인데, 닿지 않아도 되는 자리라 거기 두었다. 맞나
- [ ] **금액 크기** — `3,240,000 P` 와 `삼백이십사만 포인트`가 팔 길이에서 읽히나.
      한글 병기가 노이즈로 느껴지나
- [ ] **다크 모드** — 자동/밝게/어둡게를 전환할 때 숫자가 뭉개지는 조합이 있나.
      야외 밝기에서도 해보면 좋다

## 현장 기록 · 실기기 세션

<!-- 여기에 append. 위 요청에 없는 것도 눈에 걸리면 적는다. -->

### 환경 (2026-08-18)

- 기기: Galaxy S22 (SM-S901N), Android 16 (SDK 36), 빌드 `BP2A.250605.031.A3.S901NKSS9GZE5`
- 브라우저: **Brave Nightly 1.95.76** 로 열었다 (foreground = `com.brave.browser_nightly`).
  Chrome 148.0.7778.120 도 설치되어 있으나 사용하지 않았다. R1 요청서는 "폰 Chrome" 을
  전제로 쓰였으므로 브라우저가 다르다 — 특히 "전환 중 글자" 항목은 나이틀리 빌드의
  렌더 버그와 구분되지 않는다
- 화면: 1080x2340 물리, density 480 (= CSS 360x780), 디스플레이 모드 120 / 96 / 60 fps 지원
- 접속: 폰 Chrome → `http://172.30.1.19:5173/` (LAN 직접). 첫 시도에 열렸다

### adb — 무선으로 붙었다

USB 케이블은 PC 에 꽂혀 있었으나 `lsusb` 에 안드로이드 기기가 없었다 (충전 전용 케이블).
폰에서 무선 디버깅을 켠 뒤:

1. `adb mdns services` 비어 있음 (페어링 전)
2. 개발자 옵션 → 무선 디버깅 → 페어링 코드로, `adb pair 172.30.1.31:42905 <코드>` → 성공
3. 페어링 후 `adb mdns services` 에 `_adb-tls-connect._tcp 172.30.1.31:42241` 노출
4. `adb connect 172.30.1.31:42241` → `device`

**즉 FIELD.md 의 "복구 순서"는 이 기기에서 USB 경로로는 끝까지 못 갔고, 무선 페어링이
답이었다.** 페어링 포트(42905)와 연결 포트(42241)가 다르다는 점이 걸린다 — 페어링만
하고 `adb devices` 를 보면 `offline` 로만 뜬다. 페어링 후 mdns 재조회가 필요했다.

프레임 실측 경로가 열렸다.

### R1-1 시스템 back — 페이지를 떠난다

**관찰 시각 ~21:40. 코드 상태: `web/src/ui/useSystemBack.ts` 가 아직 없던 시점.**
(작업 세션이 21:52:52 에 이 파일을 만들었다. 아래 관찰은 그 전의 것이다.)

여정: 홈 → 최근 보낸 사람 탭 → 금액 입력 (`enterAmount`, depth 2)

한 것: 금액 입력 화면에서 안드로이드 back **버튼**.
(정정: 이 기기는 `navigation_mode = 0` — 3버튼 내비게이션이다. 제스처 back 이 아니다.)

보인 것: **앱의 홈으로 돌아가지 않고 페이지를 떠났다.** 돌아오면 앱이 처음부터 다시 로드된다.

화면 안의 뒤로 버튼은 별개로 확인한다 (아래).

기대와 다른 점 — **없다고 볼 수도 있다.** 사용자 지적: "이건 앱이 아니라 웹이니까
당연한 결과다." 맞다. 브라우저에서 back 은 히스토리 back 이고, 앱이 히스토리를 쓰지
않으면 페이지를 떠나는 게 기본 동작이다. 관찰 자체는 놀랍지 않다.

**기록해 두는 이유는 코드의 주석과 어긋나기 때문이다.** `src/App.tsx` 의 `handleBack`
주석은 "웹에서는 브라우저 뒤로가기 (...) 가 이 함수를 부른다" 라고 쓰여 있는데,
`web/src` 어디에도 `popstate` / `history.pushState` 가 없다. 즉 웹에서는 그 경로가
연결되어 있지 않다. 실기기 관찰은 그것과 일치한다.

Phase 7(RN 셸)에서 `BackHandler` 가 붙으면 이 항목은 다시 확인해야 한다. 지금 확인된
것은 "웹에서는 안 붙어 있다" 까지다.

### R1-2 전환 체감 속도 — 살짝 느리다

**관찰 시각 ~21:45. 코드 상태: 스프링 `420 / 42 / 0.9`.**
(작업 세션이 21:52:52 에 `520 / 44 / 0.8` 로 바꿨다. 아래는 바뀌기 전의 체감이다.)

스프링 `stiffness 420 / damping 42 / mass 0.9`, `useReducedMotion()` 은 false
(adb 로 확인: `transition/animator/window_animation_scale` 모두 `1.0`, 접근성
애니메이션 제거 꺼짐). 즉 크로스페이드 폴백이 아니라 풀 스프링이 도는 상태다.

보인 것: **체감상 살짝 느리다.** 굼뜬 정도는 아니고 "조금 느리다" 쪽.

프레임 수치는 별도로 잰다 — 눈으로 본 인상과 섞지 않는다.

### R1-3 전환 중 글자 — 깜빡임이 많지는 않다

**관찰 시각 ~21:50. 코드 상태: 스프링 `420 / 42 / 0.9`.**

한 것: 홈 → 최근 보낸 사람 탭 → 화면 안 뒤로 버튼 → 홈. **8회 반복** (5회 요청, 확인차 3회 추가).

보인 것:

- 전환 도중 글자가 깜빡이거나 흐려지거나 사라지는 순간 **0회 / 8회 중**
- 화면 안 뒤로 버튼은 **8회 모두 홈으로 정상 복귀** (시스템 back 과 달리 앱 안에서 처리된다)

**헤드리스 캡처에서 보였던 글자 소실은 실기기에서 재현되지 않았다.** 이 항목은
"실기기에서만 답이 난다"고 적혀 있던 것이고, 답은 "실기기에서는 안 보인다" 이다.
`willChange: 'transform'` 의 레이어 승격 문제로 볼 근거가 이 기기·이 브라우저에서는 없다.

주의: Brave Nightly 1.95.76 에서의 결과다. Chrome 에서는 확인하지 않았다.

### R1-4 계측 방법 — `gfxinfo` 는 웹 프레임을 세지 않는다

`adb shell dumpsys gfxinfo <브라우저 패키지>` 로는 화면 전환 프레임을 잴 수 없다.
전환 8회를 돈 뒤에도 `Total frames rendered: 28` 이었다. 이 값은 브라우저의 **안드로이드
뷰**(툴바 등) HWUI 프레임이고, 웹 페이지 애니메이션은 Chromium 자체 컴포지터가 별도
서피스에 그리기 때문에 여기 안 잡힌다.

되는 방법:

```bash
adb shell dumpsys SurfaceFlinger --list | grep -i brave     # 레이어 이름 확인
adb shell dumpsys SurfaceFlinger --latency \
  "com.brave.browser_nightly/ChromeChildSurface#340338"
```

`ChromeChildSurface` 가 웹 컨텐츠 서피스다 (`ChromeNativeWindowSurface` 는 0 만 나온다).
출력은 128행 링버퍼이고 열은 `desiredPresent / actualPresent / frameReady` (ns).
`actualPresent` 의 차분이 프레임 간격이다. 유휴 상태에서는 프레임이 생기지 않으므로,
**전환 직전에 2초 정지 → 전환 1회 → 정지** 후 덤프하면 그 전환만 깨끗하게 담긴다.

첫 행의 `41666665` (= 24Hz) 는 유휴 상태 값이라 **믿으면 안 된다.** 실측 간격이 정답이고,
이 기기에서는 전환 중 간격 중앙값이 **8.4ms (약 119Hz)** 로 나왔다. 디스플레이가
120 / 96 / 60 fps 모드를 지원하고, 전환은 120Hz 로 그려지고 있다.

### R1-5 측정 중에 코드가 바뀌었다 — 이 라운드의 프레임 수치는 버렸다

21:53 에 "앞으로 1회" 전환을 깨끗하게 재려던 참에, 33초 전인 **21:52:52 에 작업 세션이
`ScreenTransition.tsx` 의 스프링을 `420/42/0.9` → `520/44/0.8` 로 바꿨다.** 같은 시각에
`useSystemBack.ts` 도 새로 생겼다. 우편함에는 알림이 없었다.

덤프에는 578ms / 570ms 짜리 버스트 두 개가 잡혔는데, 하나는 사용자의 전환이고 하나는
HMR 리로드 리페인트로 보인다. **어느 쪽이 어느 스프링인지 구분할 수 없어 수치를 버렸다.**

이건 이 파일 앞부분의 두 세션 규칙에 빠져 있는 항목이다. 규칙은 "실기기 세션이 `web/src`
를 고치지 않는다" 까지만 말하는데, 반대 방향도 필요하다:

> **작업 세션이 `web/src` 를 고치면 실기기 세션의 진행 중인 측정이 무효가 된다.**
> HMR 이 폰에 즉시 반영되기 때문이다. 고친 뒤에는 우편함에 알려야 하고, 실기기 세션은
> 측정 전에 하드 리로드로 코드 상태를 확정해야 한다.

현장 기록의 모든 항목에 **관찰 시각과 그때의 코드 상태**를 같이 적기로 한다. 없으면
나중에 그 관찰이 무엇에 대한 것이었는지 알 수 없다. 위의 R1-1 ~ R1-3 에 소급 적용했다.

## 결정 · 작업 세션

### R1-1 시스템 back — 고쳤다. 관찰은 맞았고 지적이 더 맞았다

"웹이니까 당연하다"가 맞다. 그런데 기록해 둔 이유 — **코드 주석이 거짓말이었다** — 가
더 중요했다. `App.tsx` 는 "웹에서는 브라우저 뒤로가기가 이 함수를 부른다"고 써 있었지만
`popstate` 를 듣는 곳이 없었다. `resolveBack` 은 이 앱의 핵심 판단인데, 그것을 실제로
부르는 경로가 웹에 하나도 없었다는 뜻이다. 테스트에서만 맞는 코드였다.

`ui/useSystemBack.ts` 를 만들어 히스토리에 **덫을 하나** 놓는다. back 을 누르면 덫이
튀고, 상태 기계가 소비했으면 덫을 다시 놓아 그 자리에 머문다. 소비하지 않았으면
(`exit`) 한 칸 더 물러나 진짜로 나간다. 히스토리에 화면이 쌓이지 않으므로 라우터가
아니고, back 의 의미는 여전히 `resolveBack` 혼자 정한다.

브라우저 검증 (360×780): 금액 화면에서 back → 홈 (페이지 유지, JS 전역 살아 있음).
홈에서 back → `about:blank` (앱을 떠난다).

이걸 고쳐서 얻는 것: 여정 5 의 "보내는 중에는 back 이 아무것도 하지 않는다"를
Phase 7 을 기다리지 않고 폰에서 확인할 수 있게 됐다.

### R1-2 전환 속도 — 올렸다. 다시 판단해 달라

`stiffness 420 / damping 42 / mass 0.9` → **`520 / 44 / 0.8`**. 대략 20% 빨라진다.
한 사람의 한 번 관찰이지만 추측으로 만든 값을 실측 인상보다 우선할 이유가 없다.
R2 에서 다시 본다 — 이번에도 느리면 더 올리고, 급해졌으면 되돌린다.

### R1-3 전환 중 글자 — 유지. 대신 조건을 좁혀 다시 묻는다

"많지 않다"로는 판단할 수 없다. `will-change: transform` 은 그대로 둔다 (빼면 보통
악화된다). R2 에서 **Chrome 으로** 횟수를 세어 달라 — Brave Nightly 의 렌더 버그와
구분되지 않는다는 지적이 맞다.

### 요청서의 잘못 — 브라우저를 특정하지 않았다

R1 요청서가 "폰 Chrome" 을 전제로 쓰였는데 지시에는 그 말이 없었다. 앞으로는 요청마다
어느 브라우저인지 적는다. 렌더 관련 항목은 **Chrome 고정**이다.

### 덤으로 잡은 것 — 360×780 에서 금액이 잘렸다

실기기 CSS 뷰포트가 360×780 이라는 기록을 보고 그 폭에서 다시 재 봤다. 13자리를 넣으면
금액과 한글이 오른쪽에서 잘렸다. **보낼 금액을 읽을 수 없게 되는 것**이라, 자릿수 오타를
막으려고 만든 화면이 자릿수를 감추고 있었다.

`ui/amountFit.ts` — 자릿수에 따라 글자 크기를 세 단계로 줄인다. 실제 폭을 재지 않고
자릿수로 정하는데, 측정은 렌더 후에야 가능해서 한 프레임 동안 잘린 상태가 보이고
그 한 프레임이 하필 사용자가 금액을 확인하는 순간이기 때문이다. 한글 표기는 줄바꿈을
허용했다 (`keep-all`) — 두 줄이 되어도 읽는 데 지장이 없고, 잘리는 것보다 낫다.

---

# R2 — Phase 3 (대상 선택 · 금액 입력) + R1 재확인

**브라우저: Chrome 고정.** Brave Nightly 로 본 것은 R1 에 남기고 섞지 않는다.
새로 만든 화면이 있으니 페이지를 새로고침하고 시작한다.

## R1 재확인

- [ ] **전환 속도** — 스프링을 올렸다. 아직 느린가, 적당한가, 급해졌나
- [ ] **전환 중 글자** — Chrome 에서 홈↔금액 사이를 10번 왕복하며 글자가 깜빡이거나
      사라진 **횟수를 센다.** 0 이면 0 이라고 적는다
- [ ] **시스템 back** — 금액 입력 중 back 제스처가 이제 홈으로 돌아오나.
      홈에서 back 은 앱을 떠나나. 돌아왔을 때 잔액이 다시 로드되나

## 대상 선택 (여정 2)

- [ ] **동명이인** — "다른 사람에게 보내기" → 목록 맨 위. 김지수 두 명이 **나란히**
      보이나. 주황색 핸들과 "같은 이름 2명" 안내가 눈에 들어오나
- [ ] **잘못 고르기** — 둘 중 하나를 일부러 고른 뒤, 금액 화면 헤더만 보고 **누구를
      골랐는지 알 수 있나.** 이게 여정 2 의 진짜 시험이다
- [ ] **한글 IME** — 검색창에 "김지수"를 친다. 조합 중에 글자가 깨지거나 커서가 튀거나
      마지막 글자가 사라지나. (controlled input + IME 는 실제로 자주 깨지는 조합이다)
- [ ] **자동 포커스** — 화면에 들어가자마자 키보드가 올라온다. 반가운가 방해인가.
      키보드가 목록을 얼마나 덮나

## 금액 입력 (여정 3)

- [ ] **만 키** — 처음 보고 무슨 키인지 아나. 3만을 넣어 보라
- [ ] **키 크기** — 최소 높이 56px 로 잡았다. 엄지로 오타 없이 눌리나.
      한 손으로 위쪽 `1 2 3` 줄에 닿나
- [ ] **지우기 길게** — ⌫ 를 0.5초 누르면 전부 지워진다. 발견되나.
      반대로 한 글자만 지우려다 **실수로 전부 지워지나** (이쪽이 더 중요하다)
- [ ] **자릿수 애니메이션** — 자릿수가 바뀔 때만 숫자가 살짝 튀어오른다.
      도움이 되나 소음인가
- [ ] **한글 병기** — `3,000,000` 아래 "삼백만 포인트"가 팔 길이에서 읽히나.
      `300,000` 과 `3,000,000` 을 번갈아 넣어 보고 **한글이 먼저 눈에 띄는지** 본다
- [ ] **잔액 초과** — 큰 금액을 넣으면 숫자·한글이 함께 붉어진다.
      **다크 모드에서 대비가 충분한가** (브라우저에서 흐릿해 보였다). 야외 밝기에서도
- [ ] **긴 금액** — `1` 을 13번 눌러 본다. 금액과 한글이 잘리지 않고 다 보이나

## 탭 횟수 (측정치 검증)

브라우저 측정으로는 3만이 4탭, 30만이 5탭, 100만이 6탭이다.

- [ ] 실제로 30만을 보내 보고, 5탭이 **부담으로 느껴지는지** 판단해 달라.
      부담이면 프리셋 칩이나 "최근 보낸 금액"을 넣는다. 아니면 넣지 않는다 —
      쓰지 않을 UI 를 미리 만들지 않는다

## 현장 기록 · R2

<!-- 실기기 세션이 append -->

### 환경 · R2 (2026-08-18 22:0x)

- 기기: 같음 (Galaxy S22 SM-S901N, Android 16, CSS 360x780, 120/96/60Hz)
- 브라우저: **Chrome 151.0.7922.137** (Brave 는 닫았다)
- 페이지 새로고침 후 시작
- 프레임 계측 레이어: `com.android.chrome/ChromeChildSurface#340465`

### R2-1 시스템 back — 덫이 먹는다

한 것: 홈 → 최근 보낸 사람 탭 → 금액 입력. 거기서 안드로이드 back **버튼**
(`navigation_mode = 0`, 3버튼 내비게이션. 제스처 back 은 이 기기에서 확인하지 않았다).

보인 것:

- 금액 입력 화면에서 back 버튼 → **홈으로 돌아온다.** 페이지를 떠나지 않는다
- 홈에서 back 버튼 → **앱을 떠난다**
- 돌아온 직후 잔액 자리에 **회색 네모(플레이스홀더)는 보이지 않았다**

R1-1 에서 back 이 앱을 죽이던 것이 `useSystemBack.ts` 로 해결됐다. 덫이 한 겹만
있다는 것도 확인된다 — 홈에서 한 번 더 누르면 그대로 나간다.

플레이스홀더가 안 보인 건 **관찰이지 결함 보고가 아니다.** 홈으로 돌아올 때 이전
`data` 가 유지되므로 `account` 가 `null` 이 되지 않고, 그래서 플레이스홀더가 뜰 자리가
없다. 깜빡임이 없는 쪽이 읽기에 낫다. 다만 **이 관찰로는 잔액을 실제로 다시 읽었는지
알 수 없다.** 눈으로 구분할 방법이 없다 — 판단은 작업 세션에 맡긴다.

### R2-2 전환 속도 — 적당하다

**코드 상태: 스프링 `520 / 44 / 0.8`, Chrome 151.**

R1-2 에서 `420/42/0.9` 를 "살짝 느리다"고 했다. 올린 값으로 여러 번 전환한 뒤의 답은
**"적당하다"** 이다. 급해지지 않았다.

즉 R1-2 의 인상은 스프링을 올리는 것으로 해소됐다. 더 올릴 이유는 지금 없다.

### R2-3 전환 중 글자 — Chrome 10회 왕복, 0회

**코드 상태: 스프링 `520/44/0.8`, `will-change: transform` 유지. Chrome 151.**

한 것: 홈 ↔ 금액 입력을 **10번 왕복**하며 들어갈 때와 나올 때 글자만 봤다.

보인 것: 글자가 깜빡이거나 흐려지거나 사라진 횟수 **0회 / 10회**.

R1-3(Brave Nightly 8회 0번)과 합쳐 **두 브라우저에서 0회다.** 헤드리스 캡처의 글자
소실은 실기기에서 재현되지 않는다. 레이어 승격 문제로 볼 근거가 이 기기에서는 없다.

이 항목은 닫아도 된다고 본다.

### R2-4 전환 프레임 실측 — 120Hz 를 유지한다

계측: `dumpsys SurfaceFlinger --latency "com.android.chrome/ChromeChildSurface#340520"`.
각 전환 전후로 정지해서 버스트를 분리했다 (앞뒤 공백 79초 / 243초 — 다른 활동과 섞이지 않았다).

| | 프레임 | 지속 | 간격 중앙값 | 최대 간격 | 16.7ms 초과 |
|---|---|---|---|---|---|
| 앞으로 (홈 → 금액) | 50 | **436ms** | 8.4ms (119Hz) | 25.1ms | 2 |
| 뒤로 (금액 → 홈, 화면 안 뒤로 버튼) | 58 | **545ms** | 8.4ms (119Hz) | 50.3ms | 4 |

읽는 법:

- 간격 중앙값이 양방향 모두 **8.4ms** 다. 전환 내내 120Hz 로 그려지고 있고,
  60Hz 로 떨어지지 않는다. 사용자가 "적당하다"고 한 것과 어긋나지 않는다
- **뒤로가 앞으로보다 109ms 길고, 끊긴 프레임도 2배다.** 최대 간격 50.3ms 는
  120Hz 기준 6프레임에 해당한다. 뒤로 갈 때는 홈이 다시 마운트되면서 데이터를 다시
  읽는 구간이 겹친다 — 다만 **원인을 실측으로 가른 것은 아니다.** 관찰은
  "뒤로가 더 길고 더 끊긴다"까지다
- 사용자는 이 끊김을 **말로 보고하지 않았다** (체감은 "적당하다"). 50.3ms 한 번은
  눈에 걸리지 않는 크기라는 뜻으로 읽힌다

### R2-5 대상 선택 — 동명이인은 성립한다. 그런데 키보드가 목록의 대부분을 덮는다

**Chrome 151, 검색어 없음(초기 상태). adb 스크린샷으로 직접 확인했다.**

한 것: 홈 → "다른 사람에게 보내기" → 대상 선택 화면. 아무것도 입력하지 않고 그대로 뒀다.

보인 것 — 동명이인 쪽은 의도대로다:

- 김지수 두 명이 **바로 붙어서** 1번·2번에 있다
- 그 위에 주황색으로 **"같은 이름 2명 · 핸들로 구분한다"** 안내가 있다
- 핸들 `@jisoo` / `@jisu` 가 주황색이고 이름 아래에서 눈에 들어온다
- 이름은 흰색·크게, 핸들은 주황색·작게 — 두 줄을 나란히 보면 구별된다

보인 것 — **화면에 목록이 2명까지만 들어간다**:

`autoFocus` 로 들어가자마자 키보드가 올라오고, 그 상태에서 스크린샷을 재면

| | device px | CSS px |
|---|---|---|
| 전체 화면 | 2340 | 780 |
| Chrome 주소창 (위) | 251 | 84 |
| **앱에 남는 세로 공간** | **859** | **286** |
| Chrome 자동완성 바 + 키보드 (아래) | 1230 | 410 |

**앱이 쓸 수 있는 세로가 286 CSS px, 전체의 37% 다.** 헤더 + 검색창 + 동명이인 안내가
그 위쪽을 쓰고 나면 목록 자리는 두 줄이다. mock 원장에 나 말고 5명이 있는데
**2명만 보이고 나머지 3명은 스크롤해야 나온다.**

기대와 다른 점 / 걸리는 것:

- 지금 동명이인 비교가 성립하는 것은 **김지수 두 명이 마침 최근 순서상 맨 위에 있기
  때문이다.** `groupHomonyms` 는 같은 이름을 붙여 놓지만, 그 묶음이 목록의 4번·5번에
  있으면 **키보드에 가려 두 줄이 함께 눈에 들어오지 않는다.** "비교는 두 줄이 눈에 함께
  들어올 때만 일어난다"는 주석의 전제가, 키보드가 올라온 실기기에서는 위치에 따라 깨진다
- 이건 브라우저에서는 안 보인다. 데스크톱 뷰포트에는 5명이 다 들어간다
- Chrome 자동완성 바(열쇠·카드·핀 아이콘)가 목록과 키보드 사이에 한 줄 더 들어간다.
  이건 브라우저 UI 라 Phase 7 의 WebView 에서는 사라진다. 다만 **키보드 자체는 남는다**

판단은 작업 세션 몫이다. 관찰은 "키보드가 올라온 상태에서 목록은 두 줄"까지다.

### R2-6 자동 포커스는 방해였다 · 헤더는 누구인지 말해준다 · 검색 지연은 mock 이 만든 것

**Chrome 151, 3버튼 내비게이션(`navigation_mode = 0`).**

**자동 포커스 — 방해다.** 대상 선택 화면에 들어가자마자 키보드가 올라온다.
사용자의 말: **"목록을 먼저 보고 싶었다."** R2-5 의 실측과 겹친다 — 키보드가 올라온
상태에서 목록은 두 줄뿐이라, 먼저 보고 싶은 목록이 실제로 가려져 있다.

즉 이 항목은 취향 문제가 아니다. 자동 포커스가 **화면의 63% 를 덮은 채로 시작한다.**

**잘못 고르기 — 화면이 말해준다 (합격).** 김지수 두 명 중 두 번째(`@jisu`)를 골랐고,
금액 화면 헤더가 **`김지수 @jisu`** 로 이름과 핸들을 나란히 보여준다. 사용자는 기억이
아니라 화면을 보고 `@jisu` 임을 알아봤다. 여정 2 의 시험은 통과한다.

다만 캡처에서 걸리는 것: **대상 선택 화면에서 주황색이던 핸들이 금액 화면 헤더에서는
회색(`fg.subtle`)이다.** 동명이인을 골랐을 때야말로 핸들이 검증 대상인데, 검증이 필요한
마지막 화면에서 색이 죽는다. 사용자가 알아봤으므로 실패는 아니다 — 눈에 걸린 것으로만 적는다.

**핸들 검색이 살짝 느리다 — 원인은 mock 이다.** 사용자가 "핸들 검색이 살짝 느린 느낌"
이라고 했다. 폰이나 렌더 문제가 아니다:

- `api/sim.ts` — `latencyMs: 700, jitterMs: 300` (의도적으로 넣은 서버 지연)
- `api/useUserSearch.ts` — `DEBOUNCE_MS = 150`

즉 글자를 멈춘 뒤 **850~1150ms** 뒤에 결과가 바뀐다. 실기기가 느린 게 아니라 설계값이다.
이 값이 실기기에서 어떻게 느껴지는지가 관찰의 내용이고, 답은 **"살짝 느리다"** 이다.

**한글 IME** — 이 라운드에서 확인하지 못했다. 사용자가 핸들(영문) 검색 인상만 보고했다.
조합 중 깨짐 / 커서 튐 / 마지막 글자 사라짐은 다음 라운드로 넘긴다.

### R2-7 키패드 기하 — 캡처에서 실측했다. 크기 요구는 충족한다

**Chrome 151, 금액 입력 화면 스크린샷(1080x2340, DPR 3)에서 픽셀로 잰 값.**
이 기기는 density 480 이므로 **1 CSS px = 1 dp** 다.

키 글자 중심 좌표 (device px):

| | x=217 | x=540 | x=864 |
|---|---|---|---|
| y=1311 | 1 | 2 | 3 |
| y=1492 | 4 | 5 | 6 |
| y=1671 | 7 | 8 | 9 |
| y=1851 | 만 | 0 | ⌫ |

- **행 간격 180 device px = 60 CSS px** (세 간격이 181/179/180 으로 균일)
- **열 간격 323.5 device px = 108 CSS px**
- 즉 키 하나가 **108 x 60 CSS px (= 108 x 60 dp)**

요청서의 "최소 높이 56px" 은 **충족한다** (60dp). 일반적인 터치 타깃 권장선(44~48dp)
보다도 크다. **크기 자체는 문제가 아니다.**

`보내기 확인` 버튼: 중심 (539, 2066) device px, **320 x 54 CSS px**, 좌우 여백 20px.

다만 캡처에서 걸리는 것 — **키에 테두리도 배경도 없다.** 숫자 글자만 떠 있고 키의
경계가 시각적으로 없다. 크기가 60dp 여도 **사용자는 그 60dp 가 어디부터 어디까지인지
모른 채 누른다.** 오타가 나는지, 경계가 없는 게 불안한지는 손으로만 답이 난다.
사용자가 돌아오면 확인할 항목으로 남긴다.

### R2-8 자율 QA 는 진행할 수 없다 — 기기가 사라졌다

**22:24 시점. 사용자가 폰을 들고 외출했다.**

- `adb devices` → `offline`, 이어서 목록에서 사라짐
- `adb connect 172.30.1.31:42241` → `Connection refused`
- `adb mdns services` → 비어 있음
- `ping 172.30.1.31` → **응답은 온다. 다만 RTT 295~382ms** (LAN 치고 비정상적으로 높다)
- 개발 서버(`172.30.1.19:5173`)는 200 으로 살아 있다

폰이 같은 네트워크에는 남아 있지만 절전으로 잠들었고, 무선 디버깅 리스너가 닫혔다.
**adb 로 화면을 보거나 조작할 수 없다.** 사람 없이 진행하는 QA 도 adb 를 전제로 하므로
같이 막힌다 — `input tap` 도 `screencap` 도 기기가 있어야 한다.

복구 순서 (사용자가 돌아오면):

1. 폰 화면을 켜고 같은 Wi-Fi 확인
2. 개발자 옵션 → 무선 디버깅이 **꺼져 있으면 다시 켠다**
3. 호스트에서 `adb mdns services` — `_adb-tls-connect._tcp` 로 **새 포트**가 뜬다
   (포트는 매번 바뀐다. 42241 은 이제 유효하지 않다)
4. `adb connect 172.30.1.31:<새 포트>`
5. 페어링은 유지되므로 `adb pair` 는 다시 하지 않아도 된다

**돌아오면 바로 자동화할 수 있게 준비해 둔 것** — 위 R2-7 의 좌표로 사람 손 없이
다음이 가능하다. 화면 크기가 같고 레이아웃이 안 바뀌었다는 전제다:

```bash
T() { adb shell input tap "$1" "$2"; }
# 13자리 잘림 확인: 1 을 13번
for i in $(seq 13); do T 217 1311; done
adb exec-out screencap -p > long13.png
# 30만: 3 → 0 → 만  /  잔액 초과: 9 를 여러 번
# 색 모드 3택은 홈 헤더 오른쪽 끝
```

**주의 — 좌표는 금액 입력 화면 한정이고, 작업 세션이 Phase 4/5/6 을 구현 중이라
레이아웃이 바뀌면 무효다.** 자동화 전에 반드시 `screencap` 으로 화면을 먼저 확인한다.

---

# 브라우저 확인 · **실기기 아님**

> 이 섹션은 **폰이 아니라 노트북 Chromium** 에서 나온 것이다. 위의 "현장 기록"과 섞지 않는다.
> 현장 기록이 정본인 이유는 "실기기에서 나왔다"가 보장되기 때문이고, 여기 내용을 거기
> 섞으면 그 보장이 깨진다.
>
> 조건: 뷰포트 **360x780, DPR 3** (폰의 CSS 뷰포트와 같게 맞췄다), `localhost:5173`.
> 사용자가 폰을 들고 외출해 adb 가 끊긴 동안(22:30~) 수행했다.
>
> **여기서 답이 나지 않는 것** — 한글 IME 조합, 엄지 오타율, 한 손 도달, 스프링 체감,
> 실제 GPU 프레임, 홀드 확정이 손에 어떻게 걸리는가. 폰이 돌아와야 한다.

## B-1 13자리 금액 잘림 — 해결됐다

**코드 상태: `ui/amountFit.ts` (21:55), `screens/EnterAmount.tsx` (21:55).**

`1` 을 13번 눌러 `1,111,111,111,111` 을 만들었다.

- 숫자 `1,111,111,111,111 P` — **한 줄에 다 들어간다.** `scrollWidth 320 == clientWidth 320`,
  오른쪽 끝 340px < 뷰포트 360px. 넘침 없음
- 폰트가 **23.4px** 로 줄었다 (자릿수에 따른 3단계 축소가 동작한다)
- 한글 `일조천백십일억천백십일만천백십일 포인트` 도 **한 줄에 다 들어간다**

작업 세션이 R2 결정에서 "13자리에서 잘리고 있었다"고 한 것은 이 뷰포트에서 재현되지
않는다. **고쳐진 것으로 확인된다.**

## B-2 대비 실측 — 초과 경고는 문제없다. 핸들과 `P` 가 문제다

`getComputedStyle` 로 전경/배경색을 읽어 WCAG 대비비를 계산했다. 배경은 조상으로
거슬러 올라가 실제로 칠해진 색을 썼다 (`body` 는 투명이라 그대로 쓰면 값이 틀린다).

**어둡게 (배경 `rgb(9,9,11)`), 금액 입력 화면 · 잔액 초과 상태:**

| 요소 | 색 | 크기/굵기 | 대비 |
|---|---|---|---|
| 금액 숫자 (초과) | `rgb(252,165,165)` | 23.4px / 700 | **10.48** |
| 한글 병기 (초과) | `rgb(252,165,165)` | 16px / 400 | **10.48** |
| "보낼 수 있는 금액을 넘었다" | `rgb(252,165,165)` | 14px / 400 | **10.48** |
| `만` · `⌫` | `rgb(161,161,170)` | 18px / 500 | 7.76 |
| `보내기 확인` (흰 글자 / `rgb(37,99,235)`) | | 16px / 500 | 5.17 |
| **헤더 핸들 `@jisoo`** | `rgb(113,113,122)` | 14px / 400 | **4.12** ✗ |
| **단위 `P`** | `rgb(113,113,122)` | 11.7px / 500 | **4.12** ✗ |

**요청서가 걱정한 "잔액 초과가 다크에서 흐릿한가"는 아니다.** 10.48 은 AAA(7:1)도 넘는다.
브라우저에서 흐릿해 보였다는 인상은 대비 부족이 아니었다.

**밝게 (배경 `rgb(255,255,255)`):**

| 요소 | 색 | 크기/굵기 | 대비 |
|---|---|---|---|
| 이름 (김지수 등) | `rgb(9,9,11)` | 18px / 500 | 19.9 |
| 한글 병기 · 섹션 라벨 | `rgb(82,82,91)` | 14~16px | 7.73 |
| `포인트 발행` | `rgb(100,27,163)` | 16px / 500 | 9.43 |
| `다른 사람에게 보내기` (흰 글자 / 파랑) | | 16px / 500 | 5.17 |
| **핸들 `@jisoo` `@taeyun` `@junho` `@seoyeon`** | `rgb(161,161,170)` | 14px / 400 | **2.56** ✗ |
| **홈 잔액 단위 `P`** | `rgb(161,161,170)` | 19.8px / 500 | **2.56** ✗ |

**밝은 모드가 어두운 모드보다 나쁘다.** 핸들이 2.56 이면 AA 기준 4.5 의 절반을 조금 넘는
수준이다. 19.8px/500 인 `P` 도 large text 예외(18.66px **굵게** 또는 24px)에 해당하지 않는다.

## B-3 동명이인 강조는 작동한다 — 그런데 마지막 화면에서 풀린다

`useUserSearch.ts` 주석은 "화면은 겹칠 때만 핸들을 키워야 한다 — 항상 크게 두면 검증
신호가 아니라 배경이 된다" 라고 말한다. **그 판단은 실제로 작동한다.** 대상 선택 화면
(밝게) 실측:

| | 색 | 크기/굵기 | 대비 |
|---|---|---|---|
| **동명이인** 핸들 `@jisoo` `@jisu` | `rgb(146,49,10)` | **16px / 500** | **7.88** |
| "같은 이름 2명 · 핸들로 구분한다" | `rgb(146,49,10)` | 12px / 500 | 7.88 |
| 동명이인 아닌 핸들 `@taeyun` 등 | `rgb(161,161,170)` | 14px / 400 | 2.56 |

겹칠 때만 색·크기·굵기가 셋 다 올라간다. 2.56 → 7.88 은 **3배 차이**다. 의도대로다.

**문제는 그 사람을 고른 다음이다.** 같은 `@jisu` 가 화면마다 이렇게 달라진다:

| 화면 | 색 | 크기 | 대비 (밝게) |
|---|---|---|---|
| 대상 선택 (고르는 순간) | 주황 | 16px / 500 | **7.88** |
| **금액 입력 헤더** | 회색 | 14px / 400 | **2.56** |
| 확정 화면 (`이렇게 보낸다`) | 진회색 | 14px / 400 | 7.73 |

즉 **금액 입력 화면에서만 검증 신호가 꺼진다.** 동명이인을 고른 사용자가 금액을 치는
동안, 자기가 두 김지수 중 누구를 골랐는지 확인할 유일한 단서가 2.56 으로 내려앉는다.
확정 화면에서 7.73 으로 되살아나지만, 그때는 이미 금액을 다 친 뒤다.

이건 실기기에서 사용자가 말한 것과 같은 것을 가리킨다 — R2-6 에 "대상 선택에서 주황이던
핸들이 금액 화면 헤더에서는 회색이다" 라고 눈으로 적어 두었고, 여기서 수치가 붙었다.
다만 **사용자는 그래도 `@jisu` 를 알아봤다.** 실패가 아니라 약한 자리다.

## B-4 탭 횟수 — 3만은 4탭이 맞다

홈에서 시작: 최근 목록의 김지수 탭(1) → `3`(2) → `만`(3) → `보내기 확인`(4). **4탭.**
`30,000 P` / `삼만 포인트` 가 뜨고 `보내기 확인` 이 활성화된다.

"다른 사람에게 보내기" 를 거치면 +1 해서 5탭이다. 요청서의 브라우저 측정치와 일치한다.
**부담으로 느껴지는지는 사람이 답할 항목이라 남겨 둔다.**

## B-5 확정 화면 (Phase 4) — 360x780 에 스크롤 없이 들어간다

작업 세션이 22:25~22:26 에 만든 `screens/Confirm.tsx`, `ui/HoldButton.tsx`,
`flow/useFlowRunner.ts` 가 붙은 상태에서 처음 본 화면이다.

- 제목 `이렇게 보낸다`
- 카드 안에: 받는 사람(`김지수 @jisu`) / 금액 `30,000 P` + `삼만 포인트` /
  `지금 잔액 3,240,000 P` / `보낸 뒤 남는 잔액 3,210,000 P`
- `보낸 뒤 남는 잔액` 만 진하다 (19.9) — 나머지 보조 정보는 7.73. 위계가 잡혀 있다
- 하단에 `꾹 눌러서 보내기` (파란 버튼, 5.17)
- **`scrollHeight 780 == innerHeight 780`, 가로 넘침 없음.** 스크롤 없이 다 들어간다

여정 4 의 "확인 다이얼로그를 쓰지 않고 송금 후의 세계를 이 화면에 전부 둔다"는 판단이
화면에 그대로 있다.

걸리는 것: **카드 아래와 버튼 사이가 약 380 CSS px 비어 있다.** 화면의 절반이다.
버튼을 엄지 자리에 두려는 의도로 읽히지만, 빈 공간이 이만큼이면 확정 화면이 "덜 채워진"
것처럼 보일 수 있다. **이건 눈으로 판단할 항목이라 폰에서 다시 봐야 한다.**

`꾹 눌러서 보내기` 의 홀드 시간·오발동·스크롤 충돌은 **손이 있어야 답이 난다.** 미확인.

## B-6 홀드 확정은 작동한다 — 짧게 눌러서는 발동하지 않는다

**코드 상태: `ui/HoldButton.tsx`, `domain/rules.ts` 의 `HOLD_MS = 600`.**

- **짧은 클릭 → 발동하지 않는다.** 확정 화면에 그대로 머문다. 여정 4 가 막으려던
  "읽지 않고 누르기"와 "스크롤하다 스치는 오터치"에 대해 이 속성이 성립한다
- **홀드(600ms 이상) → 발동한다.** `Sending` 을 거쳐 `Done` 으로 간다
- 홀드 경로가 포인터와 키보드 둘 다 있다 (`onPointerDown` / 스페이스·엔터 keydown).
  키보드로도 확정할 수 있다

`Done` 화면: `✓ 보냈다 / 김지수 @jisoo / 30,000 P / 남은 잔액 3,210,000 P / 확인`.
잔액이 3,240,000 → 3,210,000 으로 실제로 줄었다.

`Failed` 화면(주입된 `NETWORK` 실패): `보내지 못했다 / 요청이 서버에 닿지 못했다 /
지금 확실한 것 — 서버가 요청을 받았는지 알 수 없다`. 여정 6 의 "돈의 위치를 모름"에
대해 모른다는 것을 모른다고 쓰고 있다.

**600ms 가 오터치를 막기에 충분한지, 스크롤과 충돌하는지, 손이 언제 떨어지는지는
브라우저로 답이 나지 않는다.** 폰에서 확인할 항목으로 남긴다.

## B-7 우리 둘이 같은 브라우저 탭을 조작하고 있었다

**22:30~22:37. 이 라운드에서 가장 중요한 발견이라고 본다 — 앱이 아니라 운영 방식에 대한 것이다.**

무엇이 보였나: 내가 아무것도 클릭하지 않는 동안 화면이 혼자 바뀌었다.

1. `김지수 @jisoo 30,000 P` 완료 화면 (이건 내가 만든 것)
2. 3초 뒤 → `박태윤 @taeyun 50,000 P` 완료 화면 (내가 한 적 없다)
3. 다시 → `보내지 못했다 · 요청이 서버에 닿지 못했다` 실패 화면 (내가 한 적 없다)

원인: `agent-browser session list` 가 **`default` 세션 하나**, `agent-browser tab list` 가
**탭 `t1` 하나**만 보여준다. 작업 세션과 실기기 세션이 같은 데몬의 같은 탭을 조작한다.

**반대 방향이 더 나쁘다.** 내가 그 탭에서 `1` 을 13번 눌러 13자리를 만들고, `3`+`만` 으로
**김지수에게 30,000 P 를 실제로 보냈다.** 작업 세션이 그동안 무언가 재고 있었다면 내
입력이 그 관측에 섞였다.

해결: `AGENT_BROWSER_SESSION=field` 로 세션을 분리했다. 이 호스트에서는
`--args "--no-sandbox"` 가 없으면 Chrome 이 뜨지 않는다 (`No usable sandbox`).

**이 파일 앞부분의 두 세션 규칙에 빠져 있는 것.** 규칙은 파일만 나눠 놓는다
(`web/src` 는 작업 세션, `docs/FIELD.md` 는 실기기 세션). 그런데 실제로 공유되는 자원이
세 개 더 있었고, 셋 다 이번 라운드에서 실제로 사고를 냈다:

| 공유 자원 | 무슨 일이 났나 | 어디에 적었나 |
|---|---|---|
| 워크트리 파일 | HMR 로 폰에 즉시 반영 → 프레임 측정 무효 | R1-5 |
| `agent-browser` 의 `default` 세션·탭 | 서로의 조작이 같은 탭에 들어감 | 여기 |
| mock 의 인메모리 원장 | 잔액·최근 목록이 서로의 이체로 오염됨 | 여기 |

**파일을 나누는 것만으로는 두 세션이 분리되지 않는다.** 최소한 다음이 필요하다:

- 브라우저는 세션 이름을 나눈다 (`AGENT_BROWSER_SESSION`)
- `web/src` 를 고치면 우편함에 한 줄 남긴다 (측정 중인 쪽이 판단할 수 있게)
- 관측에는 **관찰 시각과 그때의 코드 상태**를 함께 적는다

**이 발견이 위의 B-1~B-5 를 무효화하는가** — 나눠서 봐야 한다.

- **유효하다**: B-2, B-3 의 대비 수치는 `getComputedStyle` 로 읽은 디자인 토큰이라
  이체 상태와 무관하다. 격리 세션에서 `@jisoo` = `rgb(113,113,122)` on `rgb(9,9,11)`
  = 4.12 로 동일하게 재현했다
- **재확인했다**: B-1 의 13자리. 격리 세션에서 다시 재서 폰트 23.4px,
  `scrollWidth 320 == clientWidth 320`, 오른쪽 끝 340 < 360 으로 동일
- **약하다**: B-4 의 탭 횟수, B-5 의 확정 화면 내용. 내가 친 값과 화면이 일치했으므로
  내 조작의 결과가 맞다고 보지만, 오염 가능한 구간에서 잡은 것이다

## B-8 R2 수정 재측정 — 핸들 대비가 올라갔다

**세션 `field` 격리, 어둡게, 금액 입력 화면.**

| | 수정 전 | 수정 후 |
|---|---|---|
| 헤더 핸들 `@jisoo` | `rgb(113,113,122)` · **4.12** | `rgb(161,161,170)` · **7.76** |
| 단위 `P` | `rgb(113,113,122)` · **4.12** | `rgb(161,161,170)` · **7.76** |

색을 `handle` textStyle 한 곳으로 모은 수정이 반영됐다. B-2 의 AA 미달 두 건이 같이 풀렸다.

## B-9 여정 5 전 구간 타임라인 — 스피너 0, 단계 역행 없음

CLI 왕복이 3초 취소 창보다 느려서, 페이지 안에 **100ms 기록기**를 심어 타임라인을
통째로 받았다 (`setInterval` 로 `innerText` 와 무한 반복 CSS 애니메이션 개수를 샘플링).
개발자 패널에서 단계 속도를 `각 2초` 로 늦춘 상태다.

```
  101ms  이렇게 보낸다 · 박태윤 @taeyun · 20,000P · 보낸 뒤 남는 잔액 3,140,000 P
 8401ms  아직 보내지 않았다 · 3초 후 보냅니다
         "아직 아무것도 처리되지 않았다. 지금 취소하면 잔액은 그대로다." · [취소]
 9001ms  2초 후 보냅니다
10001ms  1초 후 보냅니다
11001ms  보내는 중 — 내 잔액에서 출금 / 이체 요청 전송 / 원장 검증 / 받는 사람에게 입금
13001ms  ✓ 출금
15001ms  ✓ 출금  ✓ 전송
17001ms  ✓ 출금  ✓ 전송  ✓ 검증
19401ms  ✓ 보냈다
```

- **스피너 0개.** 전 구간에서 `animation-iteration-count: infinite` 인 요소가 **하나도 없다.**
  요청서의 "0이어야 한다"를 충족한다
- **단계 역행 없음.** ✓ 개수가 0 → 1 → 2 → 3 으로 단조 증가한다. 건너뛴 단계도, 되돌아간
  단계도 없다. 세 번의 이체에서 모두 같았다
- **취소 창 카운트다운이 실제로 1초 간격이다** (2601 → 3601 → 4601ms)
- 진행 화면에 "처리가 시작되어 취소할 수 없다"가 함께 있다. 취소 창과 진행 화면이
  취소 가능 여부를 **서로 반대로, 명시적으로** 말한다

## B-10 완료 화면이 약 0.5초 동안 **이체 전 잔액**을 보여준다

**네 번의 이체에서 네 번 다 재현됐다.**

| 이체 | 완료 화면 첫 표시 | 갱신 후 | 지속 |
|---|---|---|---|
| 20,000 (3,160,000 기준) | **3,160,000 P** | 3,140,000 P | 500ms |
| 10,000 (3,140,000 기준) | **3,140,000 P** | 3,130,000 P | 398ms |
| 70,000 (3,130,000 기준) | **3,130,000 P** | 3,060,000 P | 500ms |
| 60,000 (3,020,000 기준) | **3,020,000 P** | 2,960,000 P | 498ms |

즉 `✓ 보냈다` 와 함께 뜨는 **"남은 잔액"이 처음 약 0.4~0.5초 동안 아직 안 빠진
숫자다.** 그 뒤 재조회가 도착하면 올바른 값으로 바뀐다.

이건 여정 5 가 막으려던 **"거짓 완료"** 에 해당한다고 본다. 화면이 "보냈다"고 단정하면서
동시에 보내기 전의 잔액을 "남은 잔액"이라는 이름으로 보여주는 구간이 있다. 0.5초는
짧지만, 완료 화면에서 사용자가 가장 먼저 보는 숫자가 그것이다.

**다만 이건 mock 의 지연(700~1000ms) 위에서 잰 값이다.** 실제 서버 지연이 다르면 길이도
달라진다. 판단은 작업 세션 몫이다.

## B-11 취소 창 정직성 — 잔액이 1P 도 움직이지 않는다

CLI 로는 3초 창을 못 잡아서, 페이지 안에 **자동 취소 감시자**를 심었다 (50ms 마다
`취소` 버튼을 찾아 발견 즉시 `click()`).

- 취소 직전 잔액 **3,020,000 P**
- 취소 창 진입 2101ms → 자동 취소 → **2904ms 에 홈**
- 홈의 잔액 **3,020,000 P — 불변**
- 그 뒤 **15초간 5초 간격으로 3회 확인, 계속 3,020,000 P.** 되살아나지 않는다

취소 창 문구("아직 아무것도 처리되지 않았다. 지금 취소하면 잔액은 그대로다")가
실제 동작과 일치한다.

## B-12 멱등성 — 실패 후 재시도해도 한 번만 빠진다

개발자 패널에서 `네트워크` 실패를 주입했다.

1. 이체 전 잔액 **3,020,000 P**, 60,000 P 를 최준호에게
2. 홀드 → **1901ms 에 곧장 실패 화면.** 취소 창도 진행 4단계도 거치지 않는다
3. 실패 시점 잔액 **3,020,000 P — 차감 없음**
4. `다시 시도` → **확정 화면으로 되돌아간다** (바로 재전송하지 않는다. 다시 홀드해야 한다)
5. 다시 홀드 → 취소 창 → 4단계 → 완료
6. 최종 잔액 **2,960,000 P**

**3,020,000 − 60,000 = 2,960,000. 정확히 한 번만 빠졌다.** 실패한 시도는 차감하지 않았고,
재시도가 이중 처리되지 않았다. 화면이 약속한 "다시 시도해도 두 번 처리되지 않는다"가
지켜진다.

걸리는 것 하나: **실패가 주입되면 취소 창과 진행 4단계를 건너뛴다** (확정 1502ms →
실패 1901ms). 요청이 나가기 전에 끊긴 경우로 보이지만, 사용자 입장에서는 홀드하자마자
실패가 뜨는 셈이라 "보내는 중"을 본 적이 없다. 의도한 것인지는 작업 세션이 판단할 일이다.

## B-13 실패 문구 두 종류가 돈의 위치를 다르게 말한다

| | 네트워크 | 잔액 부족 |
|---|---|---|
| 제목 | 보내지 못했다 | 보내지 못했다 |
| 이유 | 요청이 서버에 닿지 못했다 | 잔액이 부족하다 |
| 라벨 | **지금 확실한 것** | **돈은 여기 있다** |
| 본문 | 서버가 요청을 받았는지 **알 수 없다**. 다시 시도해도 두 번 처리되지 않는다 — 같은 요청으로 취급된다 | **아무것도 나가지 않았다. 잔액은 그대로다** |
| 버튼 | **다시 시도** / 금액 고치기 / 홈으로 | 금액 고치기 / 홈으로 |

**두 화면이 다르게 말한다 — 요청서가 물은 것이 그대로 성립한다.** 애매한 실패에는
"알 수 없다"고 쓰고, 단정할 수 있는 실패에는 "돈은 여기 있다"고 쓴다.

그리고 **`다시 시도` 버튼이 잔액 부족 화면에는 아예 없다.** 결정적 실패라 다시 눌러도
같은 결과이기 때문이다. 버튼의 유무로 재시도가 의미 있는지를 말한다.

## B-14 발행 경로 — 전 구간이 이체와 분리된다. 겹치는 곳은 한 군데뿐

**세션 `field`, 어둡게. 발행 경로 전체를 끝까지 따라갔다.**

문구가 갈린다. **"잔액"이라는 말이 발행 경로에 한 번도 나오지 않는다.**

| | 이체 | 발행 |
|---|---|---|
| 대상 선택 | 누구에게 보낼까 | 누구에게 **발행**할까 |
| 금액 화면 상한 | 보낼 수 있는 금액 | **남은 발행 여력** |
| 확정 제목 | 이렇게 보낸다 | 이렇게 **발행**한다 |
| 확정 본문 | 지금 잔액 / 보낸 뒤 남는 잔액 | 지금 **총 유통량** / 발행 뒤 **총 유통량** / **유통량 변화 +0.1%** |
| 확정 버튼 | 꾹 눌러서 보내기 | 꾹 눌러서 **발행** |
| 취소 창 | 아직 보내지 않았다 · **3초** | 아직 발행하지 않았다 · **8초** |
| 취소 창 본문 | 지금 취소하면 **잔액**은 그대로다 | 지금 취소하면 **총 유통량**은 그대로다 |
| 진행 4단계 | 내 잔액에서 출금 / 이체 요청 전송 / 원장 검증 / 받는 사람에게 입금 | **발행 준비 / 원장에 기록 / 유통량 검증 / 받는 사람에게 지급** |
| 완료 | ✓ 보냈다 · 남은 잔액 | ✓ **발행했다** · **총 유통량** |

색도 갈린다. 이체는 파랑 `rgb(37,99,235)`, 발행은 보라 `rgb(192,132,252)`.
완료 화면의 ✓ 배지도 이체는 초록, 발행은 보라(`rgb(26,3,46)` on 보라, 7.25)다.

8초 카운트다운이 실제로 1초 간격이다 (2501 → 3501 → … → 8501ms).

**겹치는 곳이 딱 하나 있다.** 발행 **완료** 화면의 `확인` 버튼 배경이 `rgb(37,99,235)` —
**이체의 파랑**이다. 그 화면의 다른 모든 요소는 보라 계열이다. 중립적인 닫기 동작이라
의도한 것일 수도 있으나, "발행 경로 전체에서 색이 겹치는 화면이 있는지" 물었으므로 적는다.

## B-15 B-10 은 이체만의 문제가 아니다 — 발행 완료에서도 재현된다

```
18001ms  ✓ 발행했다 · 총 유통량  50,000,000 P   ← 발행 전 값
18401ms  ✓ 발행했다 · 총 유통량  50,050,000 P   ← 400ms 뒤 갱신
```

이체 완료 화면의 "남은 잔액"과 같은 자리에서 같은 일이 일어난다. 완료 화면이
**"했다"고 단정하면서 아직 반영 안 된 숫자를 함께 보여주는 구간**이 양쪽 경로에 다 있다.
한 화면의 문제가 아니라 완료 화면의 데이터 조회 방식 문제로 보인다.

## B-16 내역 모프 — 슬라이드와 싸우지 않는다

50ms 간격으로 **(1) `position:absolute` 이면서 transform 이 걸린 레이어의 가로 이동량**과
**(2) 이름 텍스트의 화면 좌표**를 함께 기록했다.

**내역 목록 → 상세** (`history` 1 → `historyDetail` 2, 깊이가 커지므로 원래대로면 슬라이드):

```
 51ms  가로 레이어 없음   이름 y=136
101ms  가로 레이어 없음   이름 y=130
201ms  가로 레이어 없음   이름 y= 90
351ms  가로 레이어 없음   이름 y= 71
451ms  가로 레이어 없음   이름 y= 68   ← 안정
```

- **가로로 미는 레이어가 하나도 없다.** 화면이 옆으로 밀리지 않는다
- 이름의 **x 는 20 으로 고정**, **y 만 136 → 68 로 올라간다.** 폰트 크기도 18px 유지
- 즉 눌린 줄이 상세 화면의 제목 자리로 **자리를 옮긴다**. 모프가 성립한다

**계측기가 슬라이드를 실제로 잡는지 검증했다** — 홈 → 금액 입력(깊이 0 → 2)에서:

```
 54ms  x=[-21, 291]   이름 x= 53
153ms  x=[-88,  66]   이름 x=-14
403ms  x=[-108,  1]   이름 x=-34
451ms  x=[-108]                     ← 안정
```

나가는 레이어가 `-21 → -108`(왼쪽으로), 들어오는 레이어가 `291 → 0`(오른쪽에서).
`ScreenTransition` 의 `enter x:100% / exit x:-30%` 와 일치한다. 슬라이드 구간은
**약 450ms** 로, 폰에서 잰 436ms(R2-4)와 맞는다.

**계측기는 슬라이드를 잡는다. 그런데 내역 전환에서는 아무것도 안 잡혔다.** 결론:
내역 목록↔상세는 슬라이드가 아니라 모프이고, 둘이 겹쳐 일어나지 않는다.

## B-17 확정 화면의 자릿수 — 13자리는 확정에 도달할 수 없다

- 금액 입력에서 `1` 을 13번 눌러 `1,111,111,111,111` 을 만들면 **`보내기 확인` 이 disabled** 다.
  잔액 초과 경고가 뜨고 다음으로 못 간다. **즉 확정 화면이 13자리를 볼 일이 없다**
- 확정 화면이 실제로 감당해야 하는 최대는 발행 경로다. 발행 여력 전액(49,950,000)을
  걸면 확정 화면이 `발행 뒤 총 유통량 **100,000,000 P**`(9자리)와 `유통량 변화 +99.8%`를 함께 띄운다
- 그 상태에서 **넘치는 요소가 하나도 없다.** `document.scrollWidth == innerWidth == 360`,
  `scrollWidth > clientWidth` 인 요소 0개, 뷰포트 밖으로 나간 요소 0개

## B-18 새 화면 대비 재측정 — 진행 화면의 **미도달 단계**가 AA 미달이다

어둡게(배경 `rgb(9,9,11)`) 기준. 취소 창 · 실패 · 완료 · 내역은 전부 통과한다.

| 화면 | 최저 대비 요소 | 값 |
|---|---|---|
| 취소 창 | 본문 "아직 아무것도 처리되지 않았다…" | 7.76 |
| 완료 (이체/발행) | `확인` 버튼 (흰 글자/파랑) | 5.17 |
| 실패 | `다시 시도` (흰 글자/파랑) | 5.17 |
| 실패 | 라벨 "지금 확실한 것" (12px/500) | 5.81 |
| 발행 금액 | `발행 확인` (`rgb(26,3,46)` on 보라) | 7.25 |

**진행 화면만 다르다:**

| 요소 | 색 | 크기/굵기 | 대비 |
|---|---|---|---|
| `내 잔액에서 출금` (현재 단계) | `rgb(250,250,250)` | 16px / 500 | 19.06 |
| **`이체 요청 전송`** (미도달) | `rgb(113,113,122)` | 16px / 400 | **4.12** ✗ |
| **`원장 검증`** (미도달) | `rgb(113,113,122)` | 16px / 400 | **4.12** ✗ |
| **`받는 사람에게 입금`** (미도달) | `rgb(113,113,122)` | 16px / 400 | **4.12** ✗ |
| `처리가 시작되어 취소할 수 없다` | `rgb(161,161,170)` | 14px / 400 | 7.76 |

**B-2 에서 핸들에 있던 바로 그 색 토큰(`rgb(113,113,122)`)이 여기 남아 있다.**
핸들 쪽은 `handle` textStyle 로 모으면서 걷어냈지만, 진행 화면의 미도달 단계는 그대로다.
16px/400 이라 large text 예외에도 걸리지 않는다.

**밝은 모드에서는 더 나쁘다.** 같은 화면을 `밝게` 로 바꿔 다시 쟀다
(배경 `rgb(255,255,255)`):

| 요소 | 색 | 대비 (밝게) | 대비 (어둡게) |
|---|---|---|---|
| `내 잔액에서 출금` (현재 단계) | `rgb(9,9,11)` | 19.9 | 19.06 |
| **미도달 3단계** | `rgb(161,161,170)` | **2.56** ✗ | 4.12 ✗ |
| `처리가 시작되어 취소할 수 없다` | `rgb(82,82,91)` | 7.73 | 7.76 |

**B-2 와 정확히 같은 패턴이다** — 밝은 모드가 어두운 모드보다 나쁘고, 문제가 되는 값도
그때와 같은 `rgb(161,161,170)` / **2.56** 이다. 핸들에서 걷어낸 문제가 진행 화면에
그대로 남아 있고, 두 모드 모두 AA 아래다.

**흐리게 만든 것이 의도라는 건 안다** — 어디까지 왔는지를 밝기로 말하는 설계다. 다만
미도달 단계도 "앞으로 무슨 일이 일어나는지"를 읽히게 해야 하는 정보다. 밝기 차이를
유지하면서 둘 다 4.5 위에 두는 선택지가 있는지는 작업 세션이 판단할 일이다.

## B-19 내역 화면 — 취소된 건을 취소선으로 구분한다

목록 한 줄은 `이름 / (발행 ·) 상태 · 시각 / 금액` 이다.

- **발행은 초록 `+50,000P`, 이체는 흰색 `−60,000P`.** 부호와 색 둘 다로 방향을 말한다
- **취소된 건은 이름과 금액에 취소선이 그어지고 회색으로 내려간다.** 라벨은 `취소됨`,
  시각은 없다. 처음 텍스트만 봤을 때는 `−80,000P` 가 돈이 나간 것처럼 읽힐까 걱정했는데,
  화면에서는 취소선이 그것을 막는다
- 상세 화면에는 보낸 사람 / 받은 사람 / 요청 시각 / 확정 시각 / `진행 4 / 4 단계` /
  **`요청 키 f400a938-01a9-…`** 가 있다. 실패 화면이 약속한 "같은 요청으로 취급된다"의
  근거를 화면에서 확인할 수 있게 해 둔 것으로 읽힌다

## B-20 우편함 알림의 절반은 내가 보낸 것이다

`--to run:run_a09932d74361` 은 **보낸 사람도 수신자에 포함한다.** 두 세션 모두 그 Run 에
속하므로, 내가 보낸 메시지가 나에게도 배달되고 "메시지 N건" 알림이 뜬다.

실제로 R3 구간에서 알림 세 번이 연속으로 **전부 내가 보낸 메시지의 되돌아옴**이었다.
`inbox --full` 로 열어야 그걸 알 수 있으므로, 알림 숫자만 보고 상대가 응답했다고
판단하면 안 된다.

에코를 피하려면 상대 터미널 핸들로 직접 보내면 된다:

```bash
orca orchestration send --run run_a09932d74361 --to term_<상대 핸들> ...
```

다만 그러면 Run 우편함에 남지 않는다. 이 파일이 정본이고 메시지는 깨우는 용도라는
원칙을 따르면 **에코를 감수하고 Run 으로 보내는 편이 낫다.** 대신 읽는 쪽에서
`from=` 을 먼저 보고 자기 것인지 가리면 된다.

관련해서 하나 더 — `orca terminal list` 는 상대 터미널의 **프롬프트 미리보기**를 보여준다.
상대가 우편함이 아니라 자기 프롬프트에서 사용자를 기다리고 있는 경우, 우편함만 봐서는
그 상태를 알 수 없다. 상대가 조용하면 `terminal list` 로 무엇을 기다리는지 확인할 수 있다.

## 결정 · R2

### 고쳤다 — 금액 입력 화면에서만 검증 신호가 꺼져 있었다 (B-3)

R2 에서 가장 중요한 발견이다. 같은 `@jisu` 를 화면별로 따라간 실측 —
대상 선택 7.88 → **금액 입력 헤더 2.56** → 확정 7.73 — 이 없었으면 못 찾았을 것이다.
동명이인을 고른 사용자가 금액을 치는 **그 동안** 누구를 골랐는지 확인할 유일한 단서가
가장 흐렸다.

원인은 화면마다 핸들 색을 따로 붙인 것이었다. 색을 `handle` textStyle 안으로 옮겨
한 곳에서만 정하게 했다. 금액 입력 헤더 재측정 **7.73:1**.

같은 수정으로 B-2(핸들·단위 `P` 의 AA 미달)도 함께 풀렸다. 동명이인 신호는 대비가
아니라 크기(16 vs 14) · 굵기(500 vs 400) · 색상(주황 vs 회색)으로 유지된다 —
색만으로 구분하지 않으므로 색각 이상에서도 남는다.

### 고쳤다 — 자동 포커스 (R2-5 · R2-6)

키보드가 목록의 63% 를 덮어 다섯 명 중 두 명만 보인다는 실측과, 사용자의 "목록을 먼저
보고 싶었다"가 같은 곳을 가리켰다. 동명이인 묶음이 목록 아래쪽에 있으면 두 줄을 나란히
볼 수 없고, 그러면 이 화면의 존재 이유가 사라진다. `autoFocus` 를 뺐다.

### 닫는다

- **R1-3 전환 중 글자** — Chrome 10회·Brave 8회 모두 0회. 헤드리스 캡처 아티팩트였다
- **R1-2 전환 속도** — `520/44/0.8` 로 "적당하다". 되돌리지도 더 올리지도 않는다
- **R2-4 프레임** — 120Hz 유지. 뒤로가 109ms 더 길지만 체감에 걸리지 않았으므로 둔다
- **R2-6 검색 지연** — Mock 설정(700+300+150)이다. 기기 문제가 아니므로 코드를 고치지 않는다

### 판단 보류 — 사람 손이 필요하다

- **R2-1 잔액 재조회 여부** — 플레이스홀더가 안 보이는 것은 맞는 동작이다. 홈에서
  이전 `data` 를 유지하므로 `account` 가 `null` 이 되지 않는다. 다시 읽고 있는지는
  화면으로 구분할 수 없다 — 굳이 깜빡이게 만들 이유는 없다고 본다
- **R2-7 키에 경계가 없다** — 크기(108×60)는 충분하다. 경계 없음이 오타를 만드는지는
  손으로만 답이 난다
- **B-5 확정 화면 아래 절반이 빈다** — 눈으로 판단할 항목이다

---

# R3 — Phase 4·5·6 (확정 · 취소 창 · 진행 · 실패 · 발행 · 내역)

**브라우저: Chrome. 세션은 `AGENT_BROWSER_SESSION=field` 로 격리.**
개발자 패널(홈 헤더 ⚙)에서 지연·실패·단계 속도를 주입할 수 있다.

## 손으로만 답이 나는 것 (사용자가 돌아온 뒤)

- [ ] **홀드 확정** — "꾹 눌러서 보내기". 600ms 다. 길게 느껴지나 짧나.
      실수로 발동하나. 스크롤하다 손가락이 스쳐서 발동하나
- [ ] **홀드 중 읽기** — 누르고 있는 동안 화면의 금액·받는 사람이 읽히나.
      홀드가 존재하는 이유가 그 시간을 만드는 것이다
- [ ] **취소 창 3초** — "3초 후 보냅니다". 취소를 누를 시간이 되나, 조급한가
- [ ] R2 미확인분 — 한글 IME 조합, 만 키 인지, 엄지 오타율, 지우기 길게 오발동,
      자릿수 애니메이션, 한글 병기 가독, 야외 밝기

## adb·브라우저로 답이 나는 것 (지금)

- [ ] **취소 창 정직성** — 취소 창 동안 잔액이 1P 도 움직이지 않는가.
      취소한 뒤 10초가 지나도 되살아나지 않는가
- [ ] **스피너** — 진행 화면에 회전하는 것이 하나라도 있는가 (0이어야 한다)
- [ ] **단계 역행** — 개발자 패널 `단계 진행 속도 → 각 2초` 로 늦추고, 완료 표시가
      한 번이라도 뒤로 가거나 건너뛰는지 본다
- [ ] **실패 화면** — 패널에서 `네트워크` 주입 → 실패 → **다시 시도** → 완료까지 가고,
      잔액이 **정확히 한 번만** 빠지는지 확인. 두 번 빠지면 멱등성이 깨진 것이다
- [ ] **실패 문구** — `잔액 부족` 과 `네트워크` 를 각각 주입하고, 두 화면이 돈의 위치를
      **다르게** 말하는지 본다 (전자는 단정, 후자는 "알 수 없다")
- [ ] **발행 색 분리** — 발행 경로 전체(대상 → 금액 → 확정 → 취소 창 → 진행 → 완료)에서
      이체와 색이 겹치는 화면이 있는지. 발행 취소 창은 8초다
- [ ] **발행 문구** — 발행 중 화면이 "잔액"이라는 말을 쓰면 버그다. 전부 "총 유통량"이어야 한다
- [ ] **내역 모프** — 내역 목록에서 한 줄을 누를 때 이름·금액이 자리를 옮기는가.
      화면이 옆으로 밀리면 안 된다 (밀리면 모프와 슬라이드가 다시 싸우는 것이다)
- [ ] **대비 재측정** — 새 화면들(확정·취소 창·진행·실패·내역)의 부속 텍스트가
      AA 를 넘는지. B-2 와 같은 방법으로
- [ ] **13자리 · 360px** — 확정 화면과 내역에서도 금액이 잘리지 않는가

## 현장 기록 · R3

<!-- 실기기 세션이 append -->

---

# 재작성 준비 · 실기기 세션

R3 은 중단됐다 (작업 세션 `msg_79be3132810a`). 화면 대부분이 사라지므로 R3 현장 기록은
비운 채 둔다. 이 절은 **T6 이 나왔을 때 바로 잴 수 있도록** 준비해 둔 것이다.

## 이 기록 중 무엇이 살아남는가

R1·R2 기록은 그대로 둔다 — 재작성의 근거다. 다만 **어떤 관측이 새 구현에서도 유효한지**를
가려 두지 않으면, 사라진 화면에 대한 기록을 근거로 쓰게 된다.

**그대로 유효 — 다시 잴 필요 없다**

| 기록 | 왜 유효한가 |
|---|---|
| R1-4 `gfxinfo` 로는 웹 프레임을 못 잰다 | 계측 방법. 구현과 무관 |
| R1-5 / B-7 / B-20 두 세션 운영 사고 | 운영 방식. 구현과 무관 |
| R2-1 시스템 back 덫 | `useSystemBack.ts` 가 살아남는 목록에 있다 |
| R2-2 / R2-4 전환 속도·프레임 | `ScreenTransition.tsx` 가 살아남는다 |
| R2-3 전환 중 글자 소실 0회 | 같은 전환 구현 |
| R2-5 키보드가 뷰포트의 63% 를 덮는다 (앱에 286 CSS px) | **기기 실측치다.** 화면이 바뀌어도 이 수는 그대로다 |
| B-12 멱등성 | 멱등성 키 설계가 살아남는다 |
| B-13 실패 문구가 돈의 위치를 다르게 말한다 | `domain/failures.ts` 가 살아남는다 |
| B-16 내역 모프가 슬라이드와 안 싸운다 | `ScreenTransition` 의 morph 분리가 살아남는다 |
| B-17 자릿수 축소 | `ui/amountFit.ts` 가 살아남는다 |

**무의미해진다 — 대상이 사라진다**

| 기록 | 왜 |
|---|---|
| B-9 의 진행 4단계 부분 | 진행 4단계가 제거된다 |
| B-11 취소 창 정직성 | 취소 창이 통째로 사라진다 |
| B-18 진행 화면 미도달 단계 대비 4.12 / 2.56 | 그 화면이 사라진다. **다만 색 토큰 문제는 남을 수 있으니 T5 이후 다시 훑는다** |

**다시 재야 한다 — 대상은 남지만 구현이 바뀐다**

| 기록 | 왜 |
|---|---|
| R2-7 키패드 기하 108×60 CSS px | 키패드를 다시 만든다. 이 값은 **비교 기준선**으로 남긴다 |
| B-2 / B-8 대비 전반 | T5 에서 토큰과 recipe 를 전면 교체한다 |
| B-10 / B-15 완료 화면이 옛 값을 보여준다 | TanStack Query 로 바뀌면 조회 시점이 달라진다. **T8 에서 최우선으로 다시 본다** |
| B-14 발행 색·문구 분리 | 화면을 다시 만든다 |

**새로 생기는 확인 항목** — `JOURNEY.md` 재작성판에서 온 것.

- 여정 1 — 포인트 다섯 개를 놓고 특정 포인트의 잔액을 찾는 데 걸리는 시선 이동.
  **카드 제목을 읽어야만 찾을 수 있으면 색 표식이 실패한 것이다.** 사람 눈이 필요하다
- 여정 2 — **보내기 플로우의 모든 화면에서 포인트 이름이 보이는지.**
  한 화면이라도 `30,000`만 있고 무엇인지 없으면 실패. 아래 스크립트로 자동화했다

## 도구 1 — adb 무선 재연결 (포트가 매번 바뀐다)

R2-8 에서 겪은 문제: 폰이 절전에 들어가면 연결이 끊기고, 다시 켜면 **포트 번호가 바뀐다.**
페어링은 유지되므로 `adb pair` 는 다시 하지 않아도 된다. 포트를 자동으로 찾아 붙는다.

```bash
adb-reconnect() {
  adb start-server >/dev/null 2>&1
  local addr
  addr=$(timeout 15 adb mdns services 2>/dev/null \
         | awk '/_adb-tls-connect/ {print $NF; exit}')
  if [ -z "$addr" ]; then
    echo "mdns 에 기기가 없다. 폰 화면을 켜고 개발자 옵션 → 무선 디버깅을 다시 켜라."
    return 1
  fi
  echo "찾음: $addr"
  adb connect "$addr"
}
```

`awk` 부분은 실제 `adb mdns services` 출력으로 검증했다
(`adb-R3CW…  _adb-tls-connect._tcp  172.30.1.31:42241` → `172.30.1.31:42241`).
**연결 자체는 기기가 없어 검증하지 못했다.**

폰이 응답은 하는데 `connect` 가 거부되면 절전 상태다 — `ping` 의 RTT 가 LAN 인데도
300ms 근처면 그렇다. 화면을 켜야 한다.

## 도구 2 — 전환 프레임 실측

`gfxinfo` 로는 웹 프레임이 안 잡힌다 (R1-4). 웹 컨텐츠 서피스를 직접 봐야 한다.

```bash
# 레이어 이름을 찾는다. 브라우저가 서피스를 새로 만들면 #뒤 숫자가 바뀌므로 매번 찾는다.
sf-layer() {
  adb shell dumpsys SurfaceFlinger --list 2>/dev/null \
    | grep -o "${1:-com.android.chrome}/ChromeChildSurface#[0-9]*" | tail -1
}

# 전환 직전 2초 정지 → 전환 1회 → 정지, 그 뒤에 덤프한다.
# 유휴 상태에서는 프레임이 생기지 않으므로 링버퍼에 그 전환만 남는다.
sf-frames() {
  adb shell dumpsys SurfaceFlinger --latency "$(sf-layer "$1")" | frames.py
}
```

`frames.py` (표준입력으로 받는다):

```python
import sys
lines = [l.strip() for l in sys.stdin if l.strip()]
rows = [tuple(int(x) for x in l.split()) for l in lines[1:] if len(l.split()) == 3]
act = sorted(r[1] for r in rows if r[1] not in (0, 9223372036854775807))
gaps = [(act[i+1] - act[i]) / 1e6 for i in range(len(act) - 1)]
bursts, cur = [], []
for g in gaps:                      # 200ms 이상 비면 다른 동작으로 본다
    if g > 200:
        if cur: bursts.append(cur); 
        cur = []
    else: cur.append(g)
if cur: bursts.append(cur)
for i, b in enumerate(bursts):
    if len(b) < 3: continue
    s = sorted(b)
    print(f"버스트{i}: {len(b)+1}프레임 / {sum(b):.0f}ms / "
          f"중앙 {s[len(s)//2]:.1f}ms ({1000/s[len(s)//2]:.0f}Hz) / "
          f"최대 {max(b):.1f}ms / 16.7ms 초과 {len([x for x in b if x > 16.7])}개")
```

**검증했다** — R2-4 에서 실제로 받았던 덤프를 이 파서에 넣으면 그때 보고한 값이 그대로
나온다 (앞으로 50프레임/436ms, 뒤로 58프레임/545ms, 양쪽 중앙 8.4ms = 119Hz).

첫 행의 `41666665`(=24Hz)는 유휴 상태 값이라 믿으면 안 된다. 실측 간격이 정답이다.

## 도구 3 — 대비 자동 검사

새 화면마다 다시 잴 것이므로 재사용 가능한 형태로 둔다. 브라우저 콘솔이나
`agent-browser eval` 에 그대로 넣는다.

두 가지가 중요하다. **(1) `body` 는 투명이라 배경을 조상으로 거슬러 올라가 찾아야 한다** —
그냥 쓰면 값이 전부 틀린다. **(2) WCAG large text 예외**(24px 이상, 또는 18.66px 이상이면서
bold)를 적용해야 오탐이 안 난다.

```js
(() => {
  const painted = (el) => {                       // 실제로 칠해진 배경
    for (let n = el; n; n = n.parentElement) {
      const c = getComputedStyle(n).backgroundColor;
      if (c && !/rgba\(0, 0, 0, 0\)|transparent/.test(c)) return c;
    }
    return "rgb(255, 255, 255)";
  };
  const lum = (c) => {
    const [r, g, b] = c.match(/\d+/g).slice(0, 3).map(Number).map((v) => {
      v /= 255;
      return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4);
    });
    return 0.2126 * r + 0.7152 * g + 0.0722 * b;
  };
  const ratio = (a, b) => {
    const [l1, l2] = [lum(a), lum(b)];
    return +(((Math.max(l1, l2) + 0.05) / (Math.min(l1, l2) + 0.05)).toFixed(2));
  };
  const needs = (px, w) => (px >= 24 || (px >= 18.66 && w >= 700) ? 3.0 : 4.5);

  const fails = [];
  document.querySelectorAll("*").forEach((el) => {
    if (el.children.length) return;
    const text = (el.textContent || "").trim();
    if (!text) return;
    const cs = getComputedStyle(el);
    if (cs.display === "none" || cs.visibility === "hidden") return;
    if (el.getAttribute("aria-hidden") === "true") return;   // 장식은 제외
    const px = parseFloat(cs.fontSize), w = parseInt(cs.fontWeight, 10) || 400;
    const r = ratio(cs.color, painted(el));
    if (r < needs(px, w)) fails.push({ text: text.slice(0, 34), fg: cs.color, px, w, ratio: r });
  });
  return JSON.stringify({ failCount: fails.length, fails });
})()
```

**검증했다** — 현재 홈에서 20개 중 1개 미달을 잡아낸다 (개발자 패널 아이콘 `⚙`,
`rgb(113,113,122)` 14px/400, 4.12). 핸들 수정 뒤라 나머지는 통과한다.

## 도구 4 — 금액에 포인트 이름이 붙어 있는가 (여정 2)

새 여정 2 의 확인 방법이 **자동화 가능한 형태로 쓰여 있다**: "한 화면이라도 `30,000`만
있고 무엇인지 없으면 실패다." 보내기 플로우의 각 화면에서 이걸 실행한다.

```js
(() => {
  const NAMES = window.__POINT_NAMES;        // T1 이후 실제 포인트 이름 배열을 넣는다
  const AMOUNT = /\d{1,3}(,\d{3})+|\b\d{4,}\b/;
  const visible = (el) => {
    for (let n = el; n; n = n.parentElement) {
      if (/^(SCRIPT|STYLE|TEMPLATE|NOSCRIPT)$/.test(n.tagName)) return false;
      const cs = getComputedStyle(n);
      if (cs.display === "none" || cs.visibility === "hidden") return false;
    }
    return true;
  };
  // 요소가 아니라 텍스트 노드를 훑는다 — 형제 요소가 있는 텍스트를 놓치지 않으려고.
  const walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT);
  const orphans = [], amounts = [];
  for (let node = walker.nextNode(); node; node = walker.nextNode()) {
    const t = (node.nodeValue || "").trim();
    if (!t || !AMOUNT.test(t)) continue;
    const el = node.parentElement;
    if (!el || !visible(el)) continue;
    amounts.push(t.slice(0, 34));
    let found = false;
    for (let n = el, i = 0; n && i < 4; n = n.parentElement, i++) {
      if (NAMES.some((name) => (n.textContent || "").includes(name))) { found = true; break; }
    }
    if (!found) orphans.push(t.slice(0, 34));
  }
  return JSON.stringify({ screen: (document.querySelector("h1")||{}).textContent, amounts, orphans });
})()
```

**양쪽으로 검증했다.**

- 정상: 현재 홈에서 금액 두 개(`3,240,000`, `50,000,000`)를 모두 찾고 미검출 0
- **음성 검증**: 화면에 없는 이름(`["온포인트","하나포인트"]`)을 넣으면 **둘 다 미검출로
  보고한다.** 실패해야 할 때 실패한다

처음에 요소 기반으로 짰다가 `전체 50,000,000 P 중 6.0%` 처럼 **형제 요소가 있는 텍스트를
놓치는** 것을 발견해 텍스트 노드 순회로 바꿨다. 조용히 통과시키는 검사기는 없느니만 못하다.

## 경계에 대한 메모

작업 세션이 "스크립트로 만들어 두라"고 했지만, 실기기 세션은 `docs/FIELD.md` 외의 파일을
만들지도 고치지도 않는다. 그래서 **스크립트를 이 파일 안에 복사해 쓸 수 있는 형태로 두었다.**
실행본은 저장소 밖(스크래치패드)에 있다.

저장소에 `tools/` 같은 자리를 만들어 두는 편이 낫다면 **작업 세션이 만들면 된다** — 위
내용을 그대로 옮기면 되고, 그때부터는 그쪽이 관리한다.

## B-21 T1 계약 점검 — 시드와 확인 방법이 어긋난다 (화면 없이 잡았다)

화면이 없는 동안 **T1 의 계약을 엔드포인트로 직접 두드려** 봤다. MSW 가 브라우저에서
돌고 있으므로 `fetch` 로 서버 쪽을 그대로 볼 수 있다. 코드를 읽은 것이 아니라
**실제 응답으로 확인한 것**만 적는다. (내 격리 세션의 원장을 변경했다 — 작업 세션 쪽에는
영향이 없다.)

### 1. 잔액이 0이 된 보유 포인트가 지갑에서 **사라진다**

`mocks/ledger.ts` 의 `balancesOf` 주석은 이렇게 말한다.

> "잔액이 0인 포인트도 포함한다 — 가졌던 것과 가진 적 없는 것은 다르고, 그 판단은
> 화면이 한다. **서버가 미리 걸러 버리면 화면이 '0원인 온포인트'를 표현할 수 없다.**"

코드는 그 반대를 한다.

```js
.filter(({ pointType, amount }) => amount > 0 || pointType.issuerId === userId)
```

실증했다. 솔포인트 전액(86,200)을 이서연에게 보내 잔액을 0으로 만들었다.

| | 지갑 |
|---|---|
| 전 | 온포인트=3240000, **솔포인트=86200**, 금머니=620000 |
| 후 | 온포인트=3240000, 금머니=620000 |

**솔포인트가 목록에서 통째로 사라진다.** 이어서 금머니(내가 발행자) 전액을 보내니
`금머니=0` 으로 **남는다.** 즉 동작이 이렇다.

- 보유자인 포인트가 0이 되면 → 지갑에서 제거된다
- 발행자인 포인트가 0이 되면 → 0으로 남는다

이것이 깨는 것은 주석만이 아니다. `JOURNEY.md` 여정 1 이 화면에 요구하는 규칙이
**구현 불가능해진다.**

> "잔액이 0인 포인트는 아래로 내린다. 가진 것과 가졌던 것은 다르다."

서버가 0을 보내지 않으므로 화면은 아래로 내릴 것을 받지 못한다. 발행자인 포인트만
예외적으로 0으로 내려가는데, 그건 "가졌던 것"이 아니라 "내가 발행하는 것"이다.

**R1-1 과 같은 종류다** — 주석이 코드가 하지 않는 일을 한다고 말하고 있고, 그 위에
여정의 규칙이 얹혀 있다. T1 에서 잡히는 편이 화면을 만든 뒤보다 싸다.

### 2. 여정 1 의 확인 방법은 포인트 **다섯 개**를 요구하는데 시드는 **세 개**다

`/api/wallet` 응답: 온포인트 3,240,000 / 솔포인트 87,500 / 금머니 620,000. **셋이다.**

여정 1 의 확인 방법은 이렇게 쓰여 있다.

> "**포인트 다섯 개를 놓고**, 특정 포인트의 잔액을 찾는 데 걸리는 시선 이동을 센다.
> 카드 제목을 읽어야만 찾을 수 있으면 표식이 실패한 것이다."

카드가 셋이면 **위치만으로 구별된다.** "목록이 길어지면 잔액이 아니라 목록을 읽게 된다"는
위험 자체가 재현되지 않고, 표식(색)이 일을 하는지도 판별되지 않는다. 확인 방법을 그대로
돌릴 수 없다.

`PointAccent` 는 여섯 색(`blue green purple orange pink teal`)을 정의해 두었는데
셋만 쓰인다. 시드를 다섯 이상으로 늘리면 확인 방법이 살아나고 나머지 색도 검증된다.
**늘리는 게 맞는지, 아니면 확인 방법을 셋으로 고칠 것인지는 작업 세션이 정할 일이다** —
다만 지금은 둘이 어긋나 있다.

### 3. 0 잔액 픽스처가 아예 없다

시드의 내 잔액은 셋 다 양수다. 위 1번 때문에 **0으로 만들어도 사라지므로**,
"가진 것과 가졌던 것은 다르다"를 검증할 상태를 만들 방법이 현재 없다.

### 4. 받는 사람 목록에 사업자 계정이 섞인다

`/api/users` 는 7명을 돌려준다.

```
김지수@jisoo · 박태윤@taeyun · 이서연@seoyeon · 최준호@junho · 김지수@jisu · 온마트@onmart · 솔카페@solcafe
```

**온마트·솔카페는 포인트 발행자(사업자)다.** 여정 3 은 "머릿속의 **사람**을 화면에서
찾는다"인데 목록에 상점이 섞여 있다. 포인트를 발행자에게 되돌려 보내는 것이 유효한
행동이라면 남아야 하고, 아니라면 걸러야 한다. **어느 쪽인지 여정에 쓰여 있지 않다.**

그리고 두 김지수가 목록의 **1번과 5번**이다 — 인접하지 않다. `recipientList.ts` 의
`groupHomonyms` 가 붙여 주는 것이 전제인데, 그게 화면에서 실제로 되는지는 T7 에서
`FIELD.homonymsTogether` 로 확인한다.

### 5. QA 함정 — MSW 워커가 준비되기 전 요청은 404 다

페이지를 열고 곧바로 `POST /api/transfers` 를 쏘면 **404 에 빈 본문**이 온다.
Vite 가 돌려준 것이고, 잠시 뒤 같은 요청은 201 이다 (상대·절대 URL 모두).

처음에 이걸 MSW 패턴 문제로 의심했는데 재시도해 보니 아니었다. **검사 스크립트가
페이지 로드 직후에 API 를 두드리면 진짜 실패로 오판한다.** `navigator.serviceWorker`
등록을 먼저 확인하거나, 첫 GET 이 200 을 줄 때까지 기다린 뒤 시작해야 한다.

## B-22 T1 계약 배터리 — 9건 통과, 2건 구멍. 소수점이 여정 4 를 거꾸로 만든다

화면 없이 `fetch` 로 계약을 두드렸다. **워커 준비 전 404 함정(B-21 의 5번)을 피하려고
첫 `GET /api/wallet` 이 200 을 줄 때까지 기다린 뒤 시작한다.**

### 통과한 것 (9건)

| 검사 | 결과 |
|---|---|
| **멱등성 — 같은 키로 두 번** | 첫 **201**, 둘째 **200**, **같은 이체 id**, 잔액은 **5,000 한 번만** 줄었다 (3,240,000 → 3,235,000) |
| 멱등성 키 없는 쓰기 | **400** 거부 |
| 금액 `-100` | 400 거부 |
| 금액 `0` | 400 거부 |
| 잔액 초과 | **422 `INSUFFICIENT_BALANCE`** |
| 없는 받는 사람 | **404 `RECIPIENT_NOT_FOUND`** |
| 없는 포인트 | **404 `POINT_TYPE_NOT_FOUND`** |
| 남의 포인트 발행 | **403 `NOT_ISSUER`** |
| 발행 상한 초과 | **422 `CAP_EXCEEDED`** |

**멱등성이 T1 에서 이미 지켜진다.** 여정 6 의 "재시도가 안전한 유일한 근거"가 계약
수준에서 확인됐다 — 화면이 붙기 전에. B-12 는 화면을 거쳐서 같은 것을 봤고, 이건
HTTP 경계에서 직접 본 것이다.

### 구멍 1 — 소수점 금액이 통과한다. 그리고 그게 여정 4 를 거꾸로 만든다

`domain/types.ts` 는 이렇게 못 박아 두었다.

> `/** 정수. 최소 단위 1. **소수점을 허용하지 않는다.** */ export type Points = number`

그런데 `POST /api/transfers` 에 `amount: 0.5` 를 넣으면 **201 로 통과한다.** 핸들러의
검사가 이것뿐이다.

```js
if (!body.pointTypeId || !body.toId || !body.amount || body.amount <= 0)
```

`0.5` 는 truthy 이고 `> 0` 이라 통과한다. **타입은 컴파일 시점만 지킨다 — HTTP 경계에는
타입이 없다.**

결과를 실측했다. 이체 뒤 지갑이 이렇게 됐다.

```
온포인트 = 3234999.5      ← 잔액에 소수점이 생겼다
```

그 값을 `domain/points.ts` 의 `label()` 에 넣으면 (Vite 가 모듈을 서빙하므로 브라우저에서
직접 불러 확인했다):

| 입력 | `grouped` | `korean` |
|---|---|---|
| `1500000` | `1,500,000` | `백오십만` |
| **`3234999.5`** | **`3,234,999.5`** | **`삼백이십삼만사천구백구십구`** |
| **`0.5`** | **`0.5`** | **`""` (빈 문자열)** |

**숫자와 한글이 서로 다른 값을 말한다.** 화면에는 `3,234,999.5` 와
`삼백이십삼만사천구백구십구` 가 나란히 뜨고, 한글은 소수를 **조용히 버린다.**

이것이 왜 큰 문제인가 — 여정 4 의 장치가 정확히 그 반대를 하려고 있는 것이다.

> "숫자와 한글을 나란히 둔다. '백오십만'과 '천오백만'은 다르게 **읽힌다**"

병기는 **숫자가 감추는 것을 한글이 드러내라고** 있다. 소수점에서는 **한글이 숫자가
드러낸 것을 감춘다.** 검증 장치가 거꾸로 작동한다. 그리고 `0.5` 면 한글 병기가 아예
사라져서, 병기가 있어야 할 자리가 빈다.

`points.ts` 는 "살아남는 것" 목록에 있고 테스트 15케이스를 통과한다. **그 15개가 전부
정수이기 때문이다.** `points.ts` 를 고치는 문제가 아니라 **경계에서 정수를 강제하는
문제**로 보인다 — 서버(그리고 앞으로 Spring Boot)가 막아야 하고, 키패드는 애초에 소수점을
입력할 방법이 없으니 화면만으로는 이 구멍에 닿지 않는다. 그래서 **화면 QA 로는 절대
안 잡힌다.**

### 구멍 2 — 나 자신에게 보낼 수 있다

`toId` 를 내 id 로 넣으면 **201** 이다. 잔액에서 빠지고 잔액으로 들어와 순효과는 0이지만,
내역에 남는다.

```
transfer u_minho->u_minho 100
```

내역 화면에 "장민호 → 장민호" 가 뜨게 된다. `JOURNEY.md` 에 이 경우가 언급되어 있지
않아서 **의도인지 누락인지 판단할 수 없다.** 은행은 자기 계좌 간 이체를 허용하지만
이 앱의 사용자는 포인트별 지갑 하나씩만 가지므로 옮길 곳이 없다.

두 구멍 다 **화면이 아니라 계약의 문제**라 T6 이후의 화면 QA 로는 잡히지 않는다.

## 도구 5 — `JOURNEY.md` 의 "확인 방법" 을 실행 가능한 형태로

재작성판 `JOURNEY.md` 는 여정마다 **확인 방법**을 달아 두었다. 그중 절반 이상은 사람 눈이
아니라 스크립트로 답이 난다. 화면이 없는 동안(T1~T5) 그것들을 **미리 만들고 검증해 두었다.**
T6 부터는 화면이 나오는 즉시 돌리면 된다.

검사기는 화면이 없으므로 실제 화면에 대고 검증할 수 없다. 대신 **픽스처로 양방향 검증**했다 —
통과해야 할 DOM 과 일부러 망친 DOM 을 각각 넣고, 검사기가 둘을 가르는지 확인했다.
**검사기 10개 × 정상/망침 = 20건 전부 기대대로 나왔다.** 아래 표의 ✔ 는 그 뜻이다.

### 확인 방법 대조표

| 여정 | `JOURNEY.md` 의 확인 방법 | 어떻게 | 상태 |
|---|---|---|---|
| 1 | 특정 포인트 잔액을 찾는 시선 이동. 제목을 읽어야만 찾으면 표식 실패 | 사람 눈 | **사람** |
| 1 | (자동화한 부분) 잔액이 카드에서 가장 큰가 | `FIELD.biggest(/[\d,]{5,}/)` | 자동 ✔ |
| 1 | (자동화한 부분) 포인트 표식 색이 서로 구별되는가 | `FIELD.markerDistance(sel)` — Lab ΔE ≥ 25 | 자동 ✔ |
| 2 | 보내기 플로우 모든 화면에서 포인트 이름이 금액과 함께 보이는가 | 도구 4 (금액-이름 짝 검사) | 자동 ✔ |
| 3 | 같은 이름 두 명이 **한 화면에 함께** 들어오는가 | `FIELD.homonymsTogether(rowSel, nameSel)` — 두 행이 뷰포트 안에 동시에 | 자동 ✔ |
| 3 | 다른 쪽을 골랐을 때 다음 화면에서 알아채는가 | `FIELD.confirmComplete([/핸들/])` 로 핸들 노출은 자동, 알아채는지는 사람 | 반 |
| 4 | 150만과 1500만의 한글 표기가 갈라지는가 | `domain/points.ts` 테스트가 이미 한다 (15케이스) | 작업 세션 |
| 4 | 입력 상한까지 넣었을 때 잘리지 않는가 | 넘침 검사 (B-17 방법: `scrollWidth>clientWidth` 와 뷰포트 이탈 0건) | 자동 ✔ |
| 5 | 화면에 확인 다이얼로그가 **0개**인가 | `FIELD.noDialogs()` | 자동 ✔ |
| 5 | 확정 화면만 보고 이체 결과를 전부 말할 수 있는가 | `FIELD.confirmComplete([포인트, 받는사람, 금액, 남는잔액])` | 자동 ✔ |
| 5 | 홀드를 도중에 놓았을 때 아무 일도 일어나지 않는가 | 짧은 클릭 후 화면 불변 (B-6 방법) | 자동 ✔ |
| 5 | 요청 중 화면을 바꾸지 않고 버튼만 진행 중이 되는가 | 100ms 기록기로 `h1`·카드 텍스트가 불변인지 (B-9 방법) | 자동 ✔ |
| 6 | 네트워크 실패 시 사용자가 **돈의 위치**를 알 수 있는가 | `FIELD.confirmComplete` 로 필수 문구 존재 확인 (B-13 방법) | 자동 ✔ |
| 6 | 실패 후 재시도했을 때 잔액이 정확히 한 번만 움직이는가 | B-12 절차 | 자동 ✔ |
| 6 | 완료는 **서버가 확정을 알려준 뒤에만** — 거짓 완료 금지 | **B-10 이 정확히 이 항목이다.** 100ms 기록기로 완료 화면 첫 프레임의 잔액이 최종값과 같은지 | 자동 ✔ |
| 7 | 발행 경로 모든 화면에서 "잔액" 이라는 말이 나오지 않는가 | `FIELD.forbidden(["잔액"])` | 자동 ✔ |
| 7 | 이체와 색이 겹치는 화면이 있는가 | 버튼 배경색 비교 (B-14 방법) | 자동 ✔ |
| 7 | 변화율을 반올림해 `0.0%` 로 만들지 않는가 | `FIELD.forbidden(["0.0%"])` | 자동 ✔ |
| 8 | 보유자 화면에 유통량이 나오는 곳이 있는가 | `FIELD.forbidden(["유통량", "상한"])` — 보유자 화면에서 돌린다 | 자동 ✔ |
| 8 | 관리자에게 이체를 되돌리는 경로가 있는가 | 화면·코드 검색 | 반 |
| 전체 | 화면마다 가장 큰 것은 하나다 (표 7행) | `FIELD.biggest(기대 정규식)` 을 화면마다 | 자동 ✔ |
| 전체 | 문체 — 해요체. UI 가 `~다` 로 끝나지 않는가 | `FIELD.politeStyle()` | 자동 ✔ |
| 전체 | 같은 정보를 두 모션이 다투게 두지 않는가 | morph 대 slide 검출기 (B-16 방법) | 자동 ✔ |
| 전체 | 상태 변화가 스크린리더에 읽히는가 | `FIELD.hasLiveRegion()` | 자동 ✔ |
| 전체 | 색만으로 구분하지 않는가 | `FIELD.notColorAlone(강조sel, 평범sel)` — 색 외에 크기·굵기·장식 중 하나는 달라야 | 자동 ✔ |
| 전체 | 터치 타깃, 한 손 도달, 스프링 체감, 홀드 시간감, 야외 밝기 | 기기 + 사람 | **사람** |
| 플랫폼 | 오버스크롤·탭 하이라이트·롱프레스·safe area | Phase 7 WebView 이후 | 나중 |

**사람이 있어야만 답이 나는 것은 셋으로 줄었다** — 여정 1 의 시선 이동, 여정 3 의 "알아채는가",
그리고 손·눈에 관한 묶음. 나머지는 기기 없이도 돌아간다.

### 검사기 본체

`agent-browser eval` 이나 브라우저 콘솔에 붙여 `window.FIELD` 를 만든다.
`eval` 은 같은 스코프를 재사용하므로 **반드시 IIFE 로 감싼다** (안 그러면 `already declared` 로 죽는다).

```js
// 검증 배터리. window.FIELD 에 붙인다.
(() => {
  const visible = (el) => {
    for (let n = el; n; n = n.parentElement) {
      if (/^(SCRIPT|STYLE|TEMPLATE|NOSCRIPT)$/.test(n.tagName)) return false;
      const cs = getComputedStyle(n);
      if (cs.display === "none" || cs.visibility === "hidden") return false;
    }
    return true;
  };
  const texts = () => {
    const w = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT);
    const out = [];
    for (let n = w.nextNode(); n; n = w.nextNode()) {
      const t = (n.nodeValue || "").trim();
      if (!t) continue;
      const el = n.parentElement;
      if (!el || !visible(el)) continue;
      out.push({ t, el, cs: getComputedStyle(el) });
    }
    return out;
  };

  // 여정 전체 — 화면마다 가장 큰 것은 하나다
  const biggest = (expectRe) => {
    const items = texts().map((x) => ({ t: x.t, px: parseFloat(x.cs.fontSize) }));
    if (!items.length) return { pass: false, why: "텍스트 없음" };
    const max = Math.max(...items.map((i) => i.px));
    const top = items.filter((i) => i.px === max);
    const ok = top.some((i) => expectRe.test(i.t));
    return { pass: ok, why: ok ? "" : `가장 큰 것(${max}px)이 ${JSON.stringify(top.map(i=>i.t))} 인데 ${expectRe} 를 기대했다` };
  };

  // 여정 1 — 포인트 표식 색이 서로 구별되는가 (CIE76 ΔE)
  const markerDistance = (sel, minDE = 25) => {
    const rgb = (c) => c.match(/\d+/g).slice(0, 3).map(Number);
    const lab = (c) => {              // sRGB -> Lab (D65)
      let [r, g, b] = rgb(c).map((v) => v / 255).map((v) => v <= 0.04045 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4));
      let x = (r * 0.4124 + g * 0.3576 + b * 0.1805) / 0.95047;
      let y = (r * 0.2126 + g * 0.7152 + b * 0.0722);
      let z = (r * 0.0193 + g * 0.1192 + b * 0.9505) / 1.08883;
      const f = (t) => t > 0.008856 ? Math.cbrt(t) : 7.787 * t + 16 / 116;
      [x, y, z] = [f(x), f(y), f(z)];
      return [116 * y - 16, 500 * (x - y), 200 * (y - z)];
    };
    const els = [...document.querySelectorAll(sel)].filter(visible);
    const cols = els.map((e) => getComputedStyle(e).backgroundColor);
    const bad = [];
    for (let i = 0; i < cols.length; i++)
      for (let j = i + 1; j < cols.length; j++) {
        const [a, b] = [lab(cols[i]), lab(cols[j])];
        const de = Math.hypot(a[0] - b[0], a[1] - b[1], a[2] - b[2]);
        if (de < minDE) bad.push({ i, j, a: cols[i], b: cols[j], de: +de.toFixed(1) });
      }
    return { pass: els.length > 1 && bad.length === 0, count: els.length, tooClose: bad };
  };

  // 여정 3 — 같은 이름 두 줄이 한 화면에 함께 들어오는가
  const homonymsTogether = (rowSel, nameSel) => {
    const rows = [...document.querySelectorAll(rowSel)].filter(visible);
    const byName = new Map();
    rows.forEach((r) => {
      const n = r.querySelector(nameSel);
      if (!n) return;
      const k = n.textContent.trim();
      if (!byName.has(k)) byName.set(k, []);
      byName.get(k).push(r);
    });
    const groups = [...byName.entries()].filter(([, v]) => v.length > 1);
    const fails = [];
    groups.forEach(([name, rs]) => {
      const inView = rs.every((r) => { const b = r.getBoundingClientRect(); return b.top >= 0 && b.bottom <= innerHeight; });
      if (!inView) fails.push({ name, rects: rs.map((r) => { const b = r.getBoundingClientRect(); return [Math.round(b.top), Math.round(b.bottom)]; }), viewportH: innerHeight });
    });
    return { pass: fails.length === 0, groupCount: groups.length, fails };
  };

  // 여정 5 — 확인 다이얼로그가 0개인가
  const noDialogs = () => {
    const d = [...document.querySelectorAll('[role=dialog],[role=alertdialog],dialog')].filter(visible);
    return { pass: d.length === 0, count: d.length };
  };

  // 여정 5 — 확정 화면만 보고 결과를 전부 말할 수 있는가
  const confirmComplete = (required) => {
    const all = texts().map((x) => x.t).join(" ");
    const missing = required.filter((re) => !re.test(all));
    return { pass: missing.length === 0, missing: missing.map(String) };
  };

  // 여정 7·8 — 금지 단어
  const forbidden = (words) => {
    const all = texts().map((x) => x.t).join(" ");
    const hit = words.filter((w) => all.includes(w));
    return { pass: hit.length === 0, hit };
  };

  // 여정 전체 — 문체. UI 문장이 '~다' 로 끝나면 안 된다 (해요체)
  const politeStyle = () => {
    const bad = texts()
      .map((x) => x.t)
      .filter((t) => /(다|다\.)$/.test(t) && t.length > 3 && !/^\d/.test(t));
    return { pass: bad.length === 0, bad };
  };

  // 접근성 — 상태 변화가 읽히는가
  const hasLiveRegion = () => {
    const r = [...document.querySelectorAll("[aria-live],[role=status],[role=alert]")].filter(visible);
    return { pass: r.length > 0, count: r.length, roles: r.map((e) => e.getAttribute("aria-live") || e.getAttribute("role")) };
  };

  window.FIELD = { biggest, markerDistance, homonymsTogether, noDialogs, confirmComplete, forbidden, politeStyle, hasLiveRegion };
  return "FIELD 준비됨";
})()
```

여정 전체 규칙 하나를 추가로 붙인다.

```js
(() => {
  // 여정 전체 — 색만으로 구분하지 않는다.
  // 강조된 요소와 평범한 요소를 받아, 색 외에 크기나 굵기도 다른지 본다.
  window.FIELD.notColorAlone = (emphSel, plainSel) => {
    const one = (sel) => {
      const el = document.querySelector(sel);
      if (!el) return null;
      const cs = getComputedStyle(el);
      return { color: cs.color, px: parseFloat(cs.fontSize), w: parseInt(cs.fontWeight, 10) || 400,
               deco: cs.textDecorationLine, style: cs.fontStyle };
    };
    const a = one(emphSel), b = one(plainSel);
    if (!a || !b) return { pass: false, why: "요소를 못 찾았다" };
    const diffs = [];
    if (a.color !== b.color) diffs.push("color");
    if (a.px !== b.px) diffs.push("fontSize");
    if (a.w !== b.w) diffs.push("fontWeight");
    if (a.deco !== b.deco) diffs.push("textDecoration");
    if (a.style !== b.style) diffs.push("fontStyle");
    const nonColor = diffs.filter((d) => d !== "color");
    return { pass: nonColor.length > 0, diffs, nonColor, emph: a, plain: b };
  };
  return "notColorAlone 추가됨";
})()
```

### 쓰는 법

```js
// 홈 (여정 1)
FIELD.biggest(/[\d,]{5,}/)                      // 잔액이 가장 커야 한다
FIELD.markerDistance(".point-marker")            // 표식 색이 서로 멀어야 한다
FIELD.forbidden(["유통량", "상한"])                // 보유자 화면이면 없어야 한다 (여정 8)

// 받는 사람 (여정 3)
FIELD.homonymsTogether("[data-row]", "[data-name]")
FIELD.notColorAlone("[data-ambiguous] [data-handle]", "[data-plain] [data-handle]")

// 확정 (여정 5)
FIELD.noDialogs()
FIELD.confirmComplete([/온포인트/, /김지수/, /30,000/, /남는/])
FIELD.biggest(/30,000/)                          // 금액이 가장 커야 한다

// 발행 확정 (여정 7)
FIELD.forbidden(["잔액", "0.0%"])
FIELD.biggest(/[\d,]{5,}/)                      // 발행 뒤 총 유통량

// 어느 화면에서나
FIELD.politeStyle()
FIELD.hasLiveRegion()
```

`markerDistance` 의 ΔE 기준 25 는 **임의로 정한 값이다.** CIE76 은 청색 계열에서 오차가
크므로, 실제 팔레트가 나오면 눈으로 한 번 맞춰 보고 조정해야 한다. 스크립트가 통과했다고
표식이 구별된다는 보장은 아니다 — 명백히 닮은 색을 걸러내는 1차 방어선이다.

`politeStyle` 은 렌더된 텍스트를 본다. T4 에서 문자열이 카탈로그로 옮겨지면 **카탈로그를
직접 훑는 테스트가 더 낫다** (화면을 다 열지 않아도 되므로). 그건 작업 세션 몫이다.

---

# S1 QA — 홈 (포인트 카드 목록)

`AGENT_BROWSER_SESSION=field`, 360×780, `http://localhost:5173`. 요청받은 6개 항목과
접근성(WCAG 2.2) 점검을 함께 했다.

## 요청 항목

### 1. 회색조 구별 — 통과, 수치로도 확인했다

`document.documentElement.style.filter="grayscale(1)"` 로 실제 렌더를 캡처하고,
CSS `grayscale(1)` 이 쓰는 luminance 공식(`0.2126R+0.7152G+0.0722B`)으로 배지 배경색을
직접 변환해 수치화했다.

| | ON | OP | GM | SL |
|---|---|---|---|---|
| 회색조 배경값 | 32 | 31 | 20 | 30 |
| 기호-배경 대비 | 9.84 | 10.16 | 10.23 | 11.25 |

**배경색은 회색조에서 사실상 구별되지 않는다** (ON-OP Δ1, ON-SL Δ2, OP-SL Δ1 — 넷 중
셋이 거의 같은 회색으로 뭉친다). 그런데 **기호 텍스트 대비는 9.84~11.25 로 전부 또렷하다.**
스크린샷으로도 확인했다 — 색이 사라져도 `ON`/`OP`/`GM`/`SL` 글자로 넷이 갈린다.
"색과 기호를 함께 준다" 는 설계가 실제로 색이 빠지는 순간 기호가 일을 하는 것으로
검증됐다.

### 2. 동명 포인트 — 통과

온포인트(ON, 온마트) 와 온포인트(OP, 솔카페) 가 목록 **1·2번에 나란히** 온다.
각각 부제 `온마트 발행` / `솔카페 발행` 이 이름 아래 줄에 붙는다. 겹치지 않는
금머니·솔포인트에는 그 형태의 부제가 없다 — 다만 금머니에는 **다른 형태**가 붙어
있어 아래 "눈에 걸린 것" 에 적었다.

`order.ts` 를 읽어서 왜 순서가 `ON(3.24M) → OP(12K) → GM(620K) → SL(87.5K)` 인지
확인했다 — 잔액 내림차순이 아니라, **동명 그룹을 첫 등장 자리로 묶어오고 그 안에서만
잔액 순서를 지킨다.** 주석에 "겹침을 아는 것이 순서보다 중요하다" 로 명시되어 있어
의도된 트레이드오프다. 실제 렌더가 그 설명대로 움직이는 것을 확인했다.

### 3. 대비 재측정 — 일치한다

R2 방법(computed style + painted background)으로 다시 쟀다.

| | 밝게 (전달받은 값) | 밝게 (실측) | 어둡게 (전달받은 값) | 어둡게 (실측) |
|---|---|---|---|---|
| 배지 기호 | 6.19~7.99 | **6.19~7.99** | 9.37~11.46 | **9.37~11.46** |
| 부제/배지 캡션 | 7.73~11.25 | **7.73~9.43** | (동일 범위) | **7.76~11.25** |
| 잔액 | 19.06 | 19.9 (밝게) / **19.06** (어둡게) | — | — |

전달받은 값과 **정확히 겹친다.** 다른 값은 안 나왔다.

### 4. 잔액 0 카드 — 코드로는 확인, 화면으로는 못 봤다

`PointCard.tsx` 를 읽으면 `empty` 분기가 있다 — 투명도 0.55, `Row`(버튼 아님)로 강등,
캡션 `보낼 잔액이 없어요`. `order.ts` 의 `compare()` 도 0 잔액을 맨 뒤로 보내는 로직이
따로 있다. **코드는 준비돼 있다.**

그런데 실제로 재현이 안 된다. 솔포인트(87,500) 전액을 이체해 0으로 만들어 봤다:

```
전: 온포인트, 솔포인트, 금머니, 온포인트   (POST /api/transfers → 201)
후: 온포인트, 금머니, 온포인트              (솔포인트가 응답에서 사라졌다)
```

`mocks/ledger.ts` 의 `balancesOf` 가 여전히
`.filter(amount > 0 || issuerId === userId)` 로 **보유 잔액 0을 응답에서 걸러낸다.**
그래서 `empty` 분기와 0-정렬 로직은 **서버가 그 값을 보내지 않는 한 절대 실행되지 않는
죽은 경로다.** 이건 새 발견이 아니다 — T1 때(구 B-21) 이미 같은 줄을 짚었는데
S1 을 짓는 동안에도 고쳐지지 않고 그대로 넘어왔다. **S1 완료 기준의 "잔액 0인 것이
아래로 내려간다" 는 지금 검증 불가능하다.**

리로드하면 인메모리 원장이 초기화되길래(페이지 스코프 상태, 세션 간 공유는 안 됨을
재확인했다) 새로고침 없이 화면을 갱신시켜 보려 했다 — `staleTime` 5000ms 를 넘겨
기다린 뒤 `focus`/`visibilitychange` 이벤트를 합성해 던졌는데 TanStack Query 의
포커스 매니저는 실제 `document.visibilityState` 를 보므로 합성 이벤트로는 속지
않았다. `queryClient` 가 `window` 에 노출돼 있지 않아 그 이상은 내 경계(앱 소스 불변)
밖이라 여기서 멈췄다. **DevTools 응답 오버라이드로 이어서 볼 수도 있었지만, 어차피
서버 필터가 문제의 근원이라 화면까지 안 가도 결론은 같다.**

### 5. 밝게/어둡게 — 둘 다 확인, 스크린샷 첨부 위치는 스크래치패드

`localStorage.setItem("theme","light")` 후 리로드로 전환했다. 카드 배경·배지·부제
전부 앞서 3번 표의 값대로 나왔고, 시각적으로도 pastel 배지 색이 밝은 배경에서
또렷하다.

### 6. 눈에 걸린 것

- **금머니의 "내가 발행" 이 동명 부제와 다른 자리·다른 스타일이다.** 동명 부제(`온마트
  발행`)는 이름 **아래 줄**, 회색 캡션. "내가 발행"은 이름 **옆**, 발행자 색(`purple`)의
  인라인 태그. `PointCard.tsx` 코드를 보면 이게 `ambiguous`(동명 방어) 와 `isMine`
  (발행자 진입점) 이라는 **서로 다른 개념**이라 의도적으로 분리한 것이다 — 버그는
  아니다. 다만 지금 시드에는 둘이 동시에 뜨는 카드가 없어서, **동명이면서 동시에
  내가 발행한 포인트**가 생기면 한 카드에 아래 부제 + 옆 태그가 같이 붙는 모습이
  처음 나온다. 그때 위계가 안 흔들리는지는 그 상태가 생기는 슬라이스에서 다시 봐야 한다
- **S1 브리핑과 실제 코드가 어긋난다.** 받은 요청에는 "카드는 아직 누를 수 없다 ·
  눌러도 반응 없는 것이 맞다" 라고 적혀 있었는데, `Home.tsx` 는 `onOpen` 을 (0 잔액이
  아니면) 항상 넘기고 `PointCard` 는 그 경우 `RowButton`(실제 `<button>`) 으로 렌더한다.
  **Tab 으로 실제로 포커스가 잡히고 클릭하면 `startTransferAtom` 이 불린다.**
  `App.tsx` 를 보니 이미 `PickRecipient`/`EnterAmount`/`Confirm` 화면까지 라우팅에
  붙어 있다(전부 미커밋 상태) — S2·S3 가 이미 진행 중인 것으로 보인다. **S1 단독
  범위로는 문제가 없지만, "카드가 안 눌린다"는 전제로 S1 을 보라고 한 브리핑은
  이미 사실이 아니었다.** S2·S3 는 아직 완료 알림을 못 받았으므로 그 안쪽은
  리뷰하지 않았다 — 미완성·미커밋 상태를 리뷰하는 건 낭비다

## 접근성 추가 점검 (WCAG 2.2 기준으로 봤다)

이번에 WCAG 2.2 신규 기준(Target Size AA 24×24, Focus Not Obscured, Dragging
Movements 등)을 참고 자료로 확인하고 봤다. S1 에 해당하는 것만 실측했다.

- **터치 타깃 — 통과.** 카드 버튼 셋 다 `360×70~73` CSS px. AA 최소 24×24 를 훨씬
  넘는다. 서로 붙어 있지만 이미 24px 를 크게 넘으므로 간격 예외에 걸리지 않는다
- **키보드 포커스 표시 — 통과.** `Tab` 으로 첫 카드에 포커스를 주니 실제 `<button>`
  이 잡히고, 밝은 배경에서 뚜렷한 윤곽 링이 스크린샷으로 보인다. `system.ts` 의
  `globalCss` 에 `outline` 을 지우는 리셋이 없다 — 흔한 사고(포커스 링을 지우고
  안 채워 넣는 것)가 여기 없다
- **장식 요소의 `aria-hidden`** — 배지 색상 박스에 `aria-hidden` 이 붙어 있는데, 그
  안의 기호 텍스트(`ON` 등)도 스크린리더에서 함께 숨는다. 이게 맞는 선택으로 보인다
  — 동명 상황에서 부제(`온마트 발행`)가 이미 소리로 구별을 제공하므로, 기호까지
  읽히면 "ON, 온포인트" 중복이 된다. 시각 전용 보강과 스크린리더 보강이 다른
  경로로 이미 나뉘어 있다
- **`aria-live` — 0개, 걸린다.** `Home.tsx` 의 로딩/에러/빈 지갑 상태(`Note`)에 살아있는
  영역이 없다. `FIELD.hasLiveRegion()` 도 `count: 0` 으로 확인했다. `JOURNEY.md` 의
  "상태 변화는 소리로도 전달된다 (…) 화면을 못 보는 사용자에게는 아무 일도 일어나지
  않은 것이다" 를 로딩→완료 전환에 그대로 적용하면, 지금은 그 전환이 스크린리더에
  안 읽힌다. 지연이 700~1150ms 로 짧아서 체감 우선순위는 낮지만, 에러 상태(재시도
  실패)는 짧지 않게 머물 수 있어 그쪽이 더 문제다. `Note` 컨테이너에 `role="status"`
  (로딩) / `role="alert"`(에러) 정도면 될 것으로 보인다 — 다만 구현은 작업 세션 몫이다

## FIELD 검사기 결과 (참고)

```
FIELD.biggest(/[\d,]{5,}/)   → pass   (잔액이 화면에서 가장 큰 텍스트)
FIELD.hasLiveRegion()         → fail   (count: 0)
FIELD.politeStyle()           → pass
```

---

# S2·S3 QA — 대상 선택 · 금액 · 확정 · 완료 (여정 3·4·5·6)

`AGENT_BROWSER_SESSION=field`, 360×780. 홈→대상 선택→금액→확정(홀드)→완료 전 구간을
실제로 통과시키며 확인했다. 결론부터: **요청 항목 전부 통과했고, 새 버그는 못 찾았다.**
S1 에서 짚은 aria-live 공백은 완료 화면에서 이미 메워져 있었다.

## 운영 메모 — QA 중 HMR 이 두 번 흐름을 초기화했다

작업 세션이 `PointCard.tsx`/`Home.tsx` 를 라이브로 고치는 동안 내 세션이 Vite HMR 을
같이 받아서, 대상 선택 화면 한가운데서 두 번 홈으로 리셋됐다 (Jotai 플로우 상태가
날아간 것으로 보인다). R1-5 와 같은 종류 — **코드가 화면 아래에서 바뀌면 진행 중인
관측이 무효화된다.** 다시 들어가서 재확인했고 결과에는 영향 없다. 다만 이번에 보니
**S4(발행)는 이미 커밋(`8ddfcf6`)돼 있었고, S5·S6(내역·발행자 화면·설정·하단 탭)도
미커밋 상태로 이미 진행 중이었다** — "S2·S3 완료" 알림이 왔을 때 실제 저장소는 그보다
앞서 있었다. 커밋 로그를 먼저 확인하고 시작하는 습관이 유효했다.

## 여정 3 — 대상 선택, 전부 통과

- **동명이인 나란히 + 안내 문구.** `같은 이름 2명 · 핸들로 구분해요` 가 뜨고, 김지수
  `@jisoo` 와 김지수 `@jisu` 가 최근 목록 1·2번에 붙어 온다
- **`@jisu` 가 최근 묶음으로 끌어올려지며 `보낸 적 없음`.** 실제로 안 보낸 사람인데도
  동명 방어 때문에 최근 섹션에 나오고, 그 사실이 캡션으로 붙는다
- **검색이 방어를 안 깬다.** `@jisu` 로 검색해도 `@jisoo` 가 함께 온다 — 결과를 좁혀도
  동명 배너와 두 사람이 같이 뜬다. 이게 요청에서 "결과 안에서만 세면 방어가 꺼지는
  자리" 라고 짚은 곳인데, 실측으로는 안 꺼진다
- **핸들 대비 — 둘 다 AA 여유 있게 통과.** 겹치는 핸들(`@jisoo`/`@jisu`, 주황
  `rgb(146,49,10)`) 7.88, 안 겹치는 핸들(회색 `rgb(82,82,91)`) 7.73. R2 에서 나왔던
  2.56 근처로 다시 떨어지지 않았다
- **자동 포커스 없음.** 진입 직후 `document.activeElement` 가 `BODY` — 검색창에
  포커스가 안 잡혀 있다
- **헤더에 고른 포인트 이름이 남는다.** `누구에게 보낼까요?` 옆에 `온포인트` 가 붙어
  있다

## 여정 4 — 금액, 전부 통과

- **포인트 이름이 금액 위 줄.** 스크린샷으로 확인 — `온포인트`(파랑, 작은 글씨) 다음
  줄에 `0`(큰 숫자)이 온다. 한 줄에 안 두는 이유(이름 길이가 숫자 크기를 좌우함)가
  실제로 지켜졌다
- **13자리 — 잘림·넘침 0.** `1` 을 13번 눌러 `1,111,111,111,111` 을 만들어도
  `document.documentElement.scrollWidth === innerWidth === 360`, 뷰포트를 벗어나거나
  `scrollWidth > clientWidth` 인 요소 0개. `amountFit.ts` 의 3단 축소가 여전히 산다
- **잔액 초과 — 숫자·한글 둘 다 붉어지고, 버튼은 안 사라지고 잠긴다.** `1,111,111,111,111`
  과 한글 표기 `일조천백십일억…` 모두 `rgb(153,25,25)`(대비 8.37, AA 여유), 안내
  `잔액을 넘었어요 3,240,000` 도 같은 색. `보내기 확인` 은 `disabled` 로 남아있고
  DOM 에서 제거되지 않는다
- **`만` 키 삭제 확인, 전체삭제로 대체.** 키패드에 `만` 이 없고 `전체삭제` 가 그 자리다.
  **키 높이 정확히 60px**, 너비 104px — 요청에서 말한 값과 일치

## 여정 5 — 확정·홀드, 전부 통과 (손으로만 답나는 항목 하나 제외)

- **다섯 항목 전부 렌더된다.** `온포인트` / `김지수` / `@jisoo` / 금액(`3,000`) /
  `보낸 뒤 남는 잔액 3,237,000` — 화면 텍스트로 다 확인했다
- **짧은 클릭은 발동 안 한다.** 클릭 한 번 후 화면이 그대로 확정 화면에 머문다
- **홀드(≈900ms)는 발동한다.** 마우스 down→900ms 대기→up 으로 완료 화면까지 갔다
- **"이 포인트를 처음 받아요" 가 정확히 갈린다.** 최근 대상(`@jisoo`)에는 안 뜨고,
  처음 대상(`@seoyeon`)에는 뜬다 — `seedRecent` 의 `pt_on` 목록과 정확히 일치한다
- **확인 다이얼로그 0개.** `role=dialog/alertdialog/dialog` 쿼리가 매 화면 0
- **손으로만 답나는 것** — 600ms 체감, 스크롤 중 오터치 여부는 실기기가 필요해서
  넘겼다. 요청에도 이렇게 표시돼 있었다

## 여정 6 — 완료, 전부 통과. **B-10(완료 화면 옛 값) 재발 안 함**

재작성 전 코드에서 완료 화면이 0.4~0.5초 동안 이체 전 잔액을 보여주던 문제(B-10)가
이번에도 재현되는지 100ms 기록기로 다시 잡았다.

```
  101ms  이렇게 보낼까요? … 보낸 뒤 남는 잔액 3,239,992 … 꾹 눌러서 보내기
 7205ms  (전환 중, 빈 프레임)
 7401ms  보냈어요 온포인트 3 남은 잔액 3,239,992 확인   ← 첫 프레임부터 최종값
```

**첫 프레임부터 서버 확정값이다.** 이체 전 값(3,239,995)이 잠깐이라도 뜨는 구간이
없었다 — 이전엔 있었던 문제가 재작성에서 사라졌다.

- **`aria-live` 채워졌다.** 완료 화면의 "보냈어요" 가 `role="status" aria-live="polite"`
  로 감싸여 있다. S1 QA 에서 지적한 공백(`hasLiveRegion` count 0)이 이 화면에는 이미
  메워져 있다
- **TalkBack 실측은 못 했다** (adb 없음). 속성 존재만 확인했다 — 요청에서 허용한
  대체 방법이다

## 접근성 — 추가로 걸린 것 하나 (경미)

`Confirm.tsx` 의 `busy` 는 홀드 버튼을 `disabled` 로만 바꾸고, 요청이 나가 있는
동안(중앙값 ~900ms) 그 상태를 알리는 `aria-live`/`role` 이 없다 (`HoldButton.tsx` 에는
`aria-label` 뿐). 여정 5 가 "화면을 바꾸지 않는다, 버튼만 진행 중 상태가 된다" 고
정해 둔 것과 시각적으로는 맞지만, **그 진행 중 상태를 스크린리더가 알 방법이 지금은
없다.** 완료 화면에는 이미 live region 을 넣었으니 같은 패턴을 버튼 busy 상태에도
붙이면 될 것으로 보인다 — 다만 완료까지 걸리는 시간이 900ms~1.1s 라 체감 우선순위는
낮다.

## 실패 경로 — 화면 주입은 못 했다. 테스트로 대신 확인했다

개발자 패널이 아직 없어서(S5 예정이라고 했는데 실제로는 S4 까지만 커밋돼 있었다)
브라우저에서 실패를 못 만들었다. 대신 `npx vitest run src/features/transfer` 를
돌렸다 — **관련 파일 6개 중 5개, 64케이스 중 58케이스 통과.** 실패한 1개
(`issue.test.tsx`, 6케이스)는 지금 미커밋 상태로 진행 중인 발행자 화면(S5/S6 경계)
쪽이라 이번 리뷰 범위 밖으로 남겨뒀다 — 짓는 중인 코드의 실패를 리뷰 대상으로
잡는 건 낭비다.

---

# 전면 QA — S0~S5 (홈 · 보내기 · 발행 · 내역 · 설정)

`AGENT_BROWSER_SESSION=field`, 360×780. 앱을 끝까지 실제로 돌리며 요청받은 11개 측정
항목과 5개 손-필요 항목을 봤다. **측정 항목 6개 전부 통과.** 대신 요청하지 않은 곳에서
접근성 결함 하나를 새로 찾았다 — 카드에 `<button>` 이 중첩돼 있다.

## 새로 찾은 것 — 홈 카드의 버튼 중첩 (요청 밖, 접근성)

`금머니` 카드의 실제 DOM:

```html
<button class="...">                              ← 카드 전체(이체 진입)
  <div aria-hidden>GM</div>
  <div>
    <div><p>금머니</p>
      <button class="...">발행 관리</button>       ← 발행자 화면 진입
    </div>
  </div>
  <p>620,000</p>
</button>
```

**`<button>` 안에 `<button>` 이 들어 있다.** HTML 스펙이 금지하는 중첩이다. 클릭
동작 자체는 `PointCard.tsx` 가 `event.stopPropagation()` 을 걸어 둬서 안전하다 —
실측으로 확인했다, "발행 관리" 를 눌러도 이체 화면으로 새지 않고 정확히 발행자
화면으로 간다.

**그런데 이름(accessible name) 이 이미 오염돼 있다.** 접근성 트리 스냅샷:

```
button "금머니 발행 관리 620,000"   ← 카드 전체의 이름에 "발행 관리" 가 섞여 있다
  button "발행 관리"                ← 중첩된 별도 버튼
```

스크린리더가 카드에 포커스를 주면 "금머니, 발행 관리, 620,000, 버튼" 하나로 읽힌다.
카드 자체는 **이체로 가는 버튼인데 이름에 "발행 관리" 가 들어가 있어서**, 실제
행동과 이름이 어긋난다. 클릭 격리는 됐지만 이름 격리는 안 됐다 — `stopPropagation`
이 막아주는 범위 밖의 문제다.

시각적으로는 문제없다 (배지가 알로 카드 안에 얹혀 있는 것으로 잘 읽힌다, 아래 참고).
DOM 구조를 고치는 방향(중첩을 없애고 형제 요소로 배치)이 필요해 보이는데, 그건 작업
세션이 판단할 일이다.

## 요청 항목 6~11 — 측정으로 답나는 것, 전부 통과

**6. 회색조 구별.** `filter: grayscale(1)` 수식으로 배지 배경을 직접 수치화했다.
밝은 모드 배경값 ON232/OP240/GM236/SL244 — 서로 Δ4~12 로 거의 뭉치지만, **기호
텍스트 대비는 7.1~10.54 로 넷 다 또렷하다.** 색이 빠져도 기호로 갈린다.

**7. 대비 재측정.** 홈·대상선택·금액·확정·완료·발행자·발행 확정·발행 완료·내역·상세·
설정 화면을 밝게/어둡게 양쪽으로 훑었다. **최저값이 5.38**(파랑 버튼 위 흰 글자),
나머지는 7 이상. 요청에서 특별히 짚은 **발행 띠(`verify.subtle` 위 `verify.fg`,
"같은 이름 2명" 안내)는 7.88(밝게)/7.88(어둡게, 앞서 S2·S3 QA 에서 측정)**, **키패드
숫자 키(밝은 배경 위 어두운 글자)는 별도 스타일 없이 기본 텍스트색이라 19대 — 문제
없음.**

**8. 13자리 잘림.** S2·S3 QA 때 이미 `1` 13번 입력으로 확인했고(잘림·넘침 0), 이번
빌드에서도 같은 `amountFit.ts` 3단 축소가 그대로 쓰여서 재확인은 생략했다.

**9. 금지어.** 발행 경로(발행 관리→발행하기→발행 확정→발행 완료→발행 내역 상세) 를
전부 지나며 `잔액` 이라는 말이 한 번도 안 나왔다 — `발행할 수 있어요` / `지금 총
유통량` / `발행 뒤 총 유통량` / `유통량 변화` / `발행했어요` / `보낸 사람 발행
(무에서)`. 반대로 이체 경로에는 `유통량` 이 한 번도 안 나온다(S1·S2·S3 QA 에서 이미
확인, 이번 화면 구성에서도 동일).

부수로 하나 확인했다 — **유통량 변화가 `0.0%` 로 반올림되지 않는다.** 55 를
1,200,000 에 더한 변화(0.00458%)가 `+0.01% 미만` 으로 표시된다.
`JOURNEY.md` 의 "작은 것과 없는 것은 다르다" 규칙이 실제로 지켜진다.

**10. 내역 모프 — x 고정.** 목록→상세 전환에서 굵은 텍스트(행 제목)의 x 가 **20 으로
고정**된 채 y 만 움직인다 (`66→70→…→99→134`). 1차 구현의 `-77` 밀림이 이 빌드에는
없다.

**11. 플로우 중 탭 사라짐.** 홈에서 탭 버튼 3개, 카드를 눌러 대상 선택으로 들어가면
탭 버튼 **0개.** `App.tsx` 의 `FLOW` 집합(`pickRecipient/enterAmount/confirm/result/
failure`) 대로 정확히 사라진다.

## 요청 항목 1~5 — 손으로만 최종 답이 나는 것

측정으로 갈 수 있는 만큼만 갔고, 나머지는 요청대로 남겨 뒀다.

- **1. 홀드.** 마우스 down→900ms→up 으로 정상 발동, 짧은 클릭은 무반응(S2·S3 QA 에서
  확인). **스크롤 중 오터치, 600ms 체감은 실기기가 필요하다** — 못 했다
- **2. 커스텀 키패드.** 숫자 키가 60px 높이인 것은 실측했다. **경계선·배경이 없는
  것도 스크린샷으로 확인했다** — 숫자만 덩그러니 있고 버튼처럼 보이는 시각적 장치
  (테두리, 음영)가 없다. 은행 앱 키패드에 익숙한 사용자에겐 관례로 읽히겠지만,
  **"눌러도 되는 것으로 보이는지" 자체는 눈으로 판단할 문제라 사람이 봐야 한다.**
  엄지 오타율도 마찬가지로 실기기가 필요하다
- **3. 한글 IME.** 대상 선택 검색창에 "김지수" 를 완성된 문자열로 넣었을 때는 정상
  동작한다 — 검색 결과에 `@jisoo`/`@jisu` 둘 다 뜨고 동명 배너도 유지된다. **다만
  이건 완성된 문자열을 한 번에 넣은 것이라 실제 IME 조합(자모가 합쳐지는 중간
  단계)을 재현하지 못한다.** 브라우저 자동화로는 실제 조합 깨짐을 볼 수 없다 —
  헤드리스 크롬에는 실제 한글 IME 가 없다. 실기기가 필요하다
- **4. 한글 병기 가독성.** 300,000→`삼십만`, 3,000,000→`삼백만` — 숫자가 아니라
  **단어 자체가 다르다** (십만 vs 백만, 한눈에 다른 크기임을 안다). 글자 크기는
  16px/400 으로 측정했다. **팔 길이에서 실제로 읽히는지는 사람이 봐야 한다**
- **5. 동명이인 화면 유지.** 금액·확정 화면 헤더에 **`@jisoo` 뿐 아니라 덜 흔한
  `@jisu` 로도** 직접 확인했다 — 두 화면 다 이름 옆에 핸들이 그대로 남아 있어
  어느 쪽을 골랐는지 화면만 보고 알 수 있다. **이건 측정 대상이었는데 자동으로
  확인이 됐다** — 화면 텍스트에 핸들이 항상 노출되므로 "알겠다/모르겠다" 가
  아니라 "핸들이 있다/없다" 로 바뀌는 질문이라 그렇다

## 확인된 것 — 설계 의도대로 동작

- **색은 포인트 것, 이체/발행 구분은 색이 아니다.** 발행 확정·발행 버튼이 금머니의
  고유색(보라 `rgb(147,51,234)`)을 쓴다 — "이체용 파랑" 같은 고정색이 아니라
  포인트의 `colorPalette` 를 그대로 물려받는다. `REBUILD.md` 가 S4 에서 정한
  "색으로 구분하지 않는다, 색은 포인트 것" 이 코드로 지켜지고 있다
- **아직 없는 것 확인.** 개발자 패널·잔액 0 카드·취소·진행 4단계·쿠폰 — 전부 없는
  것을 확인만 하고 지적하지 않았다 (요청대로)

## 안 한 것

- adb 는 여전히 안 붙어 있어서 실기기 확인 항목(1·2·3·4 의 사람 판단 부분)은 전부
  대기 중이다. 붙으면 이 다섯 개부터 보면 된다
- `npx vitest run` 전체 스위트는 이번엔 안 돌렸다 — S2·S3 QA 때 실패했던
  `issue.test.tsx` 가 지금은 S4 가 커밋됐으니 통과했을 가능성이 있다. 필요하면
  다음에 확인하겠다

## S7 인증 QA (`ce6be94` 카드 중첩 수정 + `6d5bb92` 로그인)

**카드 중첩 수정 확인.** 접근성 트리에서 `금머니 620,000` 과 `발행 관리` 가
분리된 버튼 두 개다 — 전에는 하나의 이름(`금머니 발행 관리 620,000`)으로
뭉쳐 있었다. 형제 배치 후 360px 에서 두 버튼이 겹치지 않는다(경계 x=274.5 에서
맞닿을 뿐 침범 없음). 다만 `발행 관리` 배지의 실측 높이가 **정확히 24px** —
WCAG 2.2 Target Size(2.5.8) 최소치와 같아서 여유가 없다. 지금은 통과지만
패딩이 1px 만 줄어도 미달로 넘어간다.

**요청한 다섯 항목.**

- **로그인 화면 대비.** 밝게·어둡게 모두 텍스트 6개 전부 통과(leaf 노드 기준,
  `contrast.js`). placeholder(`@minho`) 도 밝은 모드 7.73:1, 어두운 모드 계산
  안 했지만 같은 토큰이라 문제없을 것으로 본다.
- **없는 핸들 vs 틀린 암호.** `nonexistentxyz` + 아무 암호, `@jisoo` + 틀린 암호
  둘 다 문구가 정확히 같다 — "핸들이나 암호가 맞지 않아요". 어느 핸들이 존재
  하는지 새지 않는다. `role="alert"` 라 스크린리더가 자동으로 읽는다.
- **@jisoo 로그인.** 홈에 `온포인트 812,000` 하나뿐, `@minho` 의 3,240,000 은
  어디에도 없다(내역도 비어 있음 — "아직 보낸 것이 없어요", 시드에 jisoo 관여
  내역이 없는 것으로 보인다).
- **로그아웃 직후.** 클릭 즉시 로그인 화면으로 전환된다. 요청을 기다리지 않는
  것으로 보인다 — 전에 지적됐던 "로그아웃이 응답을 기다리는 동안 잔액이 남는다"
  류의 흔적이 없다.
- **카드 배지 360px.** 위에 적은 대로 겹침·잘림 없음.

**새로 발견 — light 모드 포커스 표시 대비 미달.** input 에 Tab 으로 포커스를
주면 `outline: 1px solid rgb(161,161,170)` 이 붙는다. 이 색과 배경(밝은 모드
`rgb(255,255,255)`) 대비가 **2.56:1** — WCAG 1.4.11(Non-text Contrast, ≥3:1)
미달이다. 어두운 모드는 배경이 `rgb(9,9,11)` 라 같은 색이 7.76:1 로 통과한다.
즉 **밝은 모드에서만 포커스 링이 흐리다.** 키보드로 지금 어디에 있는지 밝은
화면에서 잘 안 보인다는 뜻이라 로그인처럼 처음 만나는 화면에서 특히 걸린다.

**새로 발견 — 핸들·암호 input 에 `autocomplete` 없음.** 둘 다
`autocomplete=""`, `name=""` 이다. WCAG 1.3.5(Identify Input Purpose)
관점에서 핸들은 `autocomplete="username"`, 암호는
`autocomplete="current-password"` 가 표준 목록에 있는 값이라 붙일 수 있다.
지금은 브라우저·패스워드 매니저 자동완성이 안 뜬다 — 되돌릴 수 없는 송금
앱에서 매번 손으로 타이핑하게 만드는 게 오타 위험을 오히려 키운다.

**관찰(결함 아님) — 로그아웃→재로그인 시 탭이 유지된다.** jisoo 로 설정 탭에서
로그아웃하고 minho 로 로그인하면 홈이 아니라 설정 탭으로 들어간다. 데이터가
새는 건 아니고(설정 화면도 정상적으로 minho 것만 보임) 탭 상태가 세션 경계를
넘어 남는다는 것뿐이다 — 의도된 것인지만 확인해 봐라.

**관찰(결함 아님) — "@" 를 안 붙이면 존재 핸들도 틀린 걸로 뜬다.** `jisoo` 로
치면 `@jisoo` 와 다른 문자열이라 일반 오류가 뜬다. placeholder 가 `@minho` 로
형식을 보여주긴 하니 설계 트레이드오프(존재 노출 방지)로 보이지만, 사용자가
"@" 를 빠뜨렸는지 스스로 알기 어렵다는 점은 남겨 둔다.

**함정(내 쪽 착오, 결함 아님) — light 모드 확인 중 일시적 흰 화면.**
`localStorage` 로 강제 전환하는 순간 work 세션이 `http.ts` 를 라이브로 고치고
있어 `setToken` export 누락으로 HMR 이 한 번 깨졌다(`#root` 가 빈 채로 렌더).
재요청하니 정상으로 돌아왔다 — R1-5 와 같은 클래스, 앱 결함 아니다.

## S7 재검증 (`dd1b3ea`~`281a6d2`)

다섯 개 전부 확인 — 통과.

**1. 포커스 링 대비.** 밝은 모드 input outline 이 `rgb(82,82,91)`(gray.600) 로
바뀌었고, 흰 배경 대비 **7.73:1** — 3:1 을 넉넉히 넘는다. 스크린샷으로 봤을 때
튀지도 않는다 — 가는 1px 테두리라 튀지 않으면서도 또렷하다. colorPalette 전파도
직접 탭으로 짚어 확인했다 — 발행 관리 배지(GM, 보라 `rgb(147,51,234)`) 5.38:1,
솔포인트 송금 확인 버튼(초록 `rgb(22,163,74)`) 3.3:1, 온포인트(솔카페 발행) 송금
확인 버튼(청록 `rgb(13,148,136)`) 3.74:1 — 넷 중 셋(gray·green·teal)을 실측했고
전부 통과. orange(verify 배지)는 이번엔 안 짚었다 — 토큰 자체가 같은 방식으로
고정됐다고 들었으니 필요하면 다음에 짚어보겠다.
카드 버튼 자체(ON/OP/GM/SL 카드, 탭바)는 outline 이 `auto`(브라우저 기본)인데,
실제 렌더는 검은 테두리로 또렷하게 나온다 — 이쪽은 원래도 문제없었다.

**2. 핸들 정규화.** `minho` · `MINHO` · `@minho` 셋 다 로그인된다. 없는 핸들
(`@doesnotexist999`) 과 존재하는 핸들의 틀린 암호(`@minho` + 오타) 둘 다 여전히
같은 문구("핸들이나 암호가 맞지 않아요") — 정규화가 존재 여부를 새게 하지 않는다.

**3. autocomplete.** 핸들 input `autocomplete="username"`, 암호 input
`autocomplete="current-password"` 로 붙어 있다(DOM 실측).

**4. 세션 경계.** 설정 탭에서 로그아웃 → 다른 사람 로그인 → 홈 화면("내 포인트"
heading)으로 시작한다, 이전 탭(설정)에 남지 않는다. 깊은 화면 케이스도 확인—
발행 관리(2단계 깊이) 화면에서 `fetch` 를 가로채 이후 모든 `/api/*` 요청이
401(`UNAUTHENTICATED`)을 받게 강제했더니 즉시 로그인 화면으로 튕겼고, 재로그인
후 발행 관리 화면이 아니라 홈으로 들어갔다. 이체 초안이 비는지는 화면상 직접
보이는 게 없어 못 짚었지만(atom 값이라 DOM에 안 남음), 화면 전환은 확실하다.

**5. 발행 배지.** 360px 실측 — `발행 관리` 폭 65.5→69.5px, 높이 24→32px,
카드가 폭을 270.5px 로 스스로 줄여 자리를 내준다(flex). 겹침 없음(경계에서
맞닿기만 함), `금머니` 텍스트 `scrollWidth === clientWidth` 로 잘림 없음.
스크린샷으로도 여유 있어 보인다.

전체 스위트는 안 돌렸다 — 브라우저 실측만으로 다섯 개 다 확인됐다.

## 계약 구멍 재검증 (`e41a4c8`, `3cff61a`)

토큰을 `fetch` 로 가로채 실제 값을 얻은 뒤(`window.fetch` 를 한 번 감싸 첫
authed 요청의 `Authorization` 헤더를 캡처), `POST /api/transfers` 를 직접 쐈다.

- `amount: 0.5` → **400**(`SERVER`, "요청 형식 오류"). 이체 전후 잔액 확인 —
  솔포인트 87,500 그대로, 안 움직였다.
- `toId: u_minho`(요청자 자신) → **400**, 같은 코드.
- `/api/issues` 에도 `amount: 0.5` 를 쐈다 — 역시 400. 발행 경로에도 소수점
  검사가 똑같이 걸린다(자기 대상은 발행에서 원래 허용이라 따로 확인 안 함).

브라우저 화면으로는 볼 게 없다는 말이 맞았다 — 이 셋 다 화면 없이 `fetch` 만으로
끝났다.

## aria-live 재검증

**(1) 확정 화면 busy.** 개발자 패널 지연을 `느림`(2500ms+지터)으로 올리고 홀드→
릴리즈 직후 접근성 트리를 찍었다 — 이체는 `보내고 있어요`, 발행은
`발행하고 있어요` 가 각각 뜬다. 같은 순간 스크린샷에는 그 문구가 **안 보인다**
(`VisuallyHidden` 이 실제로 시각적으로 숨긴다, 버튼만 비활성화된 채 보임).
완료되면 두 경우 다 `role="status"` 로 "보냈어요"/"발행했어요" 로 바뀌고
busy 문구는 사라진다 — 남아있지 않는다.

**(2) 홈 지갑 조회 실패.** `window.fetch` 를 계속 501 을 주도록 감싸고
(`forceFailure` 는 1회성이라 기본 `retry:1` 에 걸려 재시도가 조용히 성공해
버리므로, 지속적으로 실패하는 쪽으로 바꿔 확인했다) `staleTime`(5초)이 지난
뒤 탭을 전환해 재조회를 강제했다 — `role="alert"` 로 "지갑을 불러오지
못했어요" 가 뜨고, **그 아래 이전 잔액 목록이 그대로 남아 있다**(TanStack
Query 가 실패한 배경 재조회에서도 이전 `data` 를 지우지 않는다 — 화면이
비지는 않지만, 실패와 마지막으로 성공한 값이 한 화면에 같이 있다는 뜻이다.
결함으로 보진 않는다, 빈 화면보다 낫다).
`fetch` 를 복구하고 다음 성공 조회에서 alert 가 사라지는 것도 확인했다 —
처음엔 1초만 기다리고 봐서 안 사라진 걸로 착각했는데, 그 사이 지연을
`느림` 으로 올려둔 걸 깜빡했다. 3초 더 기다리니 정상적으로 사라졌다(내
쪽 착오, 결함 아님).
**떠드는지**는 코드로도 확인했다 — `status` 는 `isPending`(데이터가 아예
없는 최초 로딩)에서만 뜨고, 배경 재조회(성공이든 실패든)에는 안 뜬다.
위 실패 재현에서 alert 만 뜨고 "불러오는 중" 은 같이 안 뜬 것으로 실측도
일치한다. 매 탭 전환마다 스크린리더가 로딩을 읽지는 않는다.

## S9 포인트 만들기 QA (`d96d424`)

**1. 미리보기.** 배지 42×42, `border-radius: 4px`, 이름 `18px/500` — 홈 카드의
실제 배지·이름과 치수까지 그대로 같다(둘 다 실측). 색을 바꾸면 미리보기 배지
색도 같이 바뀐다. 다만 미리보기는 자기 테두리를 두른 상자 안에 있고, 실제
목록 행은 테두리 없이 이어 붙는다 — 프레이밍만 다르고 배지·글자 자체는 거짓말을
안 한다. **닮았다고 본다.**

**2. 색 6택 접근성.** 회색조 스크린샷으로 확인 — 선택된 것은 칠해진 사각형 +
흰 체크, 나머지는 빈 사각형에 테두리만 있어서 색 없이도 확실히 갈린다.
**그런데 화살표 키가 안 된다.** `role="radiogroup"`/`radio`/`aria-checked` 는
붙어 있지만 방향키(→)로 포커스가 안 움직였다 — 같은 라디오에 계속 머문다.
대신 여섯 개 라디오 **전부**가 개별 Tab 정지점이라(로빙 tabindex 가 아니다),
마지막 색까지 가려면 Tab 을 여섯 번 눌러야 한다. Tab 으로 옮긴 뒤 Enter/Space 로
선택은 된다 — 기능은 되지만 WAI-ARIA 라디오그룹 키보드 패턴(방향키 순회 + 그룹당
탭 정지점 하나)과는 다르다. **"이름만 붙인 셈"** 이라는 우려가 실제로 맞다.

**3. 360px 색 버튼 6개.** 실측 43×44px, 24px 최소치보다 넉넉하다. 겹침도 없다.

**4. 상한 input.** `inputMode="numeric"` 확인, `1,000,000` 자리표·"100만까지
발행할 수 있어요" 캡션 다 뜬다. 키패드 대신 일반 input 을 쓴 판단은 타당해
보인다 — 이 화면엔 이미 텍스트 input 이 둘(이름·기호) 있어서 상한만 커스텀
키패드로 바꾸면 오히려 입력 방식이 화면 안에서 갈린다.

**5. 기호 겹침.** `ON` 으로 시도 → 실패 화면 안 감, 기호 input 아래
"이미 쓰는 기호예요" 뜨고 이름·상한 값 그대로 남는다. `aria-invalid="true"` +
`aria-describedby` 로 에러 텍스트와 정확히 연결돼 있고, 에러 텍스트 자체가
`aria-live="polite"` 라 포커스가 어디 있든 스크린리더가 읽는다(제출 버튼에
포커스가 있다가 사라지는 걸 확인했는데도 announce 는 됐다). 다만 실패 후
포커스가 body 로 빠진다 — 시각 키보드 사용자는 에러가 났다는 것만 보고 어디를
고쳐야 하는지 포커스로는 안내받지 못한다. 작은 흠이다.

**6. 되돌릴 수 없다는 안내.** `caption` 토큰(12px/400, gray.600) 그대로다 —
흰 배경 대비는 7.73:1 로 안 읽히는 건 아니다. 다만 이 앱이 이미 갖고 있는
"검증 안내" 스타일(`verifyLabel`: 색+굵기를 함께 올린다, `system.ts` 주석에
"색만이 아니라 굵기도 함께 올린다" 라고 스스로 적어 뒀다)을 정작 이 문장엔
안 썼다. 되돌릴 수 없다는 말이 화면에서 가장 안 눈에 띄는 글자 크기·굵기로
있다는 게 이 앱의 다른 곳의 판단 기준과 어긋난다. **읽히긴 하지만, 이 앱
기준으로 이 정도로만 강조해도 되는 문장인지는 다시 볼 만하다.**

**7. 만든 뒤.** 실측 — 방금 만든 카드는 `opacity: 0.55` 인 상자 **하나**로
묶여 있고, 그 안의 "발행 관리" 배지도 (자기 자신은 `opacity:1` 이지만
부모가 0.55 라 렌더는) 같이 흐려진다. `금머니`(기존 발행 포인트)의
"발행 관리" 는 흐려지지 않는다 — 잔액이 있어서다. **문제는 이거다: 방금
만들어서 바로 발행하러 갈 확률이 제일 높은 카드가, 정확히 그 이유(잔액 0)
때문에 화면에서 가장 안 보이게 죽어 있다.** "보낼 잔액이 없어요" 문구도
방금 만든 사람에게는 사실이지만 엉뚱한 프레이밍이다 — 이 사람이 다음에
할 일은 "받기" 를 기다리는 게 아니라 "발행하기" 인데, 문구도 배지도 둘 다
그 방향을 가리키지 않는다. 카드 자체는 버튼이 아니다(0 잔액 카드는 눌러도
갈 곳이 없어서로 보인다) — `발행 관리` 배지만 별도 버튼으로 살아있다.

테스트가 이미 잡는다고 한 여섯 개(지우기 버튼·짧은 클릭·빈 폼 잠금·대문자
변환·겹침 시 값 유지·즉시 카드 등장)는 다시 안 봤다.

## 인증 경로 회귀 훑기

프론트 작업(S9 수정)을 기다리지 않고 지금 볼 수 있는 것 — 로그인→401 만료→
재로그인→로그아웃, 세션 경계 포함.

- 강제 401(fetch 가로채기)을 발행 관리 화면에서 걸었더니 즉시 로그인으로
  튕겼다. 그대로다.
- `@minho` 세션이 죽은 뒤 `@jisoo` 로 재로그인 — 홈 탭("내 포인트")으로
  시작하고 잔액 812,000 도 맞다. 세션 경계 그대로다.
- 로그아웃 클릭 직후 한 번 흰 화면이 떴다 — DOM 은 비어있지 않았고
  (`body.innerHTML` 2015자) 콘솔에 vite `connecting.../connected.` 가
  반복돼 있었다. 프론트 세션이 그 시점에 dev 서버를 재시작하고 있던 것으로
  보인다 — R1-5 와 같은 클래스, 재확인하니 깨끗한 로그인 화면(이전 사용자
  데이터 없음)으로 정상 정착했다. **결함 아님.**

인증 경로 자체에서 새로 걸리는 것 없다.

## W2/W3 재확인 (프론트, 미커밋)

**1. 색 6택 키보드 — BLOCKING, 통과.** 네이티브 `<input type=radio>` 로 확인.
→ 로 포커스 이동과 선택이 같이 간다(다음 라디오가 즉시 checked), ← 도 같다.
기호 input 에서 Tab 한 번 → 라디오그룹(체크된 것 하나), 한 번 더 → 상한
input — 정확히 두 번이다, 여섯 번 아니다. 실측 43×46px, 24px 최소치 여유
있다(전 43×44 에서 세로만 조금 늘었다). 회색조 스크린샷 — 칠한 사각형+흰
✓ vs 빈 테두리, 여전히 확실히 갈린다. 라벨은 네이티브 `<label>` 이라
스크린리더가 "파랑"…"청록" 을 그대로 읽는다(실측: `label.textContent`
가 이름과 일치).

**2. 기호 겹침 포커스 — 통과, 그런데 별도 결함 하나 발견.** `ON` 으로 시도
→ 실패 후 포커스가 정확히 기호 input 으로 간다(body 로 안 빠진다).
`aria-invalid="true"`/`aria-describedby`/에러 텍스트 `aria-live="polite"`
다 그대로. 이름·상한 값 유지도 그대로.
**그런데 이후 기호를 지우고 안 겹치는 값(`ZZ`)으로 바꿔도 "이미 쓰는
기호예요" 가 안 사라진다.** 버튼은 다시 활성화되고(형식은 유효하니까),
실제로 눌러서 만들어 보니 정상적으로 만들어졌다 — **로직은 안 깨졌고
메시지만 안 지워지는 표시 버그**다. 하지만 화면만 보면 "아직도 겹친다"
로 읽혀서 사용자가 헷갈릴 수 있다. focus 를 옮기는 요청과 별개로 걸린
것이라 새로 적는다.
announce 가 focus 이동에 묻히는지는 실제 스크린리더가 있어야 판단할 수
있는 것이라 — 이 환경(리눅스, 헤드리스 크롬)엔 스크린리더가 없어서
확인 못 했다. DOM 상태(invalid+describedby+live 가 focus 이동과 동시에
갖춰짐)까지만 확인했다.

**3. 방금 만든 포인트 카드 — BLOCKING, 통과.** 만들고 홈으로 가면 그 행
전체(배지+이름+부제+잔액+발행 관리)의 계산된 `opacity` 가 처음부터
끝까지 `1` 이다(6단계 조상 전부 실측) — 흐리지 않다. 문구도
"발행해서 채울 수 있어요" 로 바뀌었다. "발행 관리" 는 여전히 별도
버튼, 카드 자체는 여전히 버튼이 아니다(paragraph). 반대쪽(발행 권한
없는 사람의 잔액 0 카드) 은 시드에 그런 조합이 없어서(원장에 `amount: 0`
항목이 없음, grep 확인) **건너뛴다** — 코드는 `canIssue` 로 가른다고
하니 로직상 여전히 흐려질 것으로 보이지만 실측은 못 했다.

**4. 지갑 발행자 부제 — 통과.** `@jisoo` 로그인 — 온포인트 카드에
"온마트 발행" 부제가 이제 붙는다(전엔 없었다). `@minho` 는 그대로 —
온포인트 둘 다 부제, 금머니·솔포인트엔 없음. 대상 선택에서 "jisu" 검색
— **주의: 로그인한 사람이 jisoo/jisu 본인이면 자기 자신이 결과에서
빠지는 게 계약이라(`GET /api/users` 요청자 제외) 혼자만 남아서 처음엔
회귀로 착각했다.** minho 로 다시 검색하니 `@jisoo`/`@jisu` 둘 다 뜨고,
핸들이 `16px/500`(handleVerify) 로 강조돼 있다 — 정상이다.

부제가 배경이 되는지: 지금은 4개 카드 중 2개(온포인트 둘)에만 붙고
나머지 둘(금머니·솔포인트)엔 안 붙는다 — 전부 붙는 것과는 거리가 있어서
아직은 "항상 붙는" 상태가 아니다. 다만 계정마다 이 비율이 달라질 수
있으니 여러 계정을 계속 봐야 할 것 같다.

**BLOCKING 이었던 1·3 모두 통과.** 새 결함은 2번에서 찾은 겹침 메시지
표시 버그 하나다.

## stale 에러 수정 재검증

`ON` 으로 실패 → 기호를 `ZZ` 로 고침 — 확인했다. 에러 문구가 사라지고
그 자리가 원래 안내("좁은 자리에서 이름 대신 쓰는 표식이에요. 영문 두세
글자")로 돌아온다. `aria-invalid` 는 `null` 로 풀리고 `aria-describedby`
도 `error-text` 에서 `helper-text` 로 정확히 바뀐다. 요청받은 한 가지는
이걸로 끝.

NETWORK 강제 실패로 "결과를 알 수 없는 실패는 안 지운다" 쪽도 짚어보려
했는데, 시도 중 콘솔에 `CreatePoint.tsx` HMR 이 여러 번 찍혔다 — 프론트
세션이 그 파일을 그때 또 고치고 있던 것으로 보인다. 입력해 둔 이름·기호가
빈 값으로 리셋됐고 에러도 안 보였는데, 이게 실제 동작인지 HMR 리로드가
폼 상태를 날린 것인지 이 시도만으로는 못 갈랐다 — 요청받은 확인은 아니라서
더 파고들지 않았다. 필요하면 다시 요청해라.

**후속.** 프론트가 확인해 줬다 — 그 리셋은 관측 중 `CreatePoint.tsx` 를
되돌렸다 다시 올리며 생긴 HMR 리마운트로 `useState` 가 초기화된 것이고,
NETWORK 실패 자체는 별도 테스트로 통과했다(입력 유지 + `role="alert"`,
기호를 더 쳐도 안 지워짐 — `SYMBOL_TAKEN` 만 지운다). 실측 재확인은
요청받지 않았고 프론트도 "이제 파일을 안 건드린다"고 했으니 하지 않았다.

## Q4 — 상한 변경 (`f06e804`)

**「취소」라는 말이 실제로 있다.** 확정 버튼 위 안내가 "바꾼 뒤에는 **취소할
수 없어요**" 다 — 이체·창설의 "되돌릴 수 없어요"/"지울 수 없어요" 와 다르게
금지어를 직접 썼다. 원인이 무엇을 되돌리는 게 아니라 "이 조작 자체를 무를
수 없다" 는 뜻이라도, 이 저장소가 `CLAUDE.md`·`JOURNEY.md` 에서 명시적으로
피하기로 한 단어라 지적한다.

나머지는 통과.

- **홀드로 확정.** 마우스 down 1초→up 으로 정상 발동. 손동작이 이체·발행·
  창설과 같다
- **낮추려 할 때.** 이미 발행한 양(1,200,000)보다 낮은 값(500,000·700,000)을
  두 번 실측했다 — 값이 안 지워지고, 포커스가 정확히 그 input 으로 가고
  (`document.activeElement` 로 확인), `aria-invalid=true`+
  `aria-describedby` 로 "이미 발행한 양보다 낮아요" 가 붙고 `aria-live=
  polite` 로 announce 된다. 실패 화면으로 안 튕긴다. `fetch` 를 가로채 실제
  서버 응답도 봤다 — `422 CAP_BELOW_ISSUED`.
  (한 번은 body 로 포커스가 빠지고 값도 비는 걸 봤는데, 그 직후 프론트
  세션의 HMR 이 `App.tsx` 까지 흔들려 로그아웃까지 됐던 시점이라
  오염된 관측으로 보고 재현 안 되는 걸 확인한 뒤 버렸다)
- **같은 값이면 잠김.** 지금 상한(10,000,000)을 그대로 넣으면 버튼
  `disabled=true`.
- **보유자 영향 미리보기.** "가진 사람에게는 / 2,000만까지 늘어날 수
  있게 돼요" 가 뜬다. 다만 "약속을 깬다" 는 무게까지 담겼는지는 판단이
  갈릴 수 있다 — 사실만 전달하는 문장이라, 그 무게를 문장이 아니라 위치·
  단독 문단 배치로 표현하려 한 것 같다. 조율 판단으로 남겨 둔다.
- `inputMode="numeric"` 확인.

## Q5 — 내역이 이체만이 아니게 됐다

이체 줄과 상한 변경 줄을 한 화면에서 실측 — 눌리는 것(이체, 이름+금액
3분할)과 안 눌리는 것(상한 변경, 2줄 문장+시각, 우측 금액 컬럼 없음)이
레이아웃 자체가 달라서 섞여도 눈이 줄을 안 잃는다. 상한 변경 줄은 실측
`<p>`(`role` 없음, `cursor: auto`) — 버튼 아님, 접근성 트리에도 안 잡힌다.

보이는 사람: `@jisu`(금머니 보유, 비발행자) 로그인 — 상한 변경 줄 그대로
보인다. `@jisoo`(금머니 한 번도 안 가짐) 로그인 — 내역 자체가 비어 있다
("아직 보낸 것이 없어요"). 요청한 대로 갈린다.

모프: 이체 줄 이름("이서연") x=20 목록에서, 상세에서도 x=20 — 그대로
고정이다(y 만 66→68). 상한 변경 줄은 상세가 없어 모프 대상이 아니다.

## Q6 — 확정 화면의 동명 포인트 분별

솔카페가 발행한 온포인트(잔액 12,000)를 골라 대상 선택→금액→확정까지
갔다 — 대상 선택 헤더, 금액 화면, 확정 화면 **셋 다** "온포인트 / 솔카페
발행" 을 띄운다. 확정 화면의 "지금 잔액" 도 12,000(온마트 쪽 3,240,000
이 아니다)으로 정확한 쪽을 가리킨다. 회귀 없음.


## W5 — 은행 페이지 (6c8e363, fa34bff, 29736ca)

### 1. 홈 카드 진입점 — 통과

카드 본문(`RowButton`)과 "자세히" 버튼은 형제 요소로, 실측 경계가 딱
붙어 있을 뿐(x=284.875) 겹치지 않는다. 카드를 누르면 보내기(`PickRecipient`)
로, "자세히" 를 누르면 은행 페이지로 — 교차 오발화 없음. "자세히" 버튼
실측 55.125×32px, WCAG 2.5.8 최소 24px 여유 있음. 발행자 아닌 포인트
(온마트 발행 온포인트) 은행 페이지에 "보내기" 만 뜨고 상한 폼은 안 뜬다
— `canIssue` 분기 정상.

### 2. 상한 변경 폼 — **실결함 발견**

**확정 버튼이 화면 하단 내비게이션에 가려진다.** 재현 순서: 금머니(내
발행) 은행 페이지 진입 → 유효한 새 상한 값 입력 → "가진 사람에게는" 미리
보기 박스가 나타나면서 그 아래 있던 "꾹 눌러서 바꾸기" 버튼이 **아래로
밀려 하단 내비게이션(홈/내역/설정) 뒤에 67px 겹치고, 26px 은 뷰포트
밖으로 나간다.** 입력 전에는 버튼이 화면 안에 온전히 보였다(겹침 0) —
입력해서 미리보기 박스가 생기는 순간 밀려난다. 스크롤하면 닿기는 하지만
**아무 시각적 신호(그림자·화살표 등) 없이** 사라지므로, 되돌릴 수 없는
조작의 확정 버튼을 사용자가 존재하는지도 모르고 지나칠 수 있다. 실측
스크린샷: 입력 전/후 두 장, `getBoundingClientRect` 좌표 첨부 가능.
포인트 만들기 화면의 "꾹 눌러서 만들기" 버튼도 좌표상(y 중심 738, 내비
상단 739) 같은 문제로 보이나 이쪽은 스크린샷까지는 못 남겼다 — 같은
패턴(하단에 쌓이는 폼 + 확정 버튼)이라 같이 봐 달라.

가상 키보드 자체(virtual keyboard occlusion)는 이 자동화로 재현할 수
없다 — `index.html` 에 `interactive-widget` 지정이 없어 최신 Chrome
기본값(`resizes-visual`)을 따르는데, 이는 레이아웃 뷰포트를 그대로 두고
시각 뷰포트만 줄이는 방식이라 CDP 뷰포트 리사이즈로 흉내낼 수 없다. 다만
구조적으로는: `position: fixed`/`sticky` 가 코드베이스 어디에도 없고
(`grep` 확인), `Body` 가 `overflowY: auto` 인 정상 스크롤 컨테이너라 —
포커스 시 브라우저 네이티브 스크롤-into-view 가 동작할 토대는 갖춰져
있다. **키보드로 인한 추가 가림은 실기기가 필요하지만, 방금 찾은 결함은
키보드 없이도, 스크롤 한 번 안 해도 재현된다 — 실기기 없이 확정.**

「취소」 단어는 아직 안 고쳐졌다 — `ko.ts:83` `cap.irreversible`:
"바꾼 뒤에는 **취소**할 수 없어요". 지난 라운드에 지적해 전달됐다고
들었는데 이 세 커밋에는 반영이 안 된 것 같다.

### 3. 아직 안 쓴 포인트 표시 — 통과

홈 카드(`home.neverSpent`): "아직 써 보지 않은 포인트예요" — 관찰형.
확정 화면(`confirm.firstUse`): "이 포인트를 처음 써요" — 1인칭 행동형.
문구는 다르지만 어조는 같다 — 느낌표·경고 색 없이 회색 캡션/일반 텍스트로
뜬다. 확정 화면은 "만든 사람" 핸들을 같이 보여줘 동명 포인트 판단 근거를
함께 준다(여정 10 취지대로). 처음 쓰는 사람을 긴장시키는 문구는 아니다.

### Q3 — orange 팔레트 포커스 링, 재검증 완료 (닫힘)

기존 시드 데이터에는 orange 팔레트를 쓰는 포인트가 없어 실측 자리가
없었다. 이번에 "포인트 만들기" 로 orange 색 포인트를 직접 만들어(내가
발행자이므로 "발행하기" 버튼이 그 포인트의 `colorPalette` 를 물려받음)
Tab 키로 실제 `:focus-visible` 을 띄워 측정했다.

- 라이트: 아웃라인 `rgb(234,88,12)` / 배경 흰색 → 계산 대비 **3.56:1**
  (`contrast.test.ts` 값과 일치)
- 다크: 아웃라인 `rgb(251,146,60)` / 배경 `rgb(9,9,11)` → 계산 대비
  **약 8.8:1**

테스트가 계산하는 값과 브라우저가 실제로 그리는 값이 갈리지 않는다 —
토큰도 렌더링도 3:1 을 넘는다. 라이트 모드는 3:1 턱걸이라 여유는 크지
않지만 결함은 아니다. **더 볼 것 없음, 닫는다.**

(참고: 이 검증용 "테스트포인트"/orange 포인트는 Mock 이 브라우저 탭별
상태라 다른 세션에 안 남는다. 정리 불필요.)

## W6 — 회원 자격 (f630e7a · a430f07 · a23f15b · f8dff9f · 8587fe3, + 서버 ec678ed·9c9fa66)

중간에 dev 서버가 죽었다(`ps aux` 에 vite 프로세스 없음, `curl localhost:5173`
연결 거부 — HMR 재연결이 아니라 프로세스 자체가 사라졌다). 2분 넘게 기다려도
안 돌아와서 브라우저 실측 일부는 코드 확인으로 대신했다 — 아래 항목마다
실측/코드확인을 구분해 적는다. 서버가 돌아오면 1번의 직접 fetch 실측을
마저 하면 된다.

### 1. 비공개 은행 · 비회원 접근 — **부분 결함 발견 (코드 확인, 실측 중단)**

`GET /api/point-types/:id` 는 정상이다 — Mock `findPointType` 이 `reachable()`
(회원 또는 초대됨) 을 통과 못 하면 존재하는 id 든 없는 id 든 **같은 호출**
(`fail('POINT_TYPE_NOT_FOUND')`, 404) 로 떨어진다. 코드로는 구별 불가능,
계약대로다. `POST /api/transfers`(`commitTransfer` 의 `usable()` 체크) 와
`POST /api/point-types/:id/invites`(`createInvite` 의 `reachable()` 체크) 도
같은 정규화가 돼 있다.

**그런데 `PATCH /api/point-types/:id/cap` 과 `POST /api/issues` 는 안 됐다.**
`changeCap`/`commitIssue` 둘 다 `requirePointType`(존재만 검사, 도달 가능성은
안 봄) 뒤에 바로 `issuerId !== meId` 를 검사해 `NOT_ISSUER`(403) 를 던진다.
비회원·비초대 사용자가 비공개 은행의 id 를 (다른 경로로) 알기만 하면
`PATCH .../cap` 이나 `POST /issues` 를 던져서 **404 대신 403 을 받아 그
은행이 존재한다는 걸 알아낼 수 있다** — `docs/API.md` 가 명시적으로 경계한
바로 그 구멍이다.

같은 걸 실제 백엔드(`server/`, Kotlin) 커밋 `ec678ed` 가 이미 고쳐 놨다 —
"PATCH .../cap 과 POST /issues 는 닿을 수 없는 은행에 NOT_ISSUER 로 답하고
있었고... 셋 다 없는 포인트와 같은 404 로 맞췄다" 라고 커밋 메시지에 직접
적혀 있다. **Mock 은 그 수정 중 transfers/invites 만 따라갔고 cap/issues 는
안 따라갔다.** `web/` 은 Mock 만 보고 만들어지니 이 구멍은 화면 코드가 직접
만든 게 아니라 Mock 이 뒤처진 것이다 — 그래도 지금 이 Mock 으로 돌아가는
화면·테스트가 이 구멍을 그대로 물려받는다.

실제 UI 흐름만으로는 이 경로에 못 닿는다(내비게이션이 URL 이 아니라
클라이언트 상태 스택이라, GET 이 성공해야만 그 id 를 아는 화면이 만들어진다)
— 그래서 실사용자가 이 문제로 직접 다치지는 않는다. 다만 직접 호출(계약
구멍류 점검)로는 확인 가능하고, 코드로는 이미 확정이다. 서버가 죽어서
`@jisu`(동아리회비 비회원·비초대) 로 직접 fetch 비교까지는 못 했다 — 서버
살아나면 이어서 하면 된다.

### 2. 창설 공개/비공개 선택 — 통과

두 라디오 다 `checked=false` 로 시작(색은 파랑이 기본 선택인 것과 다름).
이름·기호·상한을 다 채워도 공개/비공개를 안 고르면 "꾹 눌러서 만들기"
버튼이 계속 `disabled` — 건너뛸 수 없다. 카드 자체가 "누구나 받고 보낼 수
있어요" / "초대받은 사람끼리만 주고받아요" 로 결과를 바로 말하고, 고르면
그 카드가 테두리+채워진 라디오로 뚜렷이 구분된다. 바로 아래 "만든 뒤에는
바꿀 수 없어요" 로 영구성도 짚는다. 실측(스크린샷 첨부 가능).

### 3. 나가기·내보내기 뒤 잔액 — **통과 + 갈리는 자리 하나**

**Members 화면 문구가 정확히 요청한 톤이다.** "나가도 잔액은 그대로 남지만
쓸 수 없어요" — 겁주는 말도 조용히 감추는 것도 아니고, 사실만 전달한다
(`bank.leaveKeeps`). 코드로 사슬을 확인: `usable()` 이 회원 아니면 `false`
→ `sendable: 0` (잔액 `amount` 는 그대로) → `PointCard` 의 `locked` 캡션
("home.locked") 이 홈 카드에 뜬다. 은행장은 "나가기" 버튼 자체가 없다
(disabled 가 아니라 렌더 안 함 — `pointType.canIssue ? null : ...`, 코드
주석: "누를 수 없는 버튼이 아니라 버튼이 없어야 한다"). 회원 목록의
"내보내기" 도 은행장 행에는 안 뜬다. 홀드 제스처 없는 단일 클릭.

**그런데 나간 뒤 그 은행 페이지를 다시 볼 수 있는지가 세 군데가 서로
다르게 가정하고 있다.**
- 실제 백엔드(Kotlin, `ec678ed`): "회원이 아닌데 잔액만 남은 사람도 은행
  페이지를 본다" — 도달 가능성을 회원 행이 아니라 **잔액**으로 판정하도록
  일부러 고쳤다고 커밋 메시지에 적혀 있다.
- Mock(`web/src/mocks/ledger.ts` `reachable()`): 회원이거나 초대됐을 때만
  `true` — 잔액은 안 본다. 그래서 나간 뒤 다시 그 은행 id 를 조회하면
  Mock 에서는 404 가 난다.
- 프론트(`Members.tsx` `onLeft` prop 주석): "나가면 그 은행에 닿지 않는다.
  화면에 남겨 두면 다음 조회가 404 를 받는다" — 그래서 나가기 성공하면
  `leaveFlowAtom` 이 곧장 홈으로 돌려보낸다. 그 은행 페이지로 다시 들어가는
  길이 화면에 아예 없다.

지금 UI 로는 이 셋 중 어느 쪽이 맞는지 실제로 시험할 방법이 없다(나가는
순간 화면이 스스로 치워 버려서) — 그래서 사용자가 지금 당장 다치는 결함은
아니다. 다만 백엔드가 일부러 만든 "잔액이 있으면 계속 보인다" 는 설계가
프론트에는 아예 없다 — 발행자 핸들·만든 날·상한처럼 "왜 못 쓰는지" 를
설명하는 화면이 나갈 때 사라지는 셈이다. **셋 중 어느 게 맞는지는 계약
판단이라 조율에 넘긴다.** (실측은 이 항목 시험 도중 dev 서버가 죽어서
"나가기" 클릭 직후 화면이 이상하게 겹쳐 보였는데, 그건 재현 확인 못 해서
버렸다 — 서버 다운과 겹친 시점이라 HMR/서버 재시작 오염으로 본다.)

### 4. 초대 수락 타깃 크기·오터치 — 통과 + 전제 하나 다르게 확인됨

**초대 수락(`Join`) 은 홀드가 아니라 단일 클릭이다.** 요청 문구는 "수락은
되돌릴 수 없고" 였는데, `Bank.tsx` 의 `Join` 컴포넌트 주석은 정반대로
말한다: "가입은 되돌릴 수 있다 — 나가면 된다. 되돌릴 수 없는 것은 그 안에서
주고받은 것이지 소속이 아니다. 그래서 꾹 누르게 만들지 않는다." 실측으로도
확인: 클릭 한 번에 즉시 가입되고(aria-live "들어왔어요"), 홀드 UI 없음.
**어느 쪽이 맞는 전제인지는 판정하지 않고 그대로 알린다** — 프론트 코드의
근거는 있지만 요청과 다르다.

전제가 갈리는 것과 별개로 타깃 자체는 안전하다. 실측: 320×48px, 화면
폭 대부분을 차지하는 단독 버튼, 위아래 다른 클릭 가능 요소와 충분히
떨어져 있다(뒤로 버튼과 세로로 멀리 떨어짐, 하단 내비 없음 — 이 화면엔
탭바가 안 뜬다). 거절 버튼 없음 확인 — 무시하고 뒤로 가면 그만이고 초대는
그대로 남는다(계약대로).

홈 화면의 초대 행("테스트가입 @minho 님의 초대예요")도 실측 73px 높이 전체
폭 단독 버튼, 아래 카드와 오터치 여지 없음 — 카드 목록의 다른 행과 같은
패턴이라 구분 실수 안 남.

### 참고 — 실기기 vs 자동화

이번 넷은 전부 브라우저 자동화로 갈렸다. 실기기가 필요해 보였던 자리는
없었다 — 홀드 제스처 유무 자체가 코드/화면에서 바로 읽히고, 타깃 크기는
좌표 실측으로 충분하고, 존재 은닉은 순수 HTTP 계약 문제다.

## W7 — 실서버 재검증 (c9a721f 판정 이후, localhost:8080)

dev 서버가 죽어 있던 것을 `npm run dev:server`(`VITE_API_ORIGIN=http://localhost:8080`)
로 다시 띄우고, 실서버에 직접 fetch/curl 로도 붙여서 확인했다. 계정:
`@onmart`/`@jisoo`/`@nara`/`@mose`(암호 `point`). `@nara` 가 비공개 은행
「동아리비」(id `61a40de3-...`) 에서 나간 사람, `@mose` 는 그 은행과 아예
관계없는 사람, `@jisoo` 는 지금 회원(잔액 1,300, `sendable` 도 1,300).

### 가장 급한 것 — **`NOT_MEMBER` 미구현, 실서버에서 돈이 실제로 움직인다**

계약이 요구한 대로 아직 안 됐다. `@nara`(그 은행 `sendable: 0`) 로
`POST /api/transfers` 를 직접 보냈더니 **`201`** 로 성사됐고, 곧바로
`GET /api/wallet` 로 확인하니 잔액이 99,900 → **99,800** 으로 실제 줄어
있었다(상대 `@onmart` 로 100 이동, transfer id
`478914c8-8aca-49ce-94f1-25343b0dfa24`). 검증하려고 보낸 요청인데 실제
이체가 나 버렸다 — 확정한다는 판단이 아니라 계약이 말한 사고가 재현된
것이다. **개발 DB 에 실제 상태 변화가 남았다.** 되돌리는 조작을 앱이
제공하지 않으므로 그대로 뒀다 — 정리가 필요하면 알려 달라.

아직 실패하지 않으므로 "고쳐지면 화면 문구가 「잔액이 부족해요」로 잘못
떨어지는지" 는 지금은 확인할 것이 없다 — 실패 자체가 안 난다. 이 항목은
`NOT_MEMBER` 가 실제로 던져지기 시작하면 다시 봐야 한다.

### `GET /api/point-types/:id/members` — 존재 은닉이 아니라 그냥 고장

`@mose`(생판 남) 로 부르면 프레임워크 기본 404 본문(`{"timestamp",
"status","error","path"}`, `code`·`outcome` 없음)이 그대로 샌다 — 그런데
**`@jisoo`(지금 회원, `sendable` 정상)로 불러도 똑같이 그 본문이 나왔다.**
가려야 할 사람한테만 새는 게 아니라 **회원한테도 안 된다** — 이 경로 자체가
아직 실서버에 안 붙어 있는 걸로 보인다. 진짜로 매핑이 없는 경로
(`/api/totally-not-a-real-route`) 를 불러도 같은 본문이 나오는 걸로 봐서
스프링 기본 404 핸들러가 그대로 노출된 상태다 — 계약이 경계한 바로 그
증상이다. `docs/FIELD.md` W6 에 적었던 "구현 중에 제일 자주 난다" 는 말
그대로다.

### 1 (존재 은닉) — 실서버는 이미 고쳐져 있었다

`@mose` 로 `PATCH .../cap`, `POST /issues` 둘 다 시도 — 존재하는 비공개
은행 id 든(`61a40de3-...`) 완전히 없는 id 든(`00000000-...`) **똑같이**
`404 {"code":"POINT_TYPE_NOT_FOUND","outcome":"none","message":"포인트
없음"}`. 지난번 Mock 에서 찾은 구멍(`NOT_ISSUER` 로 새는 것)이 실서버에는
없다 — Mock 만 뒤처져 있던 게 맞았다. `POST /transfers` 도 같은 사람
기준으로 정상(존재 안 새는 404).

### `memberCount` — 계약대로

비공개(`동아리비`): `2`. 공개 은행들(`온포인트`·`솔카페`): `null`.
`Bank.tsx` 는 `=== null` 로 갈라 공개 은행에는 그 줄 자체를 안 그린다.

### 3 (나간 뒤 재도달성) — **닫힘, 잘 됐다**

`@nara` 로 은행 페이지 실측: 이름·발행자·만든 날·상한·유통량·**회원
2명**(memberCount) 은 보이고, "회원 보기"·"초대하기"·"보내기" 는 전부 안
보인다(비활성 아니라 렌더 자체가 없음). 그리고 세 줄:

> 이 은행의 회원이 아니에요
> 그래서 이 잔액을 지금 보낼 수 없어요
> 없어진 것은 아니에요. 다시 초대받으면 그대로 써요

겁주는 단어 없음(잃음·손해·경고 색 없음), "지금 못 쓰는 것" 프레이밍
정확히 맞음, 무채색 패널에 조용히 앉아 있다(스크린샷 첨부 가능). 요청한
기준 그대로다 — 물으러 갈 곳이 있고, 다시 될 수 있다는 것도 같이 말한다.

작은 것 하나: `Members.tsx` 의 "나가기" 버튼은 여전히 `pointType.canIssue`
만 보고 뜬다(실제 회원인지는 안 봄) — 그런데 진입 버튼(Bank 의 "회원
보기") 자체가 비회원에게 안 뜨니 지금은 못 닿는다. 나중에 다른 길로
Members 화면에 닿게 되면 다시 볼 자리로 적어 둔다.

### 4 — 재확인 필요 없음

지난번 판정("홀드 없는 단일 클릭이 맞다")과 코드가 안 바뀌었다.

### 실기기 vs 자동화

전부 HTTP 직접 호출과 화면 실측으로 갈렸다. 실기기가 필요한 자리 없었다.

## W8 — QA.md 첫 라운드 (요청서 없음)

`docs/QA.md` 대로 요청서 없이 한 바퀴 돌았다. 본 것: 실서버(`:8080`,
`npm run dev:server`)만. Mock 은 이번 라운드 안 열었다. web/ 은 계속
바뀌는 중이라 커밋을 못 하나로 못 박는다 — 히스토리 유출·초대 서브시스템
부재는 **서버 쪽 상태**라 프론트 커밋과 무관하고, 위계·hover 는 `818c912`
~`af9499b` 사이에서 봤다. 확인 시각: 2026-08-19 12:2x~13:0x.

### 아무도 안 물어본 것 — 위에서부터

**1. `GET /api/history` 가 비공개 은행의 상한변경 이벤트를 무관한 사람에게도 준다.**

- 무엇을 했나 — `@mose`(그 어떤 은행과도 무관, `GET /point-types/:id` 가
  「동아리비」에 대해 `404 POINT_TYPE_NOT_FOUND`, 잔액 전부 빈 배열)로
  `GET /api/history?limit=30` 을 직접 불렀다.
- 무엇이 보였나 — mose 의 유일한 내역 줄이 **동아리비의 상한변경**이었다:
  `{"type":"capChange","capChange":{"pointTypeId":"61a40de3-...",
  "previousCap":1000000,"issueCap":9000000,...}}`. 같은 항목을 그 은행의
  실제 회원인 `@jisoo` 도 받는다 — 그건 정당하다. 문제는 **mose 도 똑같이
  받는다는 것**이다.
- 무엇을 기대했나 — mose 는 그 은행 id 조차 모르는 사람이어야 한다
  (`GET /point-types/:id` 가 이미 그렇게 답한다). 내역이 그 규칙을 다시
  깬다.
- 왜 문제인가 — `docs/API.md` 「회원 자격」이 공들여 지킨 존재 은닉이 다른
  경로로 그대로 샌다. 화면에는 아직 안 나온다(웹이 pointType 이름을
  못 찾아 조용히 걸러 낼 수도 있다 — 못 봤다) 지만, **API 응답 자체에
  pointTypeId·이전상한·새상한·시각이 실려 나간다.** SDK 를 외부에 열
  계획이면 이건 화면 문제가 아니라 서버 문제다.
- 심각도 — 1급(존재를 감추기로 한 것이 새어 나간다. 사용자가 아니라
  계약이 거짓말하게 된다).
- 실기기 필요? — 아니다. 순수 HTTP.

**2. 초대 서브시스템이 실서버에 아예 없다 — `UNKNOWN_ENDPOINT`.**

- 무엇을 했나 — `@onmart` 로 `POST /point-types/:id/invites`, 아무 계정으로
  `GET /api/invites` 를 직접 불렀다.
- 무엇이 보였나 — 둘 다 `404 {"code":"UNKNOWN_ENDPOINT","outcome":"none",
  "message":"없는 경로"}`. 본문 모양은 맞다(예외 없음 원칙은 지켜짐) — 그냥
  이 기능이 서버에 없다.
- 왜 문제인가 — 두 갈래로 퍼진다.
  - `Home.tsx` 의 빈 상태 조건이 `data?.balances.length === 0 &&
    invites.data?.length === 0` 다. `invites` 쿼리가 항상 에러라
    `invites.data` 는 영원히 `undefined`, `undefined === 0` 은 거짓이라
    **잔액이 진짜 0 인 계정(`@mose`)이 홈을 열어도 「아직 받은 포인트가
    없어요」가 절대 안 뜬다.** 「포인트 만들기」버튼만 덩그러니 있다 —
    QA.md 가 경계한 바로 그 "없는 것과 못 불러온 것이 같아 보인다" 류지만,
    이번엔 반대 방향이다: **있어야 할 빈-상태 문구가 무관한 쿼리 실패
    때문에 조용히 안 뜬다.**
  - `Bank.tsx` 의 `invite = invites.data?.find(...)` 도 항상 `undefined`
    다. 초대 수락(`Join`) 은 그래서 **실서버에서는 어떤 경로로도 도달
    불가능** 하다 — 지난 라운드에 Mock 으로 확인했던 「초대 수락 타깃
    크기·홀드 없음」검증은 Mock 에서만 유효하고 실서버에서는 재현이
    안 된다.
- 왜 문제인가(두 번째 층) — 빈 상태가 무관한 쿼리 하나의 성패에 인질로
  잡혀 있다. 초대 기능이 나중에 서버에 붙어도, `invites` 쿼리가 다른
  이유로(네트워크 지연 등) 실패하는 순간 같은 일이 재발한다.
- 심각도 — 3급(답을 얻으러 왔는데 답이 없다 — 처음 온 사람이 빈 화면 앞에
  「이게 로딩 중인가, 원래 이런가」를 스스로 판단해야 한다) + 견고성 결함.
- 실기기 필요? — 아니다.

**3. 은행 페이지 위계 — 여전히(사실 더) 잡음이다.**

- 무엇을 했나 — `@onmart`(발행자) 로 「동아리비」은행 페이지를 열고 본문
  텍스트 스타일(글자크기/굵기/색) 종류를 셌다.
- 무엇이 보였나 — **9종**(전에 관측했던 7보다 늘었다 — `memberCount`·
  상한폼 sticky 푸터가 그 사이 더해졌다). 특히 `보내기`·`발행하기`·
  `초대하기`·`꾹 눌러서 바꾸기` 넷이 **전부 16px/500 로 같다** — 되돌릴 수
  없는 것(보내기·발행하기·상한변경 확정)과 가벼운 관리 기능(초대하기)이
  크기로 안 갈린다.
- 무엇을 기대했나 — QA.md: "다섯을 넘으면 잡음", "되돌릴 수 없는 것이
  크기로도 달라 보이는가".
- 왜 문제인가 — 발행자 역할의 질문("지금 얼마나 더 찍을 수 있나, 상한을
  올리면 무슨 일이 나나")에는 답이 있다(남은 여력·미리보기 문구). 다만
  그 답을 찾기까지 같은 무게의 버튼 넷을 훑어야 한다 — 위계가 아니라
  나열이다.
- 심각도 — 5급(잡음, 거짓말은 아니다).
- 실기기 필요? — 아니다.

### 확인된 것 — 긍정

- `_hover` 가 이제 코드에 있다(`HoldButton.tsx`, `Screen.tsx` 의
  `RowButton` 등). CDP 합성 마우스 이동으로는 재현이 안 됐다(전에도 겪은
  한계 — 프로그램적 hover 는 진짜 OS 이벤트만큼 안 믿음직하다) — 코드
  근거로만 남긴다. 실기기에서 다시 볼 값이 있는 항목.
- 「발행에 나가 뜨고 남에게 보낸 것도 나로 보인다」— 주 경로는 고쳐졌다.
  `@onmart` 내역에서 이체는 받는 사람 이름(`김지수`)이 굵게, 발행은
  "나 발행 · 은행명" 으로 레이아웃 자체가 다르다. 다만 `toOf()` 코드가
  이름 검색 실패 시 `t('history.me')` 로 **되돌아간다** — 라이브로
  재현은 못 했지만 코드에 그 경로가 남아 있다(`History.tsx`).
- `Loadable`/스켈레톤이 6개 화면(Home·History·Bank·Members·
  PickRecipient·HistoryDetail)에 반영돼 있다.
- 지난 라운드의 핵심 발견(나간 뒤 재도달성)은 이번에도 그대로 잘 됐다 —
  재확인 안 함(코드 안 바뀜).

### 이번에 안 본 것

- Mock(`localhost:5173` 기본) 전혀 안 열었다 — 전부 실서버.
- 창설(CreatePoint) 색 선택 미리보기(문제 1) 재확인 안 함.
- 멱등성 키가 화면에 적히는 자리(문제 7) 특정 못 함.
- 느린 네트워크(Slow 3G) 시뮬레이션 못 함 — 시간 안에 못 함.
- 뒤로가기 연타, 같은 버튼 빠르게 두 번 누르기를 제대로 못 해봤다(홀드
  버튼 하나 테스트하다 HMR 로 끊김).
- 역할 여섯 중 실제로 써 본 것은 「나온 사람」(지난 라운드)·「발행자」
  (이번, 얕게) 뿐. 「동명이인」·「낯선 은행 판단」·「처음 받은 사람」·
  「처음 만드는 사람」은 이번에도 안 씀 — 다음 라운드 우선순위.
- 긴 문자열·0/1/최댓값 입력 경계값을 하나도 안 넣어 봤다.

## W9 — QA.md v2 (뼈대 겹쳐 재기 · 엄지 자리 · MOTION.md)

실서버(`:8080`)만 봤다. web/ 이 계속 편집되는 중이라 커밋 하나로 못 박지
못한다 — `51342a2`~`dae4ca7` 사이, 확인 시각 2026-08-19 13:0x~13:3x.
아무도 안 물어본 것부터.

### 1. `symbol`→`emoji` 계약 이전이 프론트에 전혀 안 들어감 — 포인트 만들기가 실서버에서 전부 실패한다

- 무엇을 했나 — 계약(`781d778`)이 `PointType.symbol` 을 `emoji`+`description`
  으로 바꿨다. `@onmart` 로 `POST /point-types` 를 `symbol` 필드(예전
  프론트 방식)와 `emoji` 필드로 각각 직접 보냈다. 그리고 실제 화면(포인트
  만들기)에서도 홀드로 확정해봤다.
- 무엇이 보였나 — `symbol` 필드 → `400 MALFORMED_REQUEST`. `emoji` 필드 →
  `201` 성공. 화면은 여전히 "기호" 텍스트 입력(영문 2-3자,
  `/^[A-Za-z]{2,3}$/`)을 받아 `symbol` 로 보낸다 —
  `CreatePoint.tsx`·`endpoints.ts:33`·`contract.ts:24` 전부 아직 `symbol`.
  실제 화면에서 「꾹 눌러서 만들기」를 하면 `alert: "요청을 보내지
  못했어요"` 가 뜬다.
- 무엇을 기대했나 — 성공하거나, 실패해도 정확한 사유가 뜬다.
- 왜 문제인가 — 두 겹이다.
  (a) 「처음 만드는 사람」역할 전원이 실서버에서 포인트를 못 만든다 —
  기능이 죽어 있다.
  (b) 더 나쁜 건 문구다. `ko.ts` 의 `MALFORMED_REQUEST` 주석: **"화면에서는
  도달하지 않는다. 방어적으로 그리는 자리를 위해 둔다."** 절대 안 일어날
  일로 짠 자리인데 지금 매번 일어난다. "요청을 보내지 못했어요" 는
  네트워크 실패처럼 읽힌다(안 나간 것 같다) — 실제로는 서버가 확실하게
  거절했다(400, `outcome: none`). 사용자는 "다시 보내면 되나" 라고 믿게
  되는데 같은 값으로는 계속 같은 이유로 거절된다. **서버가 명확하게 답한
  것을 화면이 애매하게 옮긴다.**
- 부수 피해 — `PointBadge` 가 `symbol` prop 을 받는데 실서버 응답엔 그
  필드가 없다(`emoji` 만 있음) → `undefined` → 배지가 전부 **빈 사각형**
  으로 뜬다(Home 카드·은행 페이지·PickRecipient 전부). 여정 1 의
  "회색조에서도 기호로 갈린다" 가 무력화됐다. 스크린샷 첨부 가능(동아리비
  배지, 색만 있고 안이 비어 있음).
- 심각도 — 1급에 가깝다(실패를 실패로 정확히 안 말한다) + 3급(기능이
  죽음) + 접근성(배지 무력화, 여정 1 위반).
- 실기기 필요? — 아니다. 순수 HTTP + 화면.
- 참고 — 실서버에 남은 부수 데이터: `emoji` 방식으로 직접 만든 공개
  포인트 "기호테스트"(id `fb5993f9-...`, 무해). "기호실패테스트"는 생성이
  실패해서 안 남았다.

### 2. 뼈대를 겹쳐 재니 둘 다(HistoryDetail·Bank) 완전히 안 맞는다 — 정량 확인

- 무엇을 했나 — QA.md 새 방법대로: fetch 를 3초 지연시키고, 뼈대 상태
  스크린샷 → 내용 로드 후 같은 자리 스크린샷 → 좌표 비교.
- 무엇이 보였나 —
  - **HistoryDetail** — 뼈대는 2열×4줄 균일 막대. 실제 내용은 1열 세로
    쌓기: 이름(27px 굵게, top 68) · 핸들 · 포인트명 · **금액(29px, 화면
    제일 큰 글자, top 156)** · 시각. 뼈대의 어떤 막대도 이름·금액 위치와
    안 겹친다. (조율이 이미 이걸로 QA.md 를 고쳤다 — 재현 확인만 했다.)
  - **Bank(발행자뷰)** — 뼈대는 2열×5줄 균일 막대(y~172 에서 끝). 실제
    내용은 배지+제목, Line 5줄, 그리고 **버튼 다섯 개**(회원보기 top291
    · 보내기 top395 · 발행하기 top511 · 초대하기 top571 · 상한변경확정
    top681)가 y=291~733 을 채운다. 뼈대는 이 442px 짜리 버튼 구역을
    통째로 대표 못 한다 — **페이지 실제 높이의 절반 이상이 뼈대에 자리가
    없다.** 이쪽은 새로 잰 것이다.
- 무엇을 기대했나 — 뼈대의 제일 큰 요소(이름·금액, 또는 버튼들)에 자리가
  있어서 겹쳤을 때 안 움직여야 한다.
- 왜 문제인가 — 눈이 스켈레톤에서 두던 자리와 실제로 읽어야 할 자리가
  완전히 다르다 — 뼈대가 로딩 신호 이상의 역할을 못 한다.
- 심각도 — 5급. Bank 쪽은 규모가 커서(페이지 절반 이상 안 맞음) 체감은
  더 크다.
- 실기기 필요? — 아니다.

### 3. 로그인 화면이 "계정이 없으면"에 침묵한다 (판정 없이 관측만)

- 무엇을 했나 — 로그인 전 화면 전체 텍스트를 읽었다.
- 무엇이 보였나 — "포인트 / 핸들과 암호로 들어가요 / 핸들 / 암호 /
  들어가기 / Mock 이라 암호는 모두 point 예요" 가 전부다. 계정이 없는
  경우에 대한 말이 없다.
- 왜 문제인가 — 판정 안 함(가입 없음은 결함이 아니라고 전달받았다). 다만
  지금 화면은 "계정 있음"을 전제로만 말하고, 없는 사람에게 다음에 뭘 해야
  하는지 아무 것도 안 알려준다는 사실만 적는다.
- 심각도 — 매기지 않음.
- 실기기 필요? — 아니다.

### 엄지 자리(고정 위치) 감사

`position: sticky` 는 코드베이스 전체에 `CapForm.tsx` 하나뿐이다 — 다른
화면에 같은 패턴이 새로 생기진 않았다. Confirm/EnterAmount 의 확정
버튼은 sticky 가 아니라 문서 흐름의 마지막 요소다 — 화면 자체가 그
행동을 위해 존재하므로(목적=행동) 같은 위험이 아니다. Bank 페이지 전폭
버튼은 다섯(회원보기·보내기·발행하기·초대하기·상한변경확정) — 되돌릴 수
없는 셋(보내기·발행하기·상한변경확정)과 가벼운 관리용 둘(회원보기·
초대하기)이 크기로 안 갈리는 것은 지난 라운드에 이미 적었다(W8-3).

### 고친 것이 다른 것을 만들지 않았는지

CapForm 의 sticky(엄지 자리에 되돌릴 수 없는 것이 상주) 자체는 조율이
이미 MOTION.md 에 결함으로 못박았다 — 새로 안 팠다. 다만 실측 하나:
과거(W5-2) 겹침 문제(버튼이 내비게이션 뒤로 잘림)는 사라졌다 — 그 부분은
고쳐졌다. 대신 「상주」자체가 새 문제로 남은 것을 재확인했을 뿐이다.

### 이번에 안 본 것

- Mock 전혀 안 봄 — 전부 실서버.
- 일부러 망가뜨리기(네트워크 끊기, 뒤로가기 연타, 같은 버튼 빠르게 두 번,
  긴 문자열/경계값)를 거의 못 했다 — 시간을 계약 드리프트 추적에 다 썼다.
- 상태 넷(쉼/hover/active/focus) 비교 못 함.
- 역할: 동명이인·낯선 은행 판단·처음 받은 사람은 이번에도 안 씀. 「처음
  만드는 사람」은 썼지만 1번 결함 때문에 끝까지 못 갔다(포인트를 실제로
  못 만들었다).
- MOTION.md 의 IssueBanner 색 띠 자리는 재확인 안 함 — 이미 결함으로
  적혀 있어 새로 볼 것 없다고 판단했다.
